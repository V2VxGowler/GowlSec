1:﻿import { useState, useEffect, useRef, useMemo } from "react";import {  Shield, Trophy, MessageSquare, Lock, Send, Plus, X,  ChevronDown, ChevronUp, Trash2, Unlock, RefreshCw, CheckCircle2,  AlertTriangle, Flag, Award, Activity, Mail, LogOut, Newspaper,  ExternalLink, Gauge, Bot, Sparkles, Pin, Ghost, Skull, Bug, Cat,  Rabbit, Cpu, Flame, Bird, Hash, Megaphone, User as UserIcon, Pencil,  ShoppingCart, CreditCard, Users, Clock, Wifi, Circle, Twitter,  MessageCircle, Globe, KeyRound, Zap, TrendingUp, Radio,  Crown,} from "lucide-react";\r\nimport ConnexionTab from "./components/ConnexionTab";\r\n\r\nconst C = {  bg: "#0A0C10",  panel: "#12151B",  panel2: "#171B22",  line: "#242A34",  text: "#EDEFF2",  muted: "#8A93A3",  primary: "#5B6EF5",  alert: "#FF4D5E",  warn: "#FF9F43",  ok: "#2ED9A3",  gold: "#FFD166",  discord: "#5865F2",};const DISPLAY_FONT = "'Sora', sans-serif";const BODY_FONT = "'Source Sans 3', sans-serif";const MONO_FONT = "'IBM Plex Mono', monospace";const QUESTION_TYPES = [  { key: "urgent", label: "Urgent avant examen", color: C.alert },  { key: "accompagnement", label: "Besoin d'accompagnement", color: C.warn },  { key: "blocage", label: "Blocage technique", color: C.primary },  { key: "question", label: "Question rapide", color: C.ok },];const DIFFICULTIES = [  { key: "facile", label: "Facile", color: C.ok },  { key: "moyen", label: "Moyen", color: C.primary },  { key: "difficile", label: "Difficile", color: C.warn },  { key: "insane", label: "Insane", color: C.alert },];const NEWS_CATEGORIES = [  { key: "annonce", label: "Annonce", color: C.gold },  { key: "faille", label: "Faille / CVE", color: C.alert },  { key: "attaque", label: "Attaque / Fuite", color: C.warn },  { key: "institutionnel", label: "Institutionnel", color: C.primary },  { key: "outils", label: "Outils / CTF", color: C.ok },];const PLATFORMS = ["TryHackMe", "Root-Me"];const LAB_PLATFORMS = [  { key: "htb", label: "Hack The Box" },  { key: "thm", label: "TryHackMe" },  { key: "rootme", label: "Root-Me" },  { key: "autre", label: "Autre" },];const TEAM_MAX_MEMBERS = 16;const LAB_MAX_MEMBERS = 8;const ROOMS = [  { key: "general", label: "GÃƒÂ©nÃƒÂ©ral", desc: "Discussion libre de la communautÃƒÂ©" },  { key: "tryhackme", label: "TryHackMe", desc: "Entraide sur les salles THM" },  { key: "rootme", label: "Root-Me", desc: "Entraide sur les challenges Root-Me" },  { key: "web", label: "Web & Pwn", desc: "Exploitation web, binaire, reverse" },  { key: "forensics", label: "Forensics & Crypto", desc: "Investigation numÃƒÂ©rique, cryptographie" },  { key: "detente", label: "DÃƒÂ©tente", desc: "Hors-sujet, humour, discussions libres" },];const AVATAR_OPTIONS = [  { key: "bird", icon: Bird, color: "#1F2937" },  { key: "ghost", icon: Ghost, color: "#5B6EF5" },  { key: "skull", icon: Skull, color: "#FF4D5E" },  { key: "bug", icon: Bug, color: "#2ED9A3" },  { key: "cat", icon: Cat, color: "#FF9F43" },  { key: "cpu", icon: Cpu, color: "#58A6FF" },  { key: "rabbit", icon: Rabbit, color: "#F477B7" },  { key: "flame", icon: Flame, color: "#FFD166" },];const AVATAR_MAP = Object.fromEntries(AVATAR_OPTIONS.map((a) => [a.key, a]));const BANNER_OPTIONS = [  { key: "indigo", css: "linear-gradient(135deg,#5B6EF5,#1F2A6B)" },  { key: "crimson", css: "linear-gradient(135deg,#FF4D5E,#4A0E17)" },  { key: "forest", css: "linear-gradient(135deg,#2ED9A3,#0B3B2E)" },  { key: "sunset", css: "linear-gradient(135deg,#FF9F43,#5C2400)" },  { key: "night", css: "linear-gradient(135deg,#3A4256,#05060A)" },];const BANNER_MAP = Object.fromEntries(BANNER_OPTIONS.map((b) => [b.key, b.css]));const TEAM_EMOJIS = ["Ã°Å¸â€ºÂ¡Ã¯Â¸Â", "Ã°Å¸Â¦â€°", "Ã°Å¸ÂÂº", "Ã°Å¸Â¦â€¦", "Ã°Å¸Ââ€°", "Ã°Å¸â€Â¥", "Ã¢Å¡Â¡", "Ã°Å¸Å½Â¯", "Ã°Å¸â€™â‚¬", "Ã°Å¸Â§Â ", "Ã°Å¸â€¢Â¸Ã¯Â¸Â", "Ã°Å¸ÂÂ", "Ã°Å¸â€˜Â¾", "Ã°Å¸â€Â", "Ã°Å¸Å¡â‚¬", "Ã°Å¸Å½Â®"];const SHOP_PRODUCTS = [  {    id: "Vip-cyber", icon: Crown,    title: "Pack VIP",    tagline: "Rejoignez l'espace VIP de la communautÃƒÂ© et profitez d'avantages exclusifs rÃƒÂ©servÃƒÂ©s aux membres..",    features: [      "AccÃƒÂ¨s au salon de discussion privÃƒÂ© rÃƒÂ©servÃƒÂ© aux membres VIP",      "Badge VIP exclusif affichÃƒÂ© sur votre profil",      "Photo de profil animÃƒÂ©e",      "BanniÃƒÂ¨re de profil personnalisÃƒÂ©e",      "Pack d'ÃƒÂ©mojis et gif exclusifs utilisables sur le site",      "IcÃƒÂ´ne VIP exclusive",    ],    format: "IcÃƒÂ´ne de membre fondateur, disponible uniquement durant la premiÃƒÂ¨re annÃƒÂ©e",    price: 9.99 , },  {    id: "init-cyber", icon: Shield,    title: "Initiation CybersÃƒÂ©curitÃƒÂ©",    tagline: "Pack DÃƒÂ©couverte.",    features: [      "Vocabulaire et fondamentaux du hacking ÃƒÂ©thique",      "Les bases de la cybersÃƒÂ©curitÃƒÂ©",      "MaÃƒÂ®trisez Linux",      "Installation et prise en main d'un environnement Parrot OS ou Kali Linux en dual boot.",      "AccÃƒÂ¨s au salon Discord dÃƒÂ©diÃƒÂ© aux membres",          ],    format: "Les coachings se dÃƒÂ©roulent ÃƒÂ  distance via Discord, TeamSpeak ou Telegram, avec partage d'ÃƒÂ©cran si nÃƒÂ©cessaire pour un accompagnement pratique et interactif.",    format: "1 semaine Ã¢â‚¬â€œ crÃƒÂ©neaux ÃƒÂ  dÃƒÂ©finir ensemble selon vos disponibilitÃƒÂ©s.",    price: 29.99,  },  ];const SEED_NEWS = [  {    id: "seed-0", ref: "HZ-2026-001", category: "annonce",    title: "Bienvenue sur GowlSec v3",    summary: "Nouveaux salons, teams, salons labs, boutique de formations, profils personnalisables, connexion Discord et assistant IA : la communautÃƒÂ© fait peau neuve. Bonnes dÃƒÂ©couvertes !",    source: "RÃƒÂ©daction GowlSec", url: "", date: "2026-07-13",  },  {    id: "seed-1", ref: "HZ-2026-014", category: "faille",    title: "RoguePlanet : Microsoft corrige une faille 0-day dans Defender",    summary: "Un correctif met fin ÃƒÂ  une vulnÃƒÂ©rabilitÃƒÂ© exploitÃƒÂ©e pour obtenir les privilÃƒÂ¨ges SYSTEM sur Windows 10 et 11 via Microsoft Defender.",    source: "IT-Connect", url: "https://www.it-connect.fr/actualites/actu-securite/", date: "2026-07-09",  },  {    id: "seed-2", ref: "HZ-2026-015", category: "faille",    title: "Chrome corrige plus de 380 bugs, dont une ÃƒÂ©chappatoire de sandbox",    summary: "Une faille use-after-free dans le GPU permettait de sortir du bac ÃƒÂ  sable du navigateur une fois le rendu compromis.",    source: "Malwarebytes", url: "https://www.malwarebytes.com/blog/bugs/2026/07/chrome-needs-another-whopper-update-to-fix-382-security-fixes", date: "2026-07-10",  },  {    id: "seed-3", ref: "HZ-2026-016", category: "attaque",    title: "Fuite de donnÃƒÂ©es chez l'ANTS : plus de 11 millions de comptes",    summary: "Une faille de contrÃƒÂ´le d'accÃƒÂ¨s de type IDOR a permis l'extraction massive d'informations d'identitÃƒÂ©.",    source: "Tech Insider", url: "https://tech-insider.org/fr/cyberattaque-ants-fuite-donnees-2026/", date: "2026-06-02",  },  {    id: "seed-4", ref: "HZ-2026-017", category: "faille",    title: "Januscape : une faille vieille de 16 ans dÃƒÂ©couverte dans KVM",    summary: "Cette vulnÃƒÂ©rabilitÃƒÂ© use-after-free permet ÃƒÂ  une VM de faire planter son hÃƒÂ´te, avec un risque d'ÃƒÂ©vasion en environnement cloud.",    source: "Le Monde Informatique", url: "https://www.lemondeinformatique.fr/securite-informatique-3.html", date: "2026-07-08",  },  {    id: "seed-5", ref: "HZ-2026-018", category: "attaque",    title: "Squidbleed : une faille cachÃƒÂ©e depuis 1997 dans le proxy Squid",    summary: "RestÃƒÂ©e invisible pendant prÃƒÂ¨s de trente ans, cette vulnÃƒÂ©rabilitÃƒÂ© touchant le serveur proxy Squid vient d'ÃƒÂªtre corrigÃƒÂ©e.",    source: "Korben", url: "https://korben.info/categories/cybersecurite/failles-vulnerabilites/", date: "2026-06-01",  },  {    id: "seed-6", ref: "HZ-2026-019", category: "outils",    title: "Nmap 7.96 : de nouveaux scripts NSE pour l'audit web",    summary: "La derniÃƒÂ¨re version du scanner de rÃƒÂ©fÃƒÂ©rence embarque des scripts de dÃƒÂ©tection plus rapides et une meilleure prise en charge d'IPv6.",    source: "Nmap.org", url: "https://nmap.org/", date: "2026-06-20",  },  {    id: "seed-7", ref: "HZ-2026-020", category: "institutionnel",    title: "L'ANSSI publie son panorama annuel de la cybermenace",    summary: "RanÃƒÂ§ongiciels, attaques sur la chaÃƒÂ®ne d'approvisionnement et exploitation de vulnÃƒÂ©rabilitÃƒÂ©s 0-day dominent le paysage des menaces recensÃƒÂ©es.",    source: "CERT-FR", url: "https://www.cert.ssi.gouv.fr/", date: "2026-06-25",  },  {    id: "seed-8", ref: "HZ-2026-021", category: "outils",    title: "Burp Suite 2026.6 amÃƒÂ©liore le fuzzing automatisÃƒÂ©",    summary: "PortSwigger ajoute un moteur de fuzzing plus rapide et de nouvelles extensions communautaires pour l'audit d'API.",    source: "PortSwigger", url: "https://portswigger.net/burp/releases", date: "2026-07-02",  },  {    id: "seed-9", ref: "HZ-2026-022", category: "attaque",    title: "Campagne de phishing ciblant les ÃƒÂ©tudiants en cybersÃƒÂ©curitÃƒÂ©",    summary: "De faux certificats de formation circulent par e-mail pour piÃƒÂ©ger des candidats ÃƒÂ  des certifications junior.",    source: "Cybermalveillance.gouv.fr", url: "https://www.cybermalveillance.gouv.fr/", date: "2026-07-05",  },];const ONLINE_WINDOW_MS = 2 * 60 * 1000;/* ---------------------------------------------------------------------   Internationalisation Ã¢â‚¬â€ sÃƒÂ©lection de langue ÃƒÂ  l'entrÃƒÂ©e du site--------------------------------------------------------------------- */const LANGUAGES = [  { key: "fr", label: "FranÃƒÂ§ais", short: "FR",  },  { key: "en", label: "English", short: "EN",  },  { key: "ru", label: "ÃÂ Ã‘Æ’Ã‘ÂÃ‘ÂÃÂºÃÂ¸ÃÂ¹", short: "RU",  },  { key: "zh", label: "Ã¤Â¸Â­Ã¦â€“â€¡", short: "Ã¤Â¸Â­Ã¦â€“â€¡",  },];const I18N = {  fr: {    chooseLangTitle: "Choisis ta langue",    chooseLangSub: "La communautÃƒÂ© GowlSec parle plusieurs langues Ã¢â‚¬â€ sÃƒÂ©lectionne la tienne pour continuer.",    chooseLangCta: "Continuer",    tagline: "by V2V",    tabs: { accueil: "Accueil", actus: "ActualitÃƒÂ©s", forum: "Question", salons: "Hub", equipes: "Team", labs: "Labs", classement: "Classement", trophies: "TrophÃƒÂ©es", boutique: "Boutique", assistant: "Assistant IA",  "connexion": "Connexion", },    live: "en ligne", admin: "Admin",    heroTitle1: "La communautÃƒÂ© pentest & CTF ", heroTitle2: "qui ne dort jamais",    heroSub: "Ici, on n'apprend pas seul. Pose une question ÃƒÂ  2h du matin, trouve une team pour un CTF, avance ÃƒÂ  plusieurs sur un lab, ou dÃƒÂ©bloque-toi en direct sur le Hub Ã¢â‚¬â€ la communautÃƒÂ© rÃƒÂ©pond.",    membersJoined: "membres inscrits", joinFirst: "Rejoins les tout premiers membres", peerHelp: "entraide entre pairs, pas de cours magistral",    ctaAsk: "Poser une question", ctaHub: "Rejoindre le Hub",    statNews: "actus", statQuestions: "questions", statTrophies: "trophÃƒÂ©es", statTeams: "teams", statLabs: "labs",    activityTitle: "Ãƒâ€¡a bouge en ce moment", activityEmpty: "La communautÃƒÂ© dÃƒÂ©marre Ã¢â‚¬â€ sois le premier ÃƒÂ  agir.",    scanLabel: "corbeau en fouille dans la base de donnÃƒÂ©es", watchAll: "Voir toutes les actualitÃƒÂ©s",    trending: "Ãƒâ€¡a circule en ce moment", live2: "EN DIRECT", watching: "veille active",    footer: "GowlSec Ã¢â‚¬â€ communautÃƒÂ© pentest & CTF Ã‚Â· by V2V Ã‚Â· ÃƒÂ  but pÃƒÂ©dagogique",    forumTitle: "Question", forumSub: "Pose une question ou demande un accompagnement.",    forumHeroEyebrow: "Nouvelle question", forumHeroTitle: "DÃƒÂ©bloque-toi en quelques minutes",    forumHeroSub: "DÃƒÂ©cris ton contexte et ce que tu as dÃƒÂ©jÃƒÂ  essayÃƒÂ© : la communautÃƒÂ© et les mentors rÃƒÂ©pondent en direct, pas de question trop bÃƒÂªte.",    newQuestion: "Nouvelle question", cancel: "Annuler", publish: "Publier",    hubTitle: "Hub", hubSub: "Entraide en direct, par thÃƒÂ¨me.",    hubHeroEyebrow: "Le Hub", hubHeroTitle: "Discute en direct, par thÃƒÂ¨me",    hubHeroSub: "Choisis un salon pour rejoindre la conversation en cours Ã¢â‚¬â€ chacun a sa propre ambiance et son propre sujet.",    teamTitle: "Team", teamSub: "CrÃƒÂ©e ta team (16 membres max), publique ou privÃƒÂ©e, recrute, publiez vos annonces.",    teamHeroEyebrow: "Nouvelle team", teamHeroTitle: "Monte ton escouade de hackers",    teamHeroSub: "Choisis un nom, un logo, jusqu'ÃƒÂ  16 membres. Publique pour recruter en ouvert, privÃƒÂ©e pour rester entre vous.",    newTeam: "CrÃƒÂ©er une ÃƒÂ©quipe",    labsTitle: "Labs", labsSub: "CrÃƒÂ©e ou rejoins un salon pour avancer ensemble sur un lab HTB, TryHackMe, Root-Me... (8 personnes max)",    labsHeroEyebrow: "Nouveau salon lab", labsHeroTitle: "Ouvre une session de lab protÃƒÂ©gÃƒÂ©e",    labsHeroSub: "RÃƒÂ©unis jusqu'ÃƒÂ  8 personnes autour d'un mÃƒÂªme lab HTB, TryHackMe ou Root-Me, avec son propre fil de discussion.",    newLab: "CrÃƒÂ©er un salon lab",    cardForumDesc: "Pose une question sur ton pentest.", cardHubDesc: "Discute en direct par thÃƒÂ¨me avec la communautÃƒÂ©.",    cardTeamDesc: "CrÃƒÂ©e ou rejoins une team (16 max), publique ou privÃƒÂ©e.", cardLabsDesc: "Rejoins un salon pour avancer ÃƒÂ  plusieurs sur un lab (8 max).",    cardClassementDesc: "Qui cumule le plus de points sur GowlSec.", cardBoutiqueDesc: "Des formations pour bien dÃƒÂ©marrer, dÃƒÂ¨s 29Ã¢â€šÂ¬.", cardAssistantDesc: "Un coup de main immÃƒÂ©diat en cas de blocage.",  },  en: {    chooseLangTitle: "Choose your language",    chooseLangSub: "The GowlSec community speaks several languages Ã¢â‚¬â€ pick yours to continue.",    chooseLangCta: "Continue",    tagline: "by V2V",    tabs: { accueil: "Home", actus: "News", forum: "Question", salons: "Hub", equipes: "Team", labs: "Labs", classement: "Leaderboard", trophies: "Trophies", boutique: "Shop", assistant: "AI Assistant", "connexion": "Login", admin: "Admin" },    live: "online", admin: "Admin",    heroTitle1: "The pentest & CTF community ", heroTitle2: "that never sleeps",    heroSub: "Here, you don't learn alone. Ask a question at 2am, find a team for a CTF, tackle a lab together, or get unblocked live on the Hub Ã¢â‚¬â€ the community answers.",    membersJoined: "registered members", joinFirst: "Join the very first members", peerHelp: "peer-to-peer help, not a lecture",    ctaAsk: "Ask a question", ctaHub: "Join the Hub",    statNews: "news", statQuestions: "questions", statTrophies: "trophies", statTeams: "teams", statLabs: "labs",    activityTitle: "Happening right now", activityEmpty: "The community is just getting started Ã¢â‚¬â€ be the first to act.",    scanLabel: "crow scanning the database", watchAll: "See all news",    trending: "Trending now", live2: "LIVE", watching: "active watch",    footer: "GowlSec Ã¢â‚¬â€ pentest & CTF community Ã‚Â· by V2V Ã‚Â· for learning purposes",    forumTitle: "Question", forumSub: "Ask a question or request guidance.",    forumHeroEyebrow: "New question", forumHeroTitle: "Get unblocked in minutes",    forumHeroSub: "Describe your context and what you've already tried: the community and mentors answer live Ã¢â‚¬â€ no question is too silly.",    newQuestion: "New question", cancel: "Cancel", publish: "Publish",    hubTitle: "Hub", hubSub: "Live peer support, by topic.",    hubHeroEyebrow: "The Hub", hubHeroTitle: "Chat live, by topic",    hubHeroSub: "Pick a room to join the ongoing conversation Ã¢â‚¬â€ each one has its own vibe and subject.",    teamTitle: "Team", teamSub: "Create your team (16 members max), public or private, recruit, post announcements.",    teamHeroEyebrow: "New team", teamHeroTitle: "Build your hacker squad",    teamHeroSub: "Pick a name, a logo, up to 16 members. Public to recruit openly, private to stay just among you.",    newTeam: "Create a team",    labsTitle: "Labs", labsSub: "Create or join a room to work together on an HTB, TryHackMe, Root-Me lab... (8 people max)",    labsHeroEyebrow: "New lab room", labsHeroTitle: "Open a protected lab session",    labsHeroSub: "Bring together up to 8 people around the same HTB, TryHackMe or Root-Me lab, with its own discussion thread.",    newLab: "Create a lab room",    cardForumDesc: "Ask a question about your pentest.", cardHubDesc: "Chat live by topic with the community.",    cardTeamDesc: "Create or join a team (16 max), public or private.", cardLabsDesc: "Join a room to progress together on a lab (8 max).",    cardClassementDesc: "Who's stacking the most points on GowlSec.", cardBoutiqueDesc: "Courses to get you started, from Ã¢â€šÂ¬29.", cardAssistantDesc: "Instant help whenever you're stuck.",  },  ru: {    chooseLangTitle: "Ãâ€™Ã‘â€¹ÃÂ±ÃÂµÃ‘â‚¬ÃÂ¸Ã‘â€šÃÂµ Ã‘ÂÃÂ·Ã‘â€¹ÃÂº",    chooseLangSub: "ÃÂ¡ÃÂ¾ÃÂ¾ÃÂ±Ã‘â€°ÃÂµÃ‘ÂÃ‘â€šÃÂ²ÃÂ¾ GowlSec ÃÂ³ÃÂ¾ÃÂ²ÃÂ¾Ã‘â‚¬ÃÂ¸Ã‘â€š ÃÂ½ÃÂ° ÃÂ½ÃÂµÃ‘ÂÃÂºÃÂ¾ÃÂ»Ã‘Å’ÃÂºÃÂ¸Ã‘â€¦ Ã‘ÂÃÂ·Ã‘â€¹ÃÂºÃÂ°Ã‘â€¦ Ã¢â‚¬â€ ÃÂ²Ã‘â€¹ÃÂ±ÃÂµÃ‘â‚¬ÃÂ¸Ã‘â€šÃÂµ Ã‘ÂÃÂ²ÃÂ¾ÃÂ¹, Ã‘â€¡Ã‘â€šÃÂ¾ÃÂ±Ã‘â€¹ ÃÂ¿Ã‘â‚¬ÃÂ¾ÃÂ´ÃÂ¾ÃÂ»ÃÂ¶ÃÂ¸Ã‘â€šÃ‘Å’.",    chooseLangCta: "ÃÅ¸Ã‘â‚¬ÃÂ¾ÃÂ´ÃÂ¾ÃÂ»ÃÂ¶ÃÂ¸Ã‘â€šÃ‘Å’",    tagline: "by V2V",    tabs: { accueil: "Ãâ€œÃÂ»ÃÂ°ÃÂ²ÃÂ½ÃÂ°Ã‘Â", actus: "ÃÂÃÂ¾ÃÂ²ÃÂ¾Ã‘ÂÃ‘â€šÃÂ¸", forum: "Ãâ€™ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘Â", salons: "ÃÂ¥ÃÂ°ÃÂ±", equipes: "ÃÅ¡ÃÂ¾ÃÂ¼ÃÂ°ÃÂ½ÃÂ´ÃÂ°", labs: "Ãâ€ºÃÂ°ÃÂ±Ã‘â€¹", classement: "ÃÂ ÃÂµÃÂ¹Ã‘â€šÃÂ¸ÃÂ½ÃÂ³", trophies: "ÃÂ¢Ã‘â‚¬ÃÂ¾Ã‘â€žÃÂµÃÂ¸", boutique: "ÃÅ“ÃÂ°ÃÂ³ÃÂ°ÃÂ·ÃÂ¸ÃÂ½", assistant: "ÃËœÃËœ-ÃÂ°Ã‘ÂÃ‘ÂÃÂ¸Ã‘ÂÃ‘â€šÃÂµÃÂ½Ã‘â€š", admin: "ÃÂÃÂ´ÃÂ¼ÃÂ¸ÃÂ½" },    live: "ÃÂ¾ÃÂ½ÃÂ»ÃÂ°ÃÂ¹ÃÂ½", admin: "ÃÂÃÂ´ÃÂ¼ÃÂ¸ÃÂ½",    heroTitle1: "ÃÂ¡ÃÂ¾ÃÂ¾ÃÂ±Ã‘â€°ÃÂµÃ‘ÂÃ‘â€šÃÂ²ÃÂ¾ ÃÂ¿ÃÂµÃÂ½Ã‘â€šÃÂµÃ‘ÂÃ‘â€šÃÂ° ÃÂ¸ CTF, ", heroTitle2: "ÃÂºÃÂ¾Ã‘â€šÃÂ¾Ã‘â‚¬ÃÂ¾ÃÂµ ÃÂ½ÃÂ¸ÃÂºÃÂ¾ÃÂ³ÃÂ´ÃÂ° ÃÂ½ÃÂµ Ã‘ÂÃÂ¿ÃÂ¸Ã‘â€š",    heroSub: "Ãâ€”ÃÂ´ÃÂµÃ‘ÂÃ‘Å’ ÃÂ½ÃÂ¸ÃÂºÃ‘â€šÃÂ¾ ÃÂ½ÃÂµ Ã‘Æ’Ã‘â€¡ÃÂ¸Ã‘â€šÃ‘ÂÃ‘Â ÃÂ² ÃÂ¾ÃÂ´ÃÂ¸ÃÂ½ÃÂ¾Ã‘â€¡ÃÂºÃ‘Æ’. Ãâ€”ÃÂ°ÃÂ´ÃÂ°ÃÂ¹ ÃÂ²ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘Â ÃÂ² 2 Ã‘â€¡ÃÂ°Ã‘ÂÃÂ° ÃÂ½ÃÂ¾Ã‘â€¡ÃÂ¸, ÃÂ½ÃÂ°ÃÂ¹ÃÂ´ÃÂ¸ ÃÂºÃÂ¾ÃÂ¼ÃÂ°ÃÂ½ÃÂ´Ã‘Æ’ ÃÂ´ÃÂ»Ã‘Â CTF, ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘â€¦ÃÂ¾ÃÂ´ÃÂ¸Ã‘â€šÃÂµ ÃÂ»ÃÂ°ÃÂ±Ã‘Æ’ ÃÂ²ÃÂ¼ÃÂµÃ‘ÂÃ‘â€šÃÂµ ÃÂ¸ÃÂ»ÃÂ¸ ÃÂ¿ÃÂ¾ÃÂ»Ã‘Æ’Ã‘â€¡ÃÂ¸ ÃÂ¿ÃÂ¾ÃÂ¼ÃÂ¾Ã‘â€°Ã‘Å’ ÃÂ² ÃÂ¿Ã‘â‚¬Ã‘ÂÃÂ¼ÃÂ¾ÃÂ¼ Ã‘ÂÃ‘â€žÃÂ¸Ã‘â‚¬ÃÂµ ÃÂ½ÃÂ° ÃÂ¥ÃÂ°ÃÂ±ÃÂµ Ã¢â‚¬â€ Ã‘ÂÃÂ¾ÃÂ¾ÃÂ±Ã‘â€°ÃÂµÃ‘ÂÃ‘â€šÃÂ²ÃÂ¾ ÃÂ¾Ã‘â€šÃÂ²ÃÂµÃ‘â€šÃÂ¸Ã‘â€š.",    membersJoined: "ÃÂ·ÃÂ°Ã‘â‚¬ÃÂµÃÂ³ÃÂ¸Ã‘ÂÃ‘â€šÃ‘â‚¬ÃÂ¸Ã‘â‚¬ÃÂ¾ÃÂ²ÃÂ°ÃÂ½ÃÂ½Ã‘â€¹Ã‘â€¦ Ã‘Æ’Ã‘â€¡ÃÂ°Ã‘ÂÃ‘â€šÃÂ½ÃÂ¸ÃÂºÃÂ¾ÃÂ²", joinFirst: "ÃÅ¸Ã‘â‚¬ÃÂ¸Ã‘ÂÃÂ¾ÃÂµÃÂ´ÃÂ¸ÃÂ½Ã‘ÂÃÂ¹Ã‘ÂÃ‘Â ÃÂº ÃÂ¿ÃÂµÃ‘â‚¬ÃÂ²Ã‘â€¹ÃÂ¼ Ã‘Æ’Ã‘â€¡ÃÂ°Ã‘ÂÃ‘â€šÃÂ½ÃÂ¸ÃÂºÃÂ°ÃÂ¼", peerHelp: "ÃÂ²ÃÂ·ÃÂ°ÃÂ¸ÃÂ¼ÃÂ¾ÃÂ¿ÃÂ¾ÃÂ¼ÃÂ¾Ã‘â€°Ã‘Å’, ÃÂ° ÃÂ½ÃÂµ ÃÂ»ÃÂµÃÂºÃ‘â€ ÃÂ¸Ã‘Â",    ctaAsk: "Ãâ€”ÃÂ°ÃÂ´ÃÂ°Ã‘â€šÃ‘Å’ ÃÂ²ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘Â", ctaHub: "ÃÅ¸ÃÂµÃ‘â‚¬ÃÂµÃÂ¹Ã‘â€šÃÂ¸ ÃÂ² ÃÂ¥ÃÂ°ÃÂ±",    statNews: "ÃÂ½ÃÂ¾ÃÂ²ÃÂ¾Ã‘ÂÃ‘â€šÃÂ¸", statQuestions: "ÃÂ²ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘ÂÃ‘â€¹", statTrophies: "Ã‘â€šÃ‘â‚¬ÃÂ¾Ã‘â€žÃÂµÃÂ¸", statTeams: "ÃÂºÃÂ¾ÃÂ¼ÃÂ°ÃÂ½ÃÂ´Ã‘â€¹", statLabs: "ÃÂ»ÃÂ°ÃÂ±Ã‘â€¹",    activityTitle: "ÃÅ¸Ã‘â‚¬ÃÂ¾ÃÂ¸Ã‘ÂÃ‘â€¦ÃÂ¾ÃÂ´ÃÂ¸Ã‘â€š ÃÂ¿Ã‘â‚¬Ã‘ÂÃÂ¼ÃÂ¾ Ã‘ÂÃÂµÃÂ¹Ã‘â€¡ÃÂ°Ã‘Â", activityEmpty: "ÃÂ¡ÃÂ¾ÃÂ¾ÃÂ±Ã‘â€°ÃÂµÃ‘ÂÃ‘â€šÃÂ²ÃÂ¾ Ã‘â€šÃÂ¾ÃÂ»Ã‘Å’ÃÂºÃÂ¾ ÃÂ½ÃÂ°Ã‘â€¡ÃÂ¸ÃÂ½ÃÂ°ÃÂµÃ‘â€šÃ‘ÂÃ‘Â Ã¢â‚¬â€ Ã‘ÂÃ‘â€šÃÂ°ÃÂ½Ã‘Å’ ÃÂ¿ÃÂµÃ‘â‚¬ÃÂ²Ã‘â€¹ÃÂ¼.",    scanLabel: "ÃÂ²ÃÂ¾Ã‘â‚¬ÃÂ¾ÃÂ½ ÃÂ¸Ã‘â€°ÃÂµÃ‘â€š ÃÂ² ÃÂ±ÃÂ°ÃÂ·ÃÂµ ÃÂ´ÃÂ°ÃÂ½ÃÂ½Ã‘â€¹Ã‘â€¦", watchAll: "Ãâ€™Ã‘ÂÃÂµ ÃÂ½ÃÂ¾ÃÂ²ÃÂ¾Ã‘ÂÃ‘â€šÃÂ¸",    trending: "ÃÂ¡ÃÂµÃÂ¹Ã‘â€¡ÃÂ°Ã‘Â ÃÂ¾ÃÂ±Ã‘ÂÃ‘Æ’ÃÂ¶ÃÂ´ÃÂ°Ã‘Å½Ã‘â€š", live2: "ÃÅ¸ÃÂ ÃÂ¯ÃÅ“ÃÅ¾Ãâ„¢ ÃÂ­ÃÂ¤ÃËœÃÂ ", watching: "ÃÂ°ÃÂºÃ‘â€šÃÂ¸ÃÂ²ÃÂ½ÃÂ¾ÃÂµ ÃÂ½ÃÂ°ÃÂ±ÃÂ»Ã‘Å½ÃÂ´ÃÂµÃÂ½ÃÂ¸ÃÂµ",    footer: "GowlSec Ã¢â‚¬â€ Ã‘ÂÃÂ¾ÃÂ¾ÃÂ±Ã‘â€°ÃÂµÃ‘ÂÃ‘â€šÃÂ²ÃÂ¾ ÃÂ¿ÃÂµÃÂ½Ã‘â€šÃÂµÃ‘ÂÃ‘â€šÃÂ° ÃÂ¸ CTF Ã‚Â· by V2V Ã‚Â· ÃÂ² Ã‘Æ’Ã‘â€¡ÃÂµÃÂ±ÃÂ½Ã‘â€¹Ã‘â€¦ Ã‘â€ ÃÂµÃÂ»Ã‘ÂÃ‘â€¦",    forumTitle: "Ãâ€™ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘Â", forumSub: "Ãâ€”ÃÂ°ÃÂ´ÃÂ°ÃÂ¹ ÃÂ²ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘Â ÃÂ¸ÃÂ»ÃÂ¸ ÃÂ¿ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘ÂÃÂ¸ ÃÂ¿ÃÂ¾ÃÂ¼ÃÂ¾Ã‘â€°ÃÂ¸.",    forumHeroEyebrow: "ÃÂÃÂ¾ÃÂ²Ã‘â€¹ÃÂ¹ ÃÂ²ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘Â", forumHeroTitle: "ÃÅ¸ÃÂ¾ÃÂ»Ã‘Æ’Ã‘â€¡ÃÂ¸ ÃÂ¿ÃÂ¾ÃÂ¼ÃÂ¾Ã‘â€°Ã‘Å’ ÃÂ·ÃÂ° ÃÂ¿ÃÂ°Ã‘â‚¬Ã‘Æ’ ÃÂ¼ÃÂ¸ÃÂ½Ã‘Æ’Ã‘â€š",    forumHeroSub: "ÃÅ¾ÃÂ¿ÃÂ¸Ã‘Ë†ÃÂ¸ ÃÂºÃÂ¾ÃÂ½Ã‘â€šÃÂµÃÂºÃ‘ÂÃ‘â€š ÃÂ¸ Ã‘â€¡Ã‘â€šÃÂ¾ Ã‘â€šÃ‘â€¹ Ã‘Æ’ÃÂ¶ÃÂµ ÃÂ¿Ã‘â‚¬ÃÂ¾ÃÂ±ÃÂ¾ÃÂ²ÃÂ°ÃÂ»: Ã‘ÂÃÂ¾ÃÂ¾ÃÂ±Ã‘â€°ÃÂµÃ‘ÂÃ‘â€šÃÂ²ÃÂ¾ ÃÂ¸ ÃÂ½ÃÂ°Ã‘ÂÃ‘â€šÃÂ°ÃÂ²ÃÂ½ÃÂ¸ÃÂºÃÂ¸ ÃÂ¾Ã‘â€šÃÂ²ÃÂµÃ‘â€¡ÃÂ°Ã‘Å½Ã‘â€š ÃÂ² ÃÂ¿Ã‘â‚¬Ã‘ÂÃÂ¼ÃÂ¾ÃÂ¼ Ã‘ÂÃ‘â€žÃÂ¸Ã‘â‚¬ÃÂµ Ã¢â‚¬â€ ÃÂ³ÃÂ»Ã‘Æ’ÃÂ¿Ã‘â€¹Ã‘â€¦ ÃÂ²ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘ÂÃÂ¾ÃÂ² ÃÂ½ÃÂµ ÃÂ±Ã‘â€¹ÃÂ²ÃÂ°ÃÂµÃ‘â€š.",    newQuestion: "ÃÂÃÂ¾ÃÂ²Ã‘â€¹ÃÂ¹ ÃÂ²ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘Â", cancel: "ÃÅ¾Ã‘â€šÃÂ¼ÃÂµÃÂ½ÃÂ°", publish: "ÃÅ¾ÃÂ¿Ã‘Æ’ÃÂ±ÃÂ»ÃÂ¸ÃÂºÃÂ¾ÃÂ²ÃÂ°Ã‘â€šÃ‘Å’",    hubTitle: "ÃÂ¥ÃÂ°ÃÂ±", hubSub: "Ãâ€“ÃÂ¸ÃÂ²ÃÂ¾ÃÂµ ÃÂ¾ÃÂ±Ã‘â€°ÃÂµÃÂ½ÃÂ¸ÃÂµ ÃÂ¿ÃÂ¾ Ã‘â€šÃÂµÃÂ¼ÃÂ°ÃÂ¼.",    hubHeroEyebrow: "ÃÂ¥ÃÂ°ÃÂ±", hubHeroTitle: "ÃÅ¾ÃÂ±Ã‘â€°ÃÂ°ÃÂ¹Ã‘ÂÃ‘Â ÃÂ² ÃÂ¿Ã‘â‚¬Ã‘ÂÃÂ¼ÃÂ¾ÃÂ¼ Ã‘ÂÃ‘â€žÃÂ¸Ã‘â‚¬ÃÂµ ÃÂ¿ÃÂ¾ Ã‘â€šÃÂµÃÂ¼ÃÂ°ÃÂ¼",    hubHeroSub: "Ãâ€™Ã‘â€¹ÃÂ±ÃÂµÃ‘â‚¬ÃÂ¸ ÃÂºÃÂ¾ÃÂ¼ÃÂ½ÃÂ°Ã‘â€šÃ‘Æ’, Ã‘â€¡Ã‘â€šÃÂ¾ÃÂ±Ã‘â€¹ ÃÂ¿Ã‘â‚¬ÃÂ¸Ã‘ÂÃÂ¾ÃÂµÃÂ´ÃÂ¸ÃÂ½ÃÂ¸Ã‘â€šÃ‘Å’Ã‘ÂÃ‘Â ÃÂº Ã‘â‚¬ÃÂ°ÃÂ·ÃÂ³ÃÂ¾ÃÂ²ÃÂ¾Ã‘â‚¬Ã‘Æ’ Ã¢â‚¬â€ Ã‘Æ’ ÃÂºÃÂ°ÃÂ¶ÃÂ´ÃÂ¾ÃÂ¹ Ã‘ÂÃÂ²ÃÂ¾Ã‘Â ÃÂ°Ã‘â€šÃÂ¼ÃÂ¾Ã‘ÂÃ‘â€žÃÂµÃ‘â‚¬ÃÂ° ÃÂ¸ Ã‘ÂÃÂ²ÃÂ¾Ã‘Â Ã‘â€šÃÂµÃÂ¼ÃÂ°.",    teamTitle: "ÃÅ¡ÃÂ¾ÃÂ¼ÃÂ°ÃÂ½ÃÂ´ÃÂ°", teamSub: "ÃÂ¡ÃÂ¾ÃÂ·ÃÂ´ÃÂ°ÃÂ¹ ÃÂºÃÂ¾ÃÂ¼ÃÂ°ÃÂ½ÃÂ´Ã‘Æ’ (ÃÂ¼ÃÂ°ÃÂºÃ‘Â. 16 Ã‘Æ’Ã‘â€¡ÃÂ°Ã‘ÂÃ‘â€šÃÂ½ÃÂ¸ÃÂºÃÂ¾ÃÂ²), ÃÂ¿Ã‘Æ’ÃÂ±ÃÂ»ÃÂ¸Ã‘â€¡ÃÂ½Ã‘Æ’Ã‘Å½ ÃÂ¸ÃÂ»ÃÂ¸ ÃÂ¿Ã‘â‚¬ÃÂ¸ÃÂ²ÃÂ°Ã‘â€šÃÂ½Ã‘Æ’Ã‘Å½, ÃÂ½ÃÂ°ÃÂ±ÃÂ¸Ã‘â‚¬ÃÂ°ÃÂ¹ ÃÂ»Ã‘Å½ÃÂ´ÃÂµÃÂ¹, ÃÂ¿Ã‘Æ’ÃÂ±ÃÂ»ÃÂ¸ÃÂºÃ‘Æ’ÃÂ¹ ÃÂ¾ÃÂ±Ã‘Å Ã‘ÂÃÂ²ÃÂ»ÃÂµÃÂ½ÃÂ¸Ã‘Â.",    teamHeroEyebrow: "ÃÂÃÂ¾ÃÂ²ÃÂ°Ã‘Â ÃÂºÃÂ¾ÃÂ¼ÃÂ°ÃÂ½ÃÂ´ÃÂ°", teamHeroTitle: "ÃÂ¡ÃÂ¾ÃÂ±ÃÂµÃ‘â‚¬ÃÂ¸ Ã‘ÂÃÂ²ÃÂ¾ÃÂ¹ ÃÂ¾Ã‘â€šÃ‘â‚¬Ã‘ÂÃÂ´ Ã‘â€¦ÃÂ°ÃÂºÃÂµÃ‘â‚¬ÃÂ¾ÃÂ²",    teamHeroSub: "Ãâ€™Ã‘â€¹ÃÂ±ÃÂµÃ‘â‚¬ÃÂ¸ ÃÂ½ÃÂ°ÃÂ·ÃÂ²ÃÂ°ÃÂ½ÃÂ¸ÃÂµ, ÃÂ»ÃÂ¾ÃÂ³ÃÂ¾Ã‘â€šÃÂ¸ÃÂ¿, ÃÂ´ÃÂ¾ 16 Ã‘Æ’Ã‘â€¡ÃÂ°Ã‘ÂÃ‘â€šÃÂ½ÃÂ¸ÃÂºÃÂ¾ÃÂ². ÃÅ¸Ã‘Æ’ÃÂ±ÃÂ»ÃÂ¸Ã‘â€¡ÃÂ½ÃÂ°Ã‘Â Ã¢â‚¬â€ ÃÂ´ÃÂ»Ã‘Â ÃÂ¾Ã‘â€šÃÂºÃ‘â‚¬Ã‘â€¹Ã‘â€šÃÂ¾ÃÂ³ÃÂ¾ ÃÂ½ÃÂ°ÃÂ±ÃÂ¾Ã‘â‚¬ÃÂ°, ÃÂ¿Ã‘â‚¬ÃÂ¸ÃÂ²ÃÂ°Ã‘â€šÃÂ½ÃÂ°Ã‘Â Ã¢â‚¬â€ Ã‘â€šÃÂ¾ÃÂ»Ã‘Å’ÃÂºÃÂ¾ ÃÂ´ÃÂ»Ã‘Â Ã‘ÂÃÂ²ÃÂ¾ÃÂ¸Ã‘â€¦.",    newTeam: "ÃÂ¡ÃÂ¾ÃÂ·ÃÂ´ÃÂ°Ã‘â€šÃ‘Å’ ÃÂºÃÂ¾ÃÂ¼ÃÂ°ÃÂ½ÃÂ´Ã‘Æ’",    labsTitle: "Ãâ€ºÃÂ°ÃÂ±Ã‘â€¹", labsSub: "ÃÂ¡ÃÂ¾ÃÂ·ÃÂ´ÃÂ°ÃÂ¹ ÃÂ¸ÃÂ»ÃÂ¸ ÃÂ¿Ã‘â‚¬ÃÂ¸Ã‘ÂÃÂ¾ÃÂµÃÂ´ÃÂ¸ÃÂ½ÃÂ¸Ã‘ÂÃ‘Å’ ÃÂº ÃÂºÃÂ¾ÃÂ¼ÃÂ½ÃÂ°Ã‘â€šÃÂµ, Ã‘â€¡Ã‘â€šÃÂ¾ÃÂ±Ã‘â€¹ ÃÂ²ÃÂ¼ÃÂµÃ‘ÂÃ‘â€šÃÂµ ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘â€¦ÃÂ¾ÃÂ´ÃÂ¸Ã‘â€šÃ‘Å’ ÃÂ»ÃÂ°ÃÂ±Ã‘Æ’ HTB, TryHackMe, Root-Me... (ÃÂ¼ÃÂ°ÃÂºÃ‘Â. 8 Ã‘â€¡ÃÂµÃÂ»ÃÂ¾ÃÂ²ÃÂµÃÂº)",    labsHeroEyebrow: "ÃÂÃÂ¾ÃÂ²ÃÂ°Ã‘Â ÃÂ»ÃÂ°ÃÂ±-ÃÂºÃÂ¾ÃÂ¼ÃÂ½ÃÂ°Ã‘â€šÃÂ°", labsHeroTitle: "ÃÅ¾Ã‘â€šÃÂºÃ‘â‚¬ÃÂ¾ÃÂ¹ ÃÂ·ÃÂ°Ã‘â€°ÃÂ¸Ã‘â€°Ã‘â€˜ÃÂ½ÃÂ½Ã‘Æ’Ã‘Å½ Ã‘ÂÃÂµÃ‘ÂÃ‘ÂÃÂ¸Ã‘Å½ ÃÂ»ÃÂ°ÃÂ±Ã‘â€¹",    labsHeroSub: "ÃÂ¡ÃÂ¾ÃÂ±ÃÂµÃ‘â‚¬ÃÂ¸ ÃÂ´ÃÂ¾ 8 Ã‘â€¡ÃÂµÃÂ»ÃÂ¾ÃÂ²ÃÂµÃÂº ÃÂ²ÃÂ¾ÃÂºÃ‘â‚¬Ã‘Æ’ÃÂ³ ÃÂ¾ÃÂ´ÃÂ½ÃÂ¾ÃÂ¹ ÃÂ»ÃÂ°ÃÂ±Ã‘â€¹ HTB, TryHackMe ÃÂ¸ÃÂ»ÃÂ¸ Root-Me, Ã‘ÂÃÂ¾ Ã‘ÂÃÂ²ÃÂ¾ÃÂ¸ÃÂ¼ ÃÂ¾Ã‘â€šÃÂ´ÃÂµÃÂ»Ã‘Å’ÃÂ½Ã‘â€¹ÃÂ¼ Ã‘â€¡ÃÂ°Ã‘â€šÃÂ¾ÃÂ¼.",    newLab: "ÃÂ¡ÃÂ¾ÃÂ·ÃÂ´ÃÂ°Ã‘â€šÃ‘Å’ ÃÂ»ÃÂ°ÃÂ±-ÃÂºÃÂ¾ÃÂ¼ÃÂ½ÃÂ°Ã‘â€šÃ‘Æ’",    cardForumDesc: "Ãâ€”ÃÂ°ÃÂ´ÃÂ°ÃÂ¹ ÃÂ²ÃÂ¾ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘Â ÃÂ¿ÃÂ¾ Ã‘ÂÃÂ²ÃÂ¾ÃÂµÃÂ¼Ã‘Æ’ ÃÂ¿ÃÂµÃÂ½Ã‘â€šÃÂµÃ‘ÂÃ‘â€šÃ‘Æ’.", cardHubDesc: "ÃÅ¾ÃÂ±Ã‘â€°ÃÂ°ÃÂ¹Ã‘ÂÃ‘Â ÃÂ² ÃÂ¿Ã‘â‚¬Ã‘ÂÃÂ¼ÃÂ¾ÃÂ¼ Ã‘ÂÃ‘â€žÃÂ¸Ã‘â‚¬ÃÂµ ÃÂ¿ÃÂ¾ Ã‘â€šÃÂµÃÂ¼ÃÂ°ÃÂ¼ Ã‘Â Ã‘ÂÃÂ¾ÃÂ¾ÃÂ±Ã‘â€°ÃÂµÃ‘ÂÃ‘â€šÃÂ²ÃÂ¾ÃÂ¼.",    cardTeamDesc: "ÃÂ¡ÃÂ¾ÃÂ·ÃÂ´ÃÂ°ÃÂ¹ ÃÂ¸ÃÂ»ÃÂ¸ ÃÂ²Ã‘ÂÃ‘â€šÃ‘Æ’ÃÂ¿ÃÂ¸ ÃÂ² ÃÂºÃÂ¾ÃÂ¼ÃÂ°ÃÂ½ÃÂ´Ã‘Æ’ (ÃÂ¼ÃÂ°ÃÂºÃ‘Â. 16), ÃÂ¿Ã‘Æ’ÃÂ±ÃÂ»ÃÂ¸Ã‘â€¡ÃÂ½Ã‘Æ’Ã‘Å½ ÃÂ¸ÃÂ»ÃÂ¸ ÃÂ¿Ã‘â‚¬ÃÂ¸ÃÂ²ÃÂ°Ã‘â€šÃÂ½Ã‘Æ’Ã‘Å½.", cardLabsDesc: "ÃÅ¸Ã‘â‚¬ÃÂ¸Ã‘ÂÃÂ¾ÃÂµÃÂ´ÃÂ¸ÃÂ½ÃÂ¸Ã‘ÂÃ‘Å’ ÃÂº ÃÂºÃÂ¾ÃÂ¼ÃÂ½ÃÂ°Ã‘â€šÃÂµ, Ã‘â€¡Ã‘â€šÃÂ¾ÃÂ±Ã‘â€¹ ÃÂ²ÃÂ¼ÃÂµÃ‘ÂÃ‘â€šÃÂµ ÃÂ¿Ã‘â‚¬ÃÂ¾Ã‘â€¦ÃÂ¾ÃÂ´ÃÂ¸Ã‘â€šÃ‘Å’ ÃÂ»ÃÂ°ÃÂ±Ã‘Æ’ (ÃÂ¼ÃÂ°ÃÂºÃ‘Â. 8).",    cardClassementDesc: "ÃÅ¡Ã‘â€šÃÂ¾ ÃÂ½ÃÂ°ÃÂ±ÃÂ¸Ã‘â‚¬ÃÂ°ÃÂµÃ‘â€š ÃÂ±ÃÂ¾ÃÂ»Ã‘Å’Ã‘Ë†ÃÂµ ÃÂ²Ã‘ÂÃÂµÃÂ³ÃÂ¾ ÃÂ¾Ã‘â€¡ÃÂºÃÂ¾ÃÂ² ÃÂ½ÃÂ° GowlSec.", cardBoutiqueDesc: "ÃÅ¡Ã‘Æ’Ã‘â‚¬Ã‘ÂÃ‘â€¹ ÃÂ´ÃÂ»Ã‘Â Ã‘ÂÃ‘â€šÃÂ°Ã‘â‚¬Ã‘â€šÃÂ° Ã¢â‚¬â€ ÃÂ¾Ã‘â€š 29Ã¢â€šÂ¬.", cardAssistantDesc: "ÃÅ“ÃÂ³ÃÂ½ÃÂ¾ÃÂ²ÃÂµÃÂ½ÃÂ½ÃÂ°Ã‘Â ÃÂ¿ÃÂ¾ÃÂ¼ÃÂ¾Ã‘â€°Ã‘Å’, ÃÂºÃÂ¾ÃÂ³ÃÂ´ÃÂ° Ã‘â€šÃ‘â€¹ ÃÂ·ÃÂ°Ã‘ÂÃ‘â€šÃ‘â‚¬Ã‘ÂÃÂ».",  },  zh: {    chooseLangTitle: "Ã©â‚¬â€°Ã¦â€¹Â©Ã¤Â½Â Ã§Å¡â€žÃ¨Â¯Â­Ã¨Â¨â‚¬",    chooseLangSub: "GowlSec Ã§Â¤Â¾Ã¥Å’ÂºÃ¤Â½Â¿Ã§â€Â¨Ã¥Â¤Å¡Ã§Â§ÂÃ¨Â¯Â­Ã¨Â¨â‚¬ Ã¢â‚¬â€ Ã¨Â¯Â·Ã©â‚¬â€°Ã¦â€¹Â©Ã¤Â½Â Ã§Å¡â€žÃ¨Â¯Â­Ã¨Â¨â‚¬Ã¤Â»Â¥Ã§Â»Â§Ã§Â»Â­Ã£â‚¬â€š",    chooseLangCta: "Ã§Â»Â§Ã§Â»Â­",    tagline: "by V2V",    tabs: { accueil: "Ã©Â¦â€“Ã©Â¡Âµ", actus: "Ã¨Âµâ€žÃ¨Â®Â¯", forum: "Ã¦ÂÂÃ©â€”Â®", salons: "Ã§Â¤Â¾Ã¥Å’ÂºÃ¤Â¸Â­Ã¥Â¿Æ’", equipes: "Ã¦Ë†ËœÃ©ËœÅ¸", labs: "Ã¥Â®Å¾Ã©ÂªÅ’Ã¥Â®Â¤", classement: "Ã¦Å½â€™Ã¨Â¡Å’Ã¦Â¦Å“", trophies: "Ã¥Â¥â€“Ã¦ÂÂ¯", boutique: "Ã¥â€¢â€ Ã¥Âºâ€”", assistant: "AI Ã¥Å Â©Ã¦â€°â€¹", admin: "Ã§Â®Â¡Ã§Ââ€ " },    live: "Ã¥Å“Â¨Ã§ÂºÂ¿", admin: "Ã§Â®Â¡Ã§Ââ€ ",    heroTitle1: "Ã¦Â°Â¸Ã¤Â¸ÂÃ¦â€°â€œÃ§Æ’Å Ã§Å¡â€ž ", heroTitle2: "Ã¦Â¸â€”Ã©â‚¬ÂÃ¦Âµâ€¹Ã¨Â¯â€¢Ã¤Â¸Å½ CTF Ã§Â¤Â¾Ã¥Å’Âº",    heroSub: "Ã¥Å“Â¨Ã¨Â¿â„¢Ã©â€¡Å’Ã¯Â¼Å’Ã¤Â½Â Ã¤Â¸ÂÃ¦ËœÂ¯Ã¤Â¸â‚¬Ã¤Â¸ÂªÃ¤ÂºÂºÃ¥Â­Â¦Ã¤Â¹Â Ã£â‚¬â€šÃ¥â€¡Å’Ã¦â„¢Â¨Ã¤Â¸Â¤Ã§â€šÂ¹Ã¦ÂÂÃ©â€”Â®Ã£â‚¬ÂÃ§Â»â€žÃ©ËœÅ¸Ã¥Ââ€šÃ¥Å Â  CTFÃ£â‚¬ÂÃ¥â€™Å’Ã¥Â¤Â§Ã¥Â®Â¶Ã¤Â¸â‚¬Ã¨ÂµÂ·Ã¦â€Â»Ã¥â€¦â€¹Ã¥Â®Å¾Ã©ÂªÅ’Ã¥Â®Â¤Ã¯Â¼Å’Ã¦Ë†â€“Ã¥Å“Â¨Ã§Â¤Â¾Ã¥Å’ÂºÃ¤Â¸Â­Ã¥Â¿Æ’Ã¥Â®Å¾Ã¦â€”Â¶Ã¨Å½Â·Ã¥Â¾â€”Ã¥Â¸Â®Ã¥Å Â©Ã¢â‚¬â€Ã¢â‚¬â€Ã§Â¤Â¾Ã¥Å’ÂºÃ¤Â¼Å¡Ã¥â€ºÅ¾Ã¥Âºâ€Ã¤Â½Â Ã£â‚¬â€š",    membersJoined: "Ã¤Â½ÂÃ¦Â³Â¨Ã¥â€ Å’Ã¦Ë†ÂÃ¥â€˜Ëœ", joinFirst: "Ã¥Å Â Ã¥â€¦Â¥Ã¦Å“â‚¬Ã¦â€”Â©Ã§Å¡â€žÃ¤Â¸â‚¬Ã¦â€°Â¹Ã¦Ë†ÂÃ¥â€˜Ëœ", peerHelp: "Ã¥ÂÅ’Ã¤Â¼Â´Ã¤Âºâ€™Ã¥Å Â©Ã¯Â¼Å’Ã¨â‚¬Å’Ã©ÂÅ¾Ã¥Ââ€¢Ã¥Ââ€˜Ã¦Å½Ë†Ã¨Â¯Â¾",    ctaAsk: "Ã¦ÂÂÃ¥â€¡ÂºÃ©â€”Â®Ã©Â¢Ëœ", ctaHub: "Ã¥Å Â Ã¥â€¦Â¥Ã§Â¤Â¾Ã¥Å’ÂºÃ¤Â¸Â­Ã¥Â¿Æ’",    statNews: "Ã¨Âµâ€žÃ¨Â®Â¯", statQuestions: "Ã©â€”Â®Ã©Â¢Ëœ", statTrophies: "Ã¥Â¥â€“Ã¦ÂÂ¯", statTeams: "Ã¦Ë†ËœÃ©ËœÅ¸", statLabs: "Ã¥Â®Å¾Ã©ÂªÅ’Ã¥Â®Â¤",    activityTitle: "Ã¥Â®Å¾Ã¦â€”Â¶Ã¥Å Â¨Ã¦â‚¬Â", activityEmpty: "Ã§Â¤Â¾Ã¥Å’ÂºÃ¥Ë†Å¡Ã¥Ë†Å¡Ã¨ÂµÂ·Ã¦Â­Â¥ Ã¢â‚¬â€ Ã¥Â¿Â«Ã¦ÂÂ¥Ã¦Ë†ÂÃ¤Â¸ÂºÃ§Â¬Â¬Ã¤Â¸â‚¬Ã¤Â¸ÂªÃ¨Â¡Å’Ã¥Å Â¨Ã§Å¡â€žÃ¤ÂºÂºÃ£â‚¬â€š",    scanLabel: "Ã¤Â¹Å’Ã©Â¸Â¦Ã¦Â­Â£Ã¥Å“Â¨Ã¦ÂÅ“Ã§Â´Â¢Ã¦â€¢Â°Ã¦ÂÂ®Ã¥Âºâ€œ", watchAll: "Ã¦Å¸Â¥Ã§Å“â€¹Ã¥â€¦Â¨Ã©Æ’Â¨Ã¨Âµâ€žÃ¨Â®Â¯",    trending: "Ã¨Â¿â€˜Ã¦Å“Å¸Ã§Æ’Â­Ã¨Â®Â®", live2: "Ã§â€ºÂ´Ã¦â€™Â­Ã¤Â¸Â­", watching: "Ã¦Å’ÂÃ§Â»Â­Ã§â€ºâ€˜Ã¦Å½Â§",    footer: "GowlSec Ã¢â‚¬â€ Ã¦Â¸â€”Ã©â‚¬ÂÃ¦Âµâ€¹Ã¨Â¯â€¢Ã¤Â¸Å½ CTF Ã§Â¤Â¾Ã¥Å’Âº Ã‚Â· by V2V Ã‚Â· Ã¤Â»â€¦Ã¤Â¾â€ºÃ¥Â­Â¦Ã¤Â¹Â Ã¤ÂºÂ¤Ã¦ÂµÂ",    forumTitle: "Ã¦ÂÂÃ©â€”Â®", forumSub: "Ã¦ÂÂÃ¥â€¡ÂºÃ©â€”Â®Ã©Â¢ËœÃ¦Ë†â€“Ã¨Â¯Â·Ã¦Â±â€šÃ¦Å’â€¡Ã¥Â¯Â¼Ã£â‚¬â€š",    forumHeroEyebrow: "Ã¦â€“Â°Ã§Å¡â€žÃ¦ÂÂÃ©â€”Â®", forumHeroTitle: "Ã¥â€¡Â Ã¥Ë†â€ Ã©â€™Å¸Ã¥â€ â€¦Ã¨Å½Â·Ã¥Â¾â€”Ã¥Â¸Â®Ã¥Å Â©",    forumHeroSub: "Ã¦ÂÂÃ¨Â¿Â°Ã¤Â½Â Ã§Å¡â€žÃ¨Æ’Å’Ã¦â„¢Â¯Ã¥â€™Å’Ã¥Â·Â²Ã§Â»ÂÃ¥Â°ÂÃ¨Â¯â€¢Ã¨Â¿â€¡Ã§Å¡â€žÃ¦â€“Â¹Ã¦Â³â€¢Ã¯Â¼Å¡Ã§Â¤Â¾Ã¥Å’ÂºÃ¦Ë†ÂÃ¥â€˜ËœÃ¥â€™Å’Ã¥Â¯Â¼Ã¥Â¸Ë†Ã¤Â¼Å¡Ã¥Â®Å¾Ã¦â€”Â¶Ã¥â€ºÅ¾Ã¥Â¤Â Ã¢â‚¬â€ Ã¦Â²Â¡Ã¦Å“â€°Ã©â€”Â®Ã©Â¢ËœÃ¦ËœÂ¯Ã¦â€žÅ¡Ã¨Â Â¢Ã§Å¡â€žÃ£â‚¬â€š",    newQuestion: "Ã¦â€“Â°Ã§Å¡â€žÃ¦ÂÂÃ©â€”Â®", cancel: "Ã¥Ââ€“Ã¦Â¶Ë†", publish: "Ã¥Ââ€˜Ã¥Â¸Æ’",    hubTitle: "Ã§Â¤Â¾Ã¥Å’ÂºÃ¤Â¸Â­Ã¥Â¿Æ’", hubSub: "Ã¦Å’â€°Ã¤Â¸Â»Ã©Â¢ËœÃ¥Â®Å¾Ã¦â€”Â¶Ã¤Âºâ€™Ã¥Å Â©Ã¤ÂºÂ¤Ã¦ÂµÂÃ£â‚¬â€š",    hubHeroEyebrow: "Ã§Â¤Â¾Ã¥Å’ÂºÃ¤Â¸Â­Ã¥Â¿Æ’", hubHeroTitle: "Ã¦Å’â€°Ã¤Â¸Â»Ã©Â¢ËœÃ¥Â®Å¾Ã¦â€”Â¶Ã¨ÂÅ Ã¥Â¤Â©",    hubHeroSub: "Ã©â‚¬â€°Ã¦â€¹Â©Ã¤Â¸â‚¬Ã¤Â¸ÂªÃ¦Ë†Â¿Ã©â€”Â´Ã¥Å Â Ã¥â€¦Â¥Ã¦Â­Â£Ã¥Å“Â¨Ã¨Â¿â€ºÃ¨Â¡Å’Ã§Å¡â€žÃ¥Â¯Â¹Ã¨Â¯Â Ã¢â‚¬â€ Ã¦Â¯ÂÃ¤Â¸ÂªÃ¦Ë†Â¿Ã©â€”Â´Ã©Æ’Â½Ã¦Å“â€°Ã¨â€¡ÂªÃ¥Â·Â±Ã§Å¡â€žÃ¦Â°â€ºÃ¥â€ºÂ´Ã¥â€™Å’Ã¤Â¸Â»Ã©Â¢ËœÃ£â‚¬â€š",    teamTitle: "Ã¦Ë†ËœÃ©ËœÅ¸", teamSub: "Ã¥Ë†â€ºÃ¥Â»ÂºÃ¤Â½Â Ã§Å¡â€žÃ¦Ë†ËœÃ©ËœÅ¸Ã¯Â¼Ë†Ã¦Å“â‚¬Ã¥Â¤Å¡16Ã¤ÂºÂºÃ¯Â¼â€°Ã¯Â¼Å’Ã¥â€¦Â¬Ã¥Â¼â‚¬Ã¦Ë†â€“Ã§Â§ÂÃ¥Â¯â€ Ã¯Â¼Å’Ã¦â€¹â€ºÃ¥â€¹Å¸Ã¦Ë†ÂÃ¥â€˜ËœÃ¯Â¼Å’Ã¥Ââ€˜Ã¥Â¸Æ’Ã¥â€¦Â¬Ã¥â€˜Å Ã£â‚¬â€š",    teamHeroEyebrow: "Ã¦â€“Â°Ã¥Â»ÂºÃ¦Ë†ËœÃ©ËœÅ¸", teamHeroTitle: "Ã§Â»â€žÃ¥Â»ÂºÃ¤Â½Â Ã§Å¡â€žÃ©Â»â€˜Ã¥Â®Â¢Ã¥Â°ÂÃ©ËœÅ¸",    teamHeroSub: "Ã©â‚¬â€°Ã¦â€¹Â©Ã¥ÂÂÃ§Â§Â°Ã£â‚¬ÂÃ©ËœÅ¸Ã¥Â¾Â½Ã¯Â¼Å’Ã¦Å“â‚¬Ã¥Â¤Å¡16Ã¥ÂÂÃ¦Ë†ÂÃ¥â€˜ËœÃ£â‚¬â€šÃ¥â€¦Â¬Ã¥Â¼â‚¬Ã¦â€¹â€ºÃ¥â€¹Å¸Ã¦Ë†â€“Ã§Â§ÂÃ¥Â¯â€ Ã¤Â»â€¦Ã©â„¢ÂÃ¥â€ â€¦Ã©Æ’Â¨Ã£â‚¬â€š",    newTeam: "Ã¥Ë†â€ºÃ¥Â»ÂºÃ¦Ë†ËœÃ©ËœÅ¸",    labsTitle: "Ã¥Â®Å¾Ã©ÂªÅ’Ã¥Â®Â¤", labsSub: "Ã¥Ë†â€ºÃ¥Â»ÂºÃ¦Ë†â€“Ã¥Å Â Ã¥â€¦Â¥Ã¦Ë†Â¿Ã©â€”Â´Ã¯Â¼Å’Ã¤Â¸â‚¬Ã¨ÂµÂ·Ã¦â€Â»Ã¥â€¦â€¹ HTBÃ£â‚¬ÂTryHackMeÃ£â‚¬ÂRoot-Me Ã§Å¡â€žÃ¥Â®Å¾Ã©ÂªÅ’Ã¥Â®Â¤...Ã¯Â¼Ë†Ã¦Å“â‚¬Ã¥Â¤Å¡8Ã¤ÂºÂºÃ¯Â¼â€°",    labsHeroEyebrow: "Ã¦â€“Â°Ã¥Â»ÂºÃ¥Â®Å¾Ã©ÂªÅ’Ã¥Â®Â¤Ã¦Ë†Â¿Ã©â€”Â´", labsHeroTitle: "Ã¥Â¼â‚¬Ã¥ÂÂ¯Ã¤Â¸â‚¬Ã¤Â¸ÂªÃ¥Ââ€”Ã¤Â¿ÂÃ¦Å Â¤Ã§Å¡â€žÃ¥Â®Å¾Ã©ÂªÅ’Ã¥Â®Â¤Ã¤Â¼Å¡Ã¨Â¯Â",    labsHeroSub: "Ã¥ÂÂ¬Ã©â€ºâ€ Ã¦Å“â‚¬Ã¥Â¤Å¡8Ã¤ÂºÂºÃ¥â€ºÂ´Ã§Â»â€¢Ã¥ÂÅ’Ã¤Â¸â‚¬Ã¤Â¸Âª HTBÃ£â‚¬ÂTryHackMe Ã¦Ë†â€“ Root-Me Ã¥Â®Å¾Ã©ÂªÅ’Ã¥Â®Â¤Ã¯Â¼Å’Ã¦â€¹Â¥Ã¦Å“â€°Ã¤Â¸â€œÃ¥Â±Å¾Ã¨Â®Â¨Ã¨Â®ÂºÃ¥Å’ÂºÃ£â‚¬â€š",    newLab: "Ã¥Ë†â€ºÃ¥Â»ÂºÃ¥Â®Å¾Ã©ÂªÅ’Ã¥Â®Â¤Ã¦Ë†Â¿Ã©â€”Â´",    cardForumDesc: "Ã¥Â°Â±Ã¤Â½Â Ã§Å¡â€žÃ¦Â¸â€”Ã©â‚¬ÂÃ¦Âµâ€¹Ã¨Â¯â€¢Ã¦ÂÂÃ¥â€¡ÂºÃ©â€”Â®Ã©Â¢ËœÃ£â‚¬â€š", cardHubDesc: "Ã¦Å’â€°Ã¤Â¸Â»Ã©Â¢ËœÃ¤Â¸Å½Ã§Â¤Â¾Ã¥Å’ÂºÃ¥Â®Å¾Ã¦â€”Â¶Ã¨ÂÅ Ã¥Â¤Â©Ã£â‚¬â€š",    cardTeamDesc: "Ã¥Ë†â€ºÃ¥Â»ÂºÃ¦Ë†â€“Ã¥Å Â Ã¥â€¦Â¥Ã¦Ë†ËœÃ©ËœÅ¸Ã¯Â¼Ë†Ã¦Å“â‚¬Ã¥Â¤Å¡16Ã¤ÂºÂºÃ¯Â¼â€°Ã¯Â¼Å’Ã¥â€¦Â¬Ã¥Â¼â‚¬Ã¦Ë†â€“Ã§Â§ÂÃ¥Â¯â€ Ã£â‚¬â€š", cardLabsDesc: "Ã¥Å Â Ã¥â€¦Â¥Ã¦Ë†Â¿Ã©â€”Â´Ã¯Â¼Å’Ã¤Â¸Å½Ã¤Â»â€“Ã¤ÂºÂºÃ¤Â¸â‚¬Ã¨ÂµÂ·Ã¦â€Â»Ã¥â€¦â€¹Ã¥Â®Å¾Ã©ÂªÅ’Ã¥Â®Â¤Ã¯Â¼Ë†Ã¦Å“â‚¬Ã¥Â¤Å¡8Ã¤ÂºÂºÃ¯Â¼â€°Ã£â‚¬â€š",    cardClassementDesc: "Ã§Å“â€¹Ã§Å“â€¹Ã¨Â°ÂÃ¥Å“Â¨ GowlSec Ã¤Â¸Å Ã§Â§Â¯Ã§Â´Â¯Ã¤Âºâ€ Ã¦Å“â‚¬Ã¥Â¤Å¡Ã§Â§Â¯Ã¥Ë†â€ Ã£â‚¬â€š", cardBoutiqueDesc: "Ã¤Â»Å½ 29Ã¢â€šÂ¬ Ã¨ÂµÂ·Ã§Å¡â€žÃ¥â€¦Â¥Ã©â€”Â¨Ã¨Â¯Â¾Ã§Â¨â€¹Ã£â‚¬â€š", cardAssistantDesc: "Ã©Ââ€¡Ã¥Ë†Â°Ã¥â€ºÂ°Ã©Å¡Â¾Ã¦â€”Â¶Ã§Â«â€¹Ã¥ÂÂ³Ã¨Å½Â·Ã¥Â¾â€”Ã¥Â¸Â®Ã¥Å Â©Ã£â‚¬â€š",  },};function t(lang, key) {  const dict = I18N[lang] || I18N.fr;  return dict[key] ?? I18N.fr[key] ?? key;}function uid() {  return (crypto?.randomUUID?.() || `${Date.now()}-${Math.random().toString(16).slice(2)}`);}function timeAgo(iso) {  const d = new Date(iso);  const diff = Math.max(0, Date.now() - d.getTime());  const m = Math.floor(diff / 60000);  if (m < 1) return "ÃƒÂ  l'instant";  if (m < 60) return `il y a ${m} min`;  const h = Math.floor(m / 60);  if (h < 24) return `il y a ${h} h`;  const j = Math.floor(h / 24);  if (j < 30) return `il y a ${j} j`;  return new Date(iso).toLocaleDateString("fr-FR");}function dayOfYear(d = new Date()) {  const start = new Date(d.getFullYear(), 0, 0);  return Math.floor((d - start) / 86400000);}async function hashText(text) {  try {    const enc = new TextEncoder().encode(text);    const buf = await crypto.subtle.digest("SHA-256", enc);    return Array.from(new Uint8Array(buf)).map((b) => b.toString(16).padStart(2, "0")).join("");  } catch {    return `plain:${text}`;  }}function passwordChecks(pw) {  return {    length: pw.length >= 8,    upper: /[A-Z]/.test(pw),    lower: /[a-z]/.test(pw),    digit: /[0-9]/.test(pw),    special: /[^A-Za-z0-9]/.test(pw),  };}/* ---------------------------------------------------------------------   Storage helpers (shared = visible ÃƒÂ  tous ; personal = propre ÃƒÂ  l'appareil)--------------------------------------------------------------------- */async function loadCollection(key, fallback, shared = true) {  try {    const res = await window.storage.get(key, shared);    if (!res) return fallback;    return JSON.parse(res.value);  } catch {    return fallback;  }}async function saveCollection(key, value, shared = true) {  try {    await window.storage.set(key, JSON.stringify(value), shared);  } catch { /* best effort */ }}/* ---------------------------------------------------------------------   UI atoms--------------------------------------------------------------------- */function Chip({ label, color }) {  return (    <span className="inline-flex items-center gap-1.5 px-2 py-0.5 text-xs rounded"      style={{ color, border: `1px solid ${color}55`, background: `${color}14`, fontFamily: MONO_FONT }}>      <span className="w-1.5 h-1.5 rounded-full" style={{ background: color }} />      {label}    </span>  );}function Panel({ children, className = "", style = {}, onClick }) {  return <div onClick={onClick} className={`rounded-lg ${className}`} style={{ background: C.panel, border: `1px solid ${C.line}`, ...style }}>{children}</div>;}function Field({ label, children }) {  return (    <label className="block mb-3">      <span className="block mb-1 text-xs uppercase tracking-wide" style={{ color: C.muted, fontFamily: MONO_FONT }}>{label}</span>      {children}    </label>  );}const inputStyle = { background: C.panel2, border: `1px solid ${C.line}`, color: C.text, fontFamily: BODY_FONT };function PrimaryButton({ children, onClick, type = "button", disabled, style = {} }) {  return (    <button type={type} onClick={onClick} disabled={disabled}      className="inline-flex items-center gap-2 px-4 py-2 rounded-md text-sm font-semibold transition-opacity disabled:opacity-40"      style={{ background: C.primary, color: "#fff", fontFamily: BODY_FONT, ...style }}>      {children}    </button>  );}function GhostButton({ children, onClick, danger }) {  return (    <button onClick={onClick} className="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-md text-xs transition-colors"      style={{ border: `1px solid ${danger ? C.alert + "66" : C.line}`, color: danger ? C.alert : C.muted, background: "transparent", fontFamily: BODY_FONT }}>      {children}    </button>  );}function EmptyState({ text }) {  return <div className="text-center py-10"><p className="text-sm" style={{ color: C.muted, fontFamily: BODY_FONT }}>{text}</p></div>;}function StatChip({ icon, label, value }) {  return (    <div className="flex items-center gap-2 px-3 py-1.5 rounded-md" style={{ border: `1px solid ${C.line}`, background: C.panel }}>      <span style={{ color: C.primary }}>{icon}</span>      <span className="text-sm font-semibold" style={{ color: C.text, fontFamily: BODY_FONT }}>{value}</span>      <span className="text-xs" style={{ color: C.muted, fontFamily: BODY_FONT }}>{label}</span>    </div>  );}function Avatar({ profile, size = 32 }) {  const a = AVATAR_MAP[profile?.avatarKey] || AVATAR_OPTIONS[0];  const Icon = a.icon;  return (    <div className="rounded-full flex items-center justify-center shrink-0" style={{ width: size, height: size, background: a.color }}>      <Icon size={Math.round(size * 0.55)} color="#fff" />    </div>  );}function MemberAvatarStack({ profiles = [], max = 6 }) {  const shown = profiles.slice(0, max);  return (    <div className="flex -space-x-2.5">      {shown.length === 0        ? AVATAR_OPTIONS.slice(0, 5).map((a, i) => {            const Icon = a.icon;            return (              <div key={a.key} className="rounded-full flex items-center justify-center" style={{ width: 30, height: 30, background: a.color, border: `2px solid ${C.bg}`, zIndex: 10 - i }}>                <Icon size={15} color="#fff" />              </div>            );          })        : shown.map((p, i) => (            <div key={p.id} className="rounded-full" style={{ border: `2px solid ${C.bg}`, zIndex: 10 - i }}>              <Avatar profile={p} size={30} />            </div>          ))}    </div>  );}function TeamLogo({ team, size = 40 }) {  if (team.logoType === "image" && team.logoValue) {    return <img src={team.logoValue} alt="" className="rounded-md object-cover shrink-0" style={{ width: size, height: size }} />;  }  return (    <div className="rounded-md flex items-center justify-center shrink-0" style={{ width: size, height: size, background: C.panel2, border: `1px solid ${C.line}`, fontSize: Math.round(size * 0.5) }}>      {team.logoValue || "Ã°Å¸â€ºÂ¡Ã¯Â¸Â"}    </div>  );}/* ---------------------------------------------------------------------   Logo Ã¢â‚¬â€ ÃƒÂ©cusson V2V--------------------------------------------------------------------- */function CrowLogo({ size = 26 }) {  return (    <svg width={size} height={size} viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg">      <path d="M32 4 58 14v16c0 18-11.5 28.5-26 30C17.5 58.5 6 48 6 30V14L32 4Z" fill={C.panel2} stroke={C.primary} strokeWidth="2" />      <path d="M14 21 6 17l8 10Z" fill={C.primary} opacity="0.75" />      <path d="M50 21 58 17l-8 10Z" fill={C.primary} opacity="0.75" />      <text x="32" y="39" textAnchor="middle" fontFamily="Sora, sans-serif" fontWeight="800" fontSize="18" fill={C.text}>V2V</text>    </svg>  );}/* ---------------------------------------------------------------------   Indicateur "en ligne" Ã¢â‚¬â€ header--------------------------------------------------------------------- */function OnlineIndicator() {  const [count, setCount] = useState(null);  useEffect(() => {    let active = true;    async function refresh() {      try {        const listing = await window.storage.list("gowlsec:presence:", true);        const keys = listing?.keys || [];        const results = await Promise.all(keys.map(async (k) => {          try {            const r = await window.storage.get(k, true);            return r ? JSON.parse(r.value) : null;          } catch { return null; }        }));        if (!active) return;        const now = Date.now();        setCount(results.filter(Boolean).filter((p) => now - new Date(p.lastSeen).getTime() < ONLINE_WINDOW_MS).length);      } catch { /* best effort */ }    }    refresh();    const interval = setInterval(refresh, 30000);    return () => { active = false; clearInterval(interval); };  }, []);  if (count === null) return null;  return (    <span className="hidden lg:inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs" style={{ border: `1px solid ${C.line}`, color: C.muted, fontFamily: MONO_FONT }}>      <Circle size={7} fill={C.ok} color={C.ok} /> {count} en ligne    </span>  );}/* ---------------------------------------------------------------------   Badge "live" pulsant Ã¢â‚¬â€ pour dynamiser l'accueil--------------------------------------------------------------------- */function LivePulseBadge({ count }) {  return (    <span className="inline-flex items-center gap-2 px-3 py-1.5 rounded-full text-xs" style={{ background: `${C.ok}14`, border: `1px solid ${C.ok}40`, color: C.ok, fontFamily: MONO_FONT }}>      <span className="relative inline-flex w-2 h-2">        <span className="absolute inline-flex w-full h-full rounded-full" style={{ background: C.ok, animation: "gowl-ping-ring 1.8s cubic-bezier(0,0,0.2,1) infinite" }} />        <span className="relative inline-flex w-2 h-2 rounded-full" style={{ background: C.ok }} />      </span>      {count === null ? "communautÃƒÂ© active" : `${count} en ligne maintenant`}    </span>  );}/* ---------------------------------------------------------------------   SÃƒÂ©lection de langue Ã¢â‚¬â€ ÃƒÂ©cran d'entrÃƒÂ©e + mini switcher dans le header--------------------------------------------------------------------- */function LanguageGate({ onChoose }) {  return (    <div className="fixed inset-0 z-[100] flex items-center justify-center p-4" style={{ background: `${C.bg}F5`, backdropFilter: "blur(6px)" }}>      <div className="w-full max-w-md rounded-2xl p-6 gowl-fade-up relative overflow-hidden" style={{ background: C.panel, border: `1px solid ${C.line}`, boxShadow: "0 24px 60px -20px rgba(0,0,0,0.6)" }}>        <div aria-hidden className="absolute rounded-full pointer-events-none" style={{ width: 260, height: 260, top: -110, right: -90, background: `radial-gradient(circle, ${C.primary}30, transparent 70%)`, filter: "blur(10px)" }} />        <div className="flex items-center gap-2.5 mb-5 relative">          <CrowLogo size={28} />          <span className="font-extrabold text-lg" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>GowlSec</span>        </div>        <h2 className="text-lg font-bold mb-1.5 relative" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Choose your language Ã‚Â· Choisis ta langue</h2>        <p className="text-xs mb-5 relative" style={{ color: C.muted, fontFamily: BODY_FONT }}>Ã©â‚¬â€°Ã¦â€¹Â©Ã¨Â¯Â­Ã¨Â¨â‚¬ Ã‚Â· Ãâ€™Ã‘â€¹ÃÂ±ÃÂµÃ‘â‚¬ÃÂ¸Ã‘â€šÃÂµ Ã‘ÂÃÂ·Ã‘â€¹ÃÂº</p>        <div className="grid grid-cols-2 gap-2.5 relative">          {LANGUAGES.map((l) => (            <button key={l.key} onClick={() => onChoose(l.key)} className="flex items-center gap-2.5 px-4 py-3 rounded-xl text-left transition-all hover:-translate-y-0.5"              style={{ background: C.panel2, border: `1px solid ${C.line}` }}              onMouseEnter={(e) => { e.currentTarget.style.borderColor = C.primary; }}              onMouseLeave={(e) => { e.currentTarget.style.borderColor = C.line; }}>              <span className="text-2xl">{l.flag}</span>              <span className="text-sm font-semibold" style={{ color: C.text, fontFamily: BODY_FONT }}>{l.label}</span>            </button>          ))}        </div>      </div>    </div>  );}function LangSwitcher({ lang, onChoose }) {  const [open, setOpen] = useState(false);  const current = LANGUAGES.find((l) => l.key === lang) || LANGUAGES[0];  return (    <div className="relative">      <button onClick={() => setOpen((o) => !o)} className="flex items-center gap-1.5 px-2.5 py-1.5 rounded-lg text-xs font-semibold" style={{ background: C.panel2, border: `1px solid ${C.line}`, color: C.text, fontFamily: MONO_FONT }}>        <span>{current.flag}</span> {current.short}      </button>      {open && (        <>          <div className="fixed inset-0 z-40" onClick={() => setOpen(false)} />          <div className="absolute right-0 top-full mt-1.5 py-1 rounded-lg z-50 min-w-[140px]" style={{ background: C.panel, border: `1px solid ${C.line}`, boxShadow: "0 12px 30px -10px rgba(0,0,0,0.6)" }}>            {LANGUAGES.map((l) => (              <button key={l.key} onClick={() => { onChoose(l.key); setOpen(false); }} className="flex items-center gap-2 w-full px-3 py-1.5 text-xs text-left"                style={{ color: l.key === lang ? C.primary : C.text, background: l.key === lang ? `${C.primary}14` : "transparent", fontFamily: BODY_FONT }}>                <span>{l.flag}</span> {l.label}              </button>            ))}          </div>        </>      )}    </div>  );}/* ---------------------------------------------------------------------   Auth widget (haut ÃƒÂ  droite) Ã¢â‚¬â€ Discord + email/mot de passe--------------------------------------------------------------------- */function AuthWidget({ currentUser, setCurrentUser, profiles, setProfiles, credentials, setCredentials, setTab }) {  const [open, setOpen] = useState(false);  const [mode, setMode] = useState("login");  const [email, setEmail] = useState("");  const [username, setUsername] = useState("");  const [password, setPassword] = useState("");  const [confirm, setConfirm] = useState("");  const [avatarKey, setAvatarKey] = useState(AVATAR_OPTIONS[0].key);  const [error, setError] = useState("");  const [busy, setBusy] = useState(false);  const [isPremium, setIsPremium] = useState(false);  function reset() {    setEmail(""); setUsername(""); setPassword(""); setConfirm(""); setError(""); setBusy(false); setIsPremium(false);  }  async function connectDiscord() {    setBusy(true);    const tag = String(Math.floor(1000 + Math.random() * 9000));    // Calculate trial expiration (7 days from now)    const trialExpiration = new Date();    trialExpiration.setDate(trialExpiration.getDate() + 7);    const profile = {      id: uid(), email: null, username: `Membre${tag}`, provider: "discord",      avatarKey: "ghost", banner: "indigo", bannerImage: "", joinedAt: new Date().toISOString(),      socials: { github: "", twitter, discord: "" },      isPremium: true,      premiumUntil: trialExpiration.toISOString(),    };    const next = [...profiles, profile];    setProfiles(next);    await saveCollection("gowlsec:profiles", next);    setCurrentUser(profile);    setOpen(false); reset();  }  async function submitRegister(e) {    e.preventDefault();    setError("");    const clean = email.trim().toLowerCase();    if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(clean)) return setError("Adresse e-mail invalide.");    if (profiles.some((p) => p.email === clean)) return setError("Un compte existe dÃƒÂ©jÃƒÂ  avec cet e-mail.");    if (!/^[a-zA-Z0-9_]{3,20}$/.test(username.trim())) return setError("Nom d'utilisateur : 3 ÃƒÂ  20 caractÃƒÂ¨res (lettres, chiffres, _).");    if (profiles.some((p) => p.username.toLowerCase() === username.trim().toLowerCase())) return setError("Ce nom d'utilisateur est dÃƒÂ©jÃƒÂ  pris.");    const checks = passwordChecks(password);    if (!Object.values(checks).every(Boolean)) return setError("Le mot de passe ne respecte pas tous les critÃƒÂ¨res.");    if (password !== confirm) return setError("Les mots de passe ne correspondent pas.");    setBusy(true);    const hash = await hashText(password);    // Calculate trial expiration (7 days from now) if premium    const premiumUntil = isPremium ? new Date(Date.now() + 7 * 24 * 60 * 60 * 1000).toISOString() : null;    const profile = {      id: uid(), email: clean, username: username.trim(), provider: "email",      avatarKey, banner: "indigo", bannerImage: "", joinedAt: new Date().toISOString(),      socials: { github: "", twitter, discord: "" },      isPremium,      premiumUntil,    };    const nextProfiles = [...profiles, profile];    const nextCreds = [...credentials, { email: clean, hash }];    setProfiles(nextProfiles); setCredentials(nextCreds);    await Promise.all([      saveCollection("gowlsec:profiles", nextProfiles),      saveCollection("gowlsec:credentials", nextCreds, false),    ]);    setCurrentUser(profile);    setOpen(false); reset();  }  async function submitLogin(e) {    e.preventDefault();    setError("");    const clean = email.trim().toLowerCase();    const profile = profiles.find((p) => p.email === clean);    if (!profile) return setError("Aucun compte trouvÃƒÂ© avec cet e-mail.");    const cred = credentials.find((c) => c.email === clean);    if (!cred) return setError("Mot de passe non reconnu sur cet appareil (dÃƒÂ©mo sans back-end : reconnecte-toi depuis l'appareil oÃƒÂ¹ le compte a ÃƒÂ©tÃƒÂ© crÃƒÂ©ÃƒÂ©, ou utilise Discord).");    setBusy(true);    const hash = await hashText(password);    if (hash !== cred.hash) { setBusy(false); return setError("Mot de passe incorrect."); }    // Preserve existing premium status if any    const updatedProfile = {      ...profile,      isPremium: profile.isPremium || false,      premiumUntil: profile.premiumUntil || null,    };    setCurrentUser(updatedProfile);    setOpen(false); reset();  }  if (currentUser) {    return (      <div className="flex items-center gap-2">        <button onClick={() => setTab("profil")} className="flex items-center gap-2">          <Avatar profile={currentUser} size={30} />          <span className="text-sm hidden sm:inline" style={{ color: C.text, fontFamily: BODY_FONT }}>{currentUser.username}</span>        </button>        <button onClick={() => setCurrentUser(null)} title="Se dÃƒÂ©connecter" style={{ color: C.muted }}><LogOut size={15} /></button>      </div>    );  }  const checks = passwordChecks(password);  return (    <div className="relative">      <button onClick={() => setOpen((o) => !o)} className="inline-flex items-center gap-2 px-3 py-1.5 rounded-md text-sm font-semibold"        style={{ background: C.primary, color: "#fff", fontFamily: BODY_FONT }}>        <Mail size={14} /> Connexion      </button>      {open && (        <div className="absolute right-0 mt-2 w-80 p-4 rounded-lg z-20 max-h-[80vh] overflow-y-auto"          style={{ background: C.panel, border: `1px solid ${C.line}`, boxShadow: "0 12px 32px rgba(0,0,0,0.5)" }}>          <div className="flex items-center justify-between mb-3">            <span className="text-sm font-semibold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Rejoindre GowlSec</span>            <button onClick={() => { setOpen(false); reset(); }} style={{ color: C.muted }}><X size={15} /></button>          </div>          {/* Premium/Free Selection Cards */}          <div className="space-y-4 mb-4">            {/* Premium Card */}            <div className="flex items-center gap-3 p-4 rounded-lg"                 style={{                   background: isPremium ? `${C.primary}1A` : C.panel2,                   border: `1px solid ${isPremium ? C.primary : C.line}`,                   boxShadow: isPremium ? `0 0 0 2px ${C.primary}33` : "none"                 }}>              <div className="flex-1">                <h3 className="text-sm font-semibold mb-1" style={{ color: C.text, fontFamily: BODY_FONT }}>                  Premium                </h3>                <p className="text-xs mb-2" style={{ color: C.muted, fontFamily: BODY_FONT }}>                  AccÃƒÂ¨s complet ÃƒÂ  toutes les fonctionnalitÃƒÂ©s                </p>                <ul className="space-y-1 text-xs" style={{ color: C.text, fontFamily: BODY_FONT }}>                  <li>Ã¢â‚¬Â¢ AccÃƒÂ¨s illimitÃƒÂ© aux labs privÃƒÂ©s</li>                  <li>Ã¢â‚¬Â¢ Badge VIP exclusif sur votre profil</li>                  <li>Ã¢â‚¬Â¢ AccÃƒÂ¨s au salon Discord rÃƒÂ©servÃƒÂ© aux membres</li>                  <li>Ã¢â‚¬Â¢ Formations avec accompagnement personnalisÃƒÂ©</li>                  <li>Ã¢â‚¬Â¢ Priority support de la communautÃƒÂ©</li>                </ul>                <p className="text-xs mt-2" style={{ color: C.alert, fontFamily: BODY_FONT }}>                  *Essai premium : 7 jours gratuits puis 9,99Ã¢â€šÂ¬/mois. Annulable ÃƒÂ  tout moment.                </p>                <PrimaryButton                  onClick={() => setIsPremium(true)}                  disabled={busy}                  className="w-full mt-2"                >                  {busy ? "..." : "Choisir Premium"}                </PrimaryButton>              </div>            </div>            {/* Free Card */}            <div className="flex items-center gap-3 p-4 rounded-lg"                 style={{                   background: !isPremium ? `${C.ok}1A` : C.panel2,                   border: `1px solid ${!isPremium ? C.ok : C.line}`,                   boxShadow: !isPremium ? `0 0 0 2px ${C.ok}33` : "none"                 }}>              <div className="flex-1">                <h3 className="text-sm font-semibold mb-1" style={{ color: C.text, fontFamily: BODY_FONT }}>                  Gratuit                </h3>                <p className="text-xs mb-2" style={{ color: C.muted, fontFamily: BODY_FONT }}>                  AccÃƒÂ¨s de base ÃƒÂ  la communautÃƒÂ©                </p>                <ul className="space-y-1 text-xs" style={{ color: C.text, fontFamily: BODY_FONT }}>                  <li>Ã¢â‚¬Â¢ AccÃƒÂ¨s aux labs publics</li>                  <li>Ã¢â‚¬Â¢ Participation au forum et au Hub</li>                  <li>Ã¢â‚¬Â¢ CrÃƒÂ©ation de teams (max 4 personnes)</li>                  <li>Ã¢â‚¬Â¢ AccÃƒÂ¨s aux actualitÃƒÂ©s cybersÃƒÂ©curitÃƒÂ©</li>                </ul>                <PrimaryButton                  onClick={() => setIsPremium(false)}                  disabled={busy}                  className="w-full mt-2"                >                  {busy ? "..." : "Continuer en gratuit"}                </PrimaryButton>              </div>            </div>          </div>          <div className="flex items-center gap-2 mb-3">            <div className="flex-1 h-px" style={{ background: C.line }} />            <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>ou</span>            <div className="flex-1 h-px" style={{ background: C.line }} />          </div>          {/* Discord Button - Emphasized */}          <button onClick={connectDiscord} disabled={busy}            className="w-full flex items-center justify-center gap-3 px-4 py-3 rounded-md text-lg font-bold mb-4 disabled:opacity-50"            style={{              background: C.discord,              color: "#fff",              fontFamily: BODY_FONT,              boxShadow: "0 4px 12px rgba(88, 101, 242, 0.3)",              transform: "translateY(-2px)",              transition: "all 0.2s ease"            }}            onMouseEnter={(e) => { if (!busy) e.currentTarget.style.transform = "translateY(-4px)"; }}            onMouseLeave={(e) => { if (!busy) e.currentTarget.style.transform = "translateY(-2px)"; }}          >            <div className="flex items-center gap-2">              <MessageSquare size={18} />              <span>Continuer avec Discord</span>            </div>          </button>          <p className="text-xs mb-2" style={{ color: C.muted, fontFamily: BODY_FONT }}>            Connexion Discord simulÃƒÂ©e pour cette dÃƒÂ©mo (une vraie intÃƒÂ©gration OAuth nÃƒÂ©cessite un serveur back-end).          </p>          <div className="flex items-center gap-2 mb-3">            <div className="flex-1 h-px" style={{ background: C.line }} />            <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>ou par e-mail</span>            <div className="flex-1 h-px" style={{ background: C.line }} />          </div>          {/* Mode Tabs */}          <div className="flex gap-1 mb-3 p-1 rounded-md" style={{ background: C.panel2 }}>            <button onClick={() => { setMode("login"); setError(""); }} className="flex-1 text-xs py-1.5 rounded"              style={{ background: mode === "login" ? C.primary : "transparent", color: mode === "login" ? "#fff" : C.muted, fontFamily: BODY_FONT }}>Connexion</button>            <button onClick={() => { setMode("register"); setError(""); }} className="flex-1 text-xs py-1.5 rounded"              style={{ background: mode === "register" ? C.primary : "transparent", color: mode === "register" ? "#fff" : C.muted, fontFamily: BODY_FONT }}>Inscription</button>          </div>          {mode === "login" ? (            <form onSubmit={submitLogin}>              <Field label="E-mail"><input type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="toi@exemple.fr" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>              <Field label="Mot de passe"><input type="password" value={password} onChange={(e) => setPassword(e.target.value)} className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>              {error && <p className="text-xs mb-2" style={{ color: C.alert, fontFamily: BODY_FONT }}>{error}</p>}              <PrimaryButton type="submit" disabled={busy}>{busy ? "..." : "Se connecter"}</PrimaryButton>            </form>          ) : (            <form onSubmit={submitRegister}>              <Field label="E-mail"><input type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="toi@exemple.fr" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>              <Field label="Nom d'utilisateur"><input value={username} onChange={(e) => setUsername(e.target.value)} placeholder="ex : n0va_hxr" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>              <Field label="Mot de passe"><input type="password" value={password} onChange={(e) => setPassword(e.target.value)} className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>              <div className="grid grid-cols-2 gap-x-2 gap-y-1 mb-3">                {[["length", "8 caractÃƒÂ¨res min."], ["upper", "1 majuscule"], ["lower", "1 minuscule"], ["digit", "1 chiffre"], ["special", "1 caractÃƒÂ¨re spÃƒÂ©cial"]{ icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },`n].map(([k, l]) => (                  <span key={k} className="text-xs flex items-center gap-1" style={{ color: checks[k] ? C.ok : C.muted, fontFamily: BODY_FONT }}>                    <CheckCircle2 size={11} /> {l}                  </span>                ))}              </div>              <Field label="Confirmer le mot de passe"><input type="password" value={confirm} onChange={(e) => setConfirm(e.target.value)} className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>              <Field label="Photo de profil">                <div className="flex flex-wrap gap-2">                  {AVATAR_OPTIONS.map((a) => {                    const Icon = a.icon;                    const active = avatarKey === a.key;                    return (                      <button type="button" key={a.key} onClick={() => setAvatarKey(a.key)}                        className="w-9 h-9 rounded-full flex items-center justify-center"                        style={{ background: a.color, outline: active ? `2px solid ${C.text}` : "none", outlineOffset: 2 }}>                        <Icon size={17} color="#fff" />                      </button>                    );                  })}                </div>              </Field>              {error && <p className="text-xs mb-2" style={{ color: C.alert, fontFamily: BODY_FONT }}>{error}</p>}              <PrimaryButton type="submit" disabled={busy}>{busy ? "..." : "CrÃƒÂ©er mon compte"}</PrimaryButton>            </form>          )}        </div>      )}    </div>  );}/* ---------------------------------------------------------------------   Profil--------------------------------------------------------------------- */function ProfileTab({ currentUser, setCurrentUser, profiles, setProfiles, questions, trophies, setTab }) {  const [editing, setEditing] = useState(false);  const [avatarKey, setAvatarKey] = useState(currentUser?.avatarKey);  const [banner, setBanner] = useState(currentUser?.banner);  const [bannerImage, setBannerImage] = useState(currentUser?.bannerImage || "");  const [github, setGithub] = useState(currentUser?.socials?.github || "");  const [twitter, setTwitter] = useState(currentUser?.socials?.twitter || "");  const [discord, setDiscord] = useState(currentUser?.socials?.discord || "");  const [bio, setBio] = useState(currentUser?.bio || "");  if (!currentUser) {    return (      <div className="max-w-sm mx-auto text-center py-16">        <UserIcon size={28} className="mx-auto mb-3" style={{ color: C.primary }} />        <h2 className="text-lg font-bold mb-2" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Pas encore connectÃƒÂ©</h2>        <p className="text-sm" style={{ color: C.muted, fontFamily: BODY_FONT }}>Connecte-toi (e-mail ou Discord) pour crÃƒÂ©er et voir ton profil.</p>      </div>    );  }  const myQuestions = questions.filter((q) => q.author === currentUser.username).length;  const myTrophies = trophies.filter((t) => t.author === currentUser.username).length;  const bannerCss = bannerImage ? `url(${bannerImage}) center/cover no-repeat` : BANNER_MAP[banner] || BANNER_MAP.indigo;  const socials = currentUser.socials || {};  async function save() {    const updated = { ...currentUser, avatarKey, banner, bannerImage, bio: bio.trim().slice(0, 160), socials: { github: github.trim(), twitter: twitter.trim(), discord: discord.trim() } };    const next = profiles.map((p) => (p.id === currentUser.id ? updated : p));    setProfiles(next);    setCurrentUser(updated);    await saveCollection("gowlsec:profiles", next);    setEditing(false);  }  return (    <div>      <Panel className="overflow-hidden mb-6">        <div className="h-36" style={{ background: bannerCss }} />        <div className="px-5 pb-5">          <div className="-mt-8 mb-3">            <div className="inline-block rounded-full p-1" style={{ background: C.panel }}>              <Avatar profile={{ avatarKey }} size={64} />            </div>          </div>          <div className="flex items-center justify-between flex-wrap gap-2">            <div>              <div className="flex items-center gap-2 flex-wrap">                <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{currentUser.username}</h2>                {currentUser.isPremium && (                  <span className="inline-flex items-center gap-1 px-2.5 py-0.5 rounded-md text-xs font-semibold"                    style={{ background: `${C.primary}20`, color: C.primary }}>                    Premium                  </span>                )}              </div>              <p className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>                Membre depuis {timeAgo(currentUser.joinedAt)} Ã‚Â· {currentUser.provider === "discord" ? "Discord" : "E-mail"}                {currentUser.isPremium && currentUser.premiumUntil && (                  <span className="ml-2 text-xs" style={{ color: C.muted, fontFamily: BODY_FONT }}>                    Ã¢â‚¬Â¢ Expire le {new Date(currentUser.premiumUntil).toLocaleDateString()}                  </span>                )}              </p>              {currentUser.bio && <p className="text-sm mt-2" style={{ color: C.text, fontFamily: BODY_FONT }}>{currentUser.bio}</p>}            </div>            <GhostButton onClick={() => setEditing((e) => !e)}><Pencil size={12} /> {editing ? "Annuler" : "Modifier"}</GhostButton>          </div>          <div className="flex gap-3 mt-4 flex-wrap">            <StatChip icon={<Flag size={14} />} label="questions" value={myQuestions} />            <StatChip icon={<Trophy size={14} />} label="trophÃƒÂ©es" value={myTrophies} />          </div>          {(socials.github || socials.twitter || socials.discord) && !editing && (            <div className="flex flex-wrap gap-2 mt-4 pt-4" style={{ borderTop: `1px solid ${C.line}` }}>              {socials.github && (                <a href={socials.github.startsWith("http") ? socials.github : `https://github.com/${socials.github.replace(/^@/, "")}`} target="_blank" rel="noreferrer"                  className="inline-flex items-center gap-1.5 px-2.5 py-1.5 rounded-md text-xs" style={{ border: `1px solid ${C.line}`, color: C.text, fontFamily: BODY_FONT }}>                  <GitHub size={13} /> GitHub                </a>              )}              {socials.twitter && (                <a href={socials.twitter.startsWith("http") ? socials.twitter : `https://twitter.com/${socials.twitter.replace(/^@/, "")}`} target="_blank" rel="noreferrer"                  className="inline-flex items-center gap-1.5 px-2.5 py-1.5 rounded-md text-xs" style={{ border: `1px solid ${C.line}`, color: C.text, fontFamily: BODY_FONT }}>                  <Twitter size={13} /> Twitter                </a>              )}              {socials.discord && (                <span className="inline-flex items-center gap-1.5 px-2.5 py-1.5 rounded-md text-xs" style={{ border: `1px solid ${C.line}`, color: C.text, fontFamily: BODY_FONT }}>                  <MessageCircle size={13} /> {socials.discord}                </span>              )}            </div>          )}        </div>      </Panel>      {editing && (        <Panel className="p-4">          <Field label="Bio (160 caractÃƒÂ¨res max)">            <textarea value={bio} onChange={(e) => setBio(e.target.value.slice(0, 160))} rows={2} placeholder="Une courte prÃƒÂ©sentation, ta spÃƒÂ©cialitÃƒÂ©, tes objectifs..." className="w-full px-3 py-2 rounded-md text-sm resize-none" style={inputStyle} />            <span className="block mt-1 text-xs text-right" style={{ color: C.muted, fontFamily: MONO_FONT }}>{bio.length}/160</span>          </Field>          <Field label="Photo de profil">            <div className="flex flex-wrap gap-2">              {AVATAR_OPTIONS.map((a) => {                const Icon = a.icon;                const active = avatarKey === a.key;                return (                  <button key={a.key} onClick={() => setAvatarKey(a.key)} className="w-10 h-10 rounded-full flex items-center justify-center"                    style={{ background: a.color, outline: active ? `2px solid ${C.text}` : "none", outlineOffset: 2 }}>                    <Icon size={18} color="#fff" />                  </button>                );              })}            </div>          </Field>          <Field label="BanniÃƒÂ¨re">            <div className="flex flex-wrap gap-2 mb-2">              {BANNER_OPTIONS.map((b) => (                <button key={b.key} onClick={() => { setBanner(b.key); setBannerImage(""); }} className="w-14 h-9 rounded-md"                  style={{ background: b.css, outline: banner === b.key && !bannerImage ? `2px solid ${C.text}` : "none", outlineOffset: 2 }} />              ))}            </div>            <input value={bannerImage} onChange={(e) => setBannerImage(e.target.value)} placeholder="ou colle une URL d'image..."              className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} />          </Field>          <Field label="GitHub">            <div className="flex items-center gap-2">              <GitHub size={15} style={{ color: C.muted }} />              <input value={github} onChange={(e) => setGithub(e.target.value)} placeholder="pseudo ou lien github.com/..." className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} />            </div>          </Field>          <Field label="Twitter / X">            <div className="flex items-center gap-2">              <Twitter size={15} style={{ color: C.muted }} />              <input value={twitter} onChange={(e) => setTwitter(e.target.value)} placeholder="@pseudo ou lien" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} />            </div>          </Field>          <Field label="Discord">            <div className="flex items-center gap-2">              <MessageCircle size={15} style={{ color: C.muted }} />              <input value={discord} onChange={(e) => setDiscord(e.target.value)} placeholder="pseudo#0000 ou pseudo" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} />            </div>          </Field>          <PrimaryButton onClick={save}><CheckCircle2 size={14} /> Enregistrer</PrimaryButton>        </Panel>      )}    </div>  );}/* ---------------------------------------------------------------------   Bandeau d'actus (dÃƒÂ©filement auto, sans images)--------------------------------------------------------------------- */function NewsMarquee({ news }) {  if (news.length === 0) return null;  const loop = [...news, ...news];  return (    <div className="overflow-hidden rounded-lg" style={{ border: `1px solid ${C.line}` }}>      <style>{`        @keyframes gowl-marquee { from { transform: translateX(0); } to { transform: translateX(-50%); } }        .gowl-marquee-track { animation: gowl-marquee 40s linear infinite; }        .gowl-marquee-track:hover { animation-play-state: paused; }      `}</style>      <div className="flex gowl-marquee-track" style={{ width: "max-content" }}>        {loop.map((n, i) => {          const cat = NEWS_CATEGORIES.find((c) => c.key === n.category) || NEWS_CATEGORIES[3];          return (            <div key={`${n.id}-${i}`} className="flex items-center gap-3 px-4 py-2.5 shrink-0" style={{ width: 340, borderRight: `1px solid ${C.line}`, background: C.panel }}>              <div className="w-9 h-9 rounded-md flex items-center justify-center shrink-0" style={{ background: `${cat.color}1A`, color: cat.color }}>                <Newspaper size={15} />              </div>              <div className="min-w-0">                <Chip label={cat.label} color={cat.color} />                <p className="text-xs mt-1 truncate" style={{ color: C.text, fontFamily: BODY_FONT }}>{n.title}</p>              </div>            </div>          );        })}      </div>    </div>  );}/* ---------------------------------------------------------------------   News--------------------------------------------------------------------- */function NewsCard({ item, isAdmin, onDelete }) {  const cat = NEWS_CATEGORIES.find((c) => c.key === item.category) || NEWS_CATEGORIES[3];  return (    <Panel className="overflow-hidden flex flex-col h-full" style={{ borderColor: `${cat.color}40` }}>      <div className="h-1.5" style={{ background: cat.color }} />      <div className="p-4 flex flex-col flex-1">        <div className="flex items-center justify-between mb-2">          <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>{item.ref}</span>          {isAdmin && <button onClick={() => onDelete(item.id)} style={{ color: C.alert }}><Trash2 size={13} /></button>}        </div>        <Chip label={cat.label} color={cat.color} />        <h3 className="text-sm font-semibold mt-2.5 mb-1.5 leading-snug" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{item.title}</h3>        <p className="text-sm flex-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>{item.summary}</p>        <div className="mt-3 pt-2.5 flex items-center justify-between" style={{ borderTop: `1px solid ${C.line}` }}>          <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>{item.source} Ã‚Â· {timeAgo(item.date)}</span>          {item.url && <a href={item.url} target="_blank" rel="noreferrer" className="flex items-center gap-1 text-xs" style={{ color: C.primary, fontFamily: BODY_FONT }}>Lire <ExternalLink size={11} /></a>}        </div>      </div>    </Panel>  );}function NewsTab({ news, setNews, isAdmin, full }) {  const [filter, setFilter] = useState("Tous");  const [showForm, setShowForm] = useState(false);  const [title, setTitle] = useState("");  const [summary, setSummary] = useState("");  const [category, setCategory] = useState(NEWS_CATEGORIES[0].key);  const [source, setSource] = useState("");  const [url, setUrl] = useState("");  async function submit(e) {    e.preventDefault();    if (!title.trim() || !summary.trim()) return;    const n = { id: uid(), ref: `HZ-2026-${String(200 + news.length).slice(-3)}`, category, title: title.trim(), summary: summary.trim(), source: source.trim() || "RÃƒÂ©daction GowlSec", url: url.trim(), date: new Date().toISOString() };    const next = [n, ...news];    setNews(next);    saveCollection("gowlsec:news", next);    setTitle(""); setSummary(""); setSource(""); setUrl(""); setCategory(NEWS_CATEGORIES[0].key); setShowForm(false);  }  async function removeNews(id) {    const next = news.filter((n) => n.id !== id);    setNews(next);    saveCollection("gowlsec:news", next);  }  const filtered = filter === "Tous" ? news : news.filter((n) => n.category === filter);  const shown = full ? filtered : filtered.slice(0, 6);  return (    <div>      {full && (        <div className="flex items-center justify-between mb-5 flex-wrap gap-3">          <div>            <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>ActualitÃƒÂ©s cybersÃƒÂ©curitÃƒÂ©</h2>            <p className="text-sm mt-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>Failles, fuites de donnÃƒÂ©es, annonces et veille.</p>          </div>          {isAdmin && <PrimaryButton onClick={() => setShowForm((s) => !s)}>{showForm ? <X size={15} /> : <Plus size={15} />} {showForm ? "Annuler" : "Publier"}</PrimaryButton>}        </div>      )}      {full && (        <div className="flex gap-2 mb-4 flex-wrap">          {["Tous", ...NEWS_CATEGORIES.map((c) => c.key){ icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },`n].map((k) => {            const c = NEWS_CATEGORIES.find((x) => x.key === k);            const active = filter === k;            return (              <button key={k} onClick={() => setFilter(k)} className="text-xs px-3 py-1.5 rounded-md"                style={{ background: active ? (c ? c.color : C.primary) : "transparent", color: active ? "#0A0C10" : C.muted, border: `1px solid ${active ? "transparent" : C.line}`, fontFamily: MONO_FONT }}>                {k === "Tous" ? "Tous" : c.label}              </button>            );          })}        </div>      )}      {showForm && (        <Panel className="p-4 mb-6">          <form onSubmit={submit}>            <Field label="CatÃƒÂ©gorie">              <div className="flex flex-wrap gap-2">                {NEWS_CATEGORIES.map((c) => (                  <button type="button" key={c.key} onClick={() => setCategory(c.key)} style={{ opacity: category === c.key ? 1 : 0.5 }}><Chip label={c.label} color={c.color} /></button>                ))}              </div>            </Field>            <Field label="Titre"><input value={title} onChange={(e) => setTitle(e.target.value)} className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>            <Field label="RÃƒÂ©sumÃƒÂ©"><textarea value={summary} onChange={(e) => setSummary(e.target.value)} rows={3} className="w-full px-3 py-2 rounded-md text-sm resize-none" style={inputStyle} /></Field>            <div className="grid sm:grid-cols-2 gap-3">              <Field label="Source"><input value={source} onChange={(e) => setSource(e.target.value)} placeholder="ex : CERT-FR" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>              <Field label="Lien (optionnel)"><input value={url} onChange={(e) => setUrl(e.target.value)} placeholder="https://..." className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>            </div>            <PrimaryButton type="submit"><CheckCircle2 size={14} /> Publier</PrimaryButton>          </form>        </Panel>      )}      <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">        {shown.length === 0 && <EmptyState text="Aucune actualitÃƒÂ© pour l'instant." />}        {shown.map((n) => <NewsCard key={n.id} item={n} isAdmin={isAdmin} onDelete={removeNews} />)}      </div>    </div>  );}/* ---------------------------------------------------------------------   Forum Ã¢â‚¬â€ question / accompagnement--------------------------------------------------------------------- */function ForumTab({ pseudo, questions, setQuestions, isAdmin, lang = "fr" }) {  const [showForm, setShowForm] = useState(false);  const [title, setTitle] = useState("");  const [body, setBody] = useState("");  const [type, setType] = useState(QUESTION_TYPES[3].key);  const [openId, setOpenId] = useState(null);  const [replyDraft, setReplyDraft] = useState({});  async function submitQuestion(e) {    e.preventDefault();    if (!title.trim() || !body.trim()) return;    const q = { id: uid(), author: pseudo, title: title.trim(), body: body.trim(), type, createdAt: new Date().toISOString(), answers: [] };    const next = [q, ...questions];    setQuestions(next);    saveCollection("gowlsec:questions", next);    setTitle(""); setBody(""); setType(QUESTION_TYPES[3].key); setShowForm(false);    setOpenId(q.id);  }  async function addAnswer(qid) {    const text = (replyDraft[qid] || "").trim();    if (!text) return;    const next = questions.map((q) => q.id === qid ? { ...q, answers: [...q.answers, { id: uid(), author: pseudo, body: text, createdAt: new Date().toISOString() }] } : q);    setQuestions(next);    saveCollection("gowlsec:questions", next);    setReplyDraft((d) => ({ ...d, [qid]: "" }));  }  async function removeQuestion(qid) {    const next = questions.filter((q) => q.id !== qid);    setQuestions(next);    saveCollection("gowlsec:questions", next);  }  return (    <div>      <div className="flex items-center justify-between mb-5 flex-wrap gap-3">        <div>          <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{t(lang, "forumTitle")}</h2>          <p className="text-sm mt-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>{t(lang, "forumSub")}</p>        </div>        <PrimaryButton onClick={() => setShowForm((s) => !s)}>{showForm ? <X size={15} /> : <Plus size={15} />} {showForm ? t(lang, "cancel") : t(lang, "newQuestion")}</PrimaryButton>      </div>      {showForm && (        <CreationHero scene={<ForumScene />} accent={C.primary} eyebrow={t(lang, "forumHeroEyebrow")}          title={t(lang, "forumHeroTitle")}          subtitle={t(lang, "forumHeroSub")}>          <form onSubmit={submitQuestion}>            <Field label="Type de demande">              <div className="flex flex-wrap gap-2">                {QUESTION_TYPES.map((t2) => (                  <button type="button" key={t2.key} onClick={() => setType(t2.key)} style={{ opacity: type === t2.key ? 1 : 0.5 }}><Chip label={t2.label} color={t2.color} /></button>                ))}              </div>            </Field>            <Field label="Titre"><input value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Ex : Blocage sur l'escalade de privilÃƒÂ¨ges (Linux)" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>            <Field label="DÃƒÂ©tails"><textarea value={body} onChange={(e) => setBody(e.target.value)} rows={4} placeholder="Contexte, ce que tu as dÃƒÂ©jÃƒÂ  essayÃƒÂ©, oÃƒÂ¹ tu bloques..." className="w-full px-3 py-2 rounded-md text-sm resize-none" style={inputStyle} /></Field>            <PrimaryButton type="submit"><Send size={14} /> {t(lang, "publish")}</PrimaryButton>          </form>        </CreationHero>      )}      <div className="space-y-3">        {questions.length === 0 && <EmptyState text="Aucune question pour l'instant." />}        {questions.map((q) => {          const qt = QUESTION_TYPES.find((t) => t.key === q.type) || QUESTION_TYPES[3];          const open = openId === q.id;          return (            <Panel key={q.id} className="p-4">              <div className="flex items-start justify-between gap-3">                <div className="flex-1 cursor-pointer" onClick={() => setOpenId(open ? null : q.id)}>                  <div className="flex items-center gap-2 mb-1.5 flex-wrap">                    <Chip label={qt.label} color={qt.color} />                    <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>{q.author} Ã‚Â· {timeAgo(q.createdAt)}</span>                  </div>                  <h3 className="font-semibold text-sm mb-1" style={{ color: C.text, fontFamily: BODY_FONT }}>{q.title}</h3>                  {!open && <p className="text-sm line-clamp-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>{q.body}</p>}                </div>                <div className="flex items-center gap-2 shrink-0">                  <span className="text-xs flex items-center gap-1" style={{ color: C.muted, fontFamily: MONO_FONT }}><MessageSquare size={12} /> {q.answers.length}</span>                  {isAdmin && <GhostButton danger onClick={() => removeQuestion(q.id)}><Trash2 size={12} /></GhostButton>}                  <button onClick={() => setOpenId(open ? null : q.id)} style={{ color: C.muted }}>{open ? <ChevronUp size={16} /> : <ChevronDown size={16} />}</button>                </div>              </div>              {open && (                <div className="mt-3 pt-3" style={{ borderTop: `1px solid ${C.line}` }}>                  <p className="text-sm mb-3 whitespace-pre-wrap" style={{ color: C.text, fontFamily: BODY_FONT }}>{q.body}</p>                  <div className="space-y-2 mb-3">                    {q.answers.map((a) => (                      <div key={a.id} className="pl-3" style={{ borderLeft: `2px solid ${C.primary}55` }}>                        <div className="text-xs mb-0.5" style={{ color: C.primary, fontFamily: MONO_FONT }}>{a.author} <span style={{ color: C.muted }}>Ã‚Â· {timeAgo(a.createdAt)}</span></div>                        <p className="text-sm" style={{ color: C.text, fontFamily: BODY_FONT }}>{a.body}</p>                      </div>                    ))}                  </div>                  <div className="flex gap-2">                    <input value={replyDraft[q.id] || ""} onChange={(e) => setReplyDraft((d) => ({ ...d, [q.id]: e.target.value }))}                      onKeyDown={(e) => e.key === "Enter" && addAnswer(q.id)} placeholder="RÃƒÂ©pondre..." className="flex-1 px-3 py-1.5 rounded-md text-sm" style={inputStyle} />                    <GhostButton onClick={() => addAnswer(q.id)}><Send size={12} /> RÃƒÂ©pondre</GhostButton>                  </div>                </div>              )}            </Panel>          );        })}      </div>    </div>  );}/* ---------------------------------------------------------------------   Hub (multi-rooms) Ã¢â‚¬â€ discussion en direct + rÃƒÂ©actions--------------------------------------------------------------------- */const REACTION_EMOJIS = ["Ã°Å¸â€˜Â", "Ã°Å¸â€Â¥", "Ã°Å¸â€™â‚¬", "Ã°Å¸Ââ€º", "Ã°Å¸Ëœâ€š"];function RoomsTab({ pseudo, messages, setMessages, isAdmin, lang = "fr" }) {  const [room, setRoom] = useState("general");  const [text, setText] = useState("");  const [composerBg, setComposerBg] = useState("linear-gradient(135deg, #5B6EF5 0%, #171B22 100%)");  const bottomRef = useRef(null);  const roomMessages = messages.filter((m) => (m.room || "general") === room);  useEffect(() => { bottomRef.current?.scrollIntoView({ behavior: "smooth" }); }, [roomMessages.length, room]);  async function send() {    if (!text.trim()) return;    const m = { id: uid(), room, author: pseudo, text: text.trim(), createdAt: new Date().toISOString(), reactions: {} };    const next = [...messages, m];    setMessages(next);    saveCollection("gowlsec:chat", next);    setText("");  }  async function removeMsg(id) {    const next = messages.filter((m) => m.id !== id);    setMessages(next);    saveCollection("gowlsec:chat", next);  }  async function toggleReaction(id, emoji) {    const next = messages.map((m) => {      if (m.id !== id) return m;      const reactions = { ...(m.reactions || {}) };      const users = reactions[emoji] || [];      reactions[emoji] = users.includes(pseudo) ? users.filter((u) => u !== pseudo) : [...users, pseudo];      if (reactions[emoji].length === 0) delete reactions[emoji];      return { ...m, reactions };    });    setMessages(next);    saveCollection("gowlsec:chat", next);  }  const current = ROOMS.find((r) => r.key === room);  return (    <div>      <Panel className="overflow-hidden mb-6" style={{ border: `1px solid ${C.ok}40`, boxShadow: `0 16px 40px -28px ${C.ok}AA` }}>        <div className="grid md:grid-cols-[0.92fr_1.3fr]">          <div className="relative flex flex-col justify-center px-6 pt-6 pb-5" style={{ background: `linear-gradient(165deg, ${C.ok}14, transparent 75%)`, borderRight: `1px solid ${C.line}` }}>            <HubScene />            <span className="text-[11px] font-bold uppercase tracking-widest mb-1.5" style={{ color: C.ok, fontFamily: MONO_FONT }}>{t(lang, "hubHeroEyebrow")}</span>            <h3 className="text-lg font-bold mb-1.5 leading-tight" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{t(lang, "hubHeroTitle")}</h3>            <p className="text-xs leading-relaxed" style={{ color: C.muted, fontFamily: BODY_FONT }}>{t(lang, "hubHeroSub")}</p>          </div>          <div className="p-5">            <div className="grid sm:grid-cols-2 gap-2">              {ROOMS.map((r) => {                const active = r.key === room;                const count = messages.filter((m) => (m.room || "general") === r.key).length;                return (                  <button key={r.key} onClick={() => setRoom(r.key)} className="flex items-start gap-2.5 text-left px-3 py-2.5 rounded-lg transition-all"                    style={{ background: active ? `${C.ok}18` : C.panel2, border: `1px solid ${active ? C.ok : C.line}` }}>                    <span className="w-7 h-7 rounded-md flex items-center justify-center shrink-0 mt-0.5" style={{ background: active ? `${C.ok}22` : C.bg, color: active ? C.ok : C.muted }}><Hash size={13} /></span>                    <span className="min-w-0">                      <span className="flex items-center gap-1.5">                        <span className="text-sm font-semibold" style={{ color: C.text, fontFamily: BODY_FONT }}>{r.label}</span>                        {count > 0 && <span className="text-[10px] px-1.5 py-0.5 rounded-full" style={{ background: C.panel, color: C.muted, fontFamily: MONO_FONT }}>{count}</span>}                      </span>                      <span className="block text-xs mt-0.5" style={{ color: C.muted, fontFamily: BODY_FONT }}>{r.desc}</span>                    </span>                  </button>                );              })}            </div>          </div>        </div>      </Panel>      <div className="grid md:grid-cols-[220px_1fr] gap-4">        <Panel className="p-2 h-fit md:sticky md:top-20">          <div className="flex md:flex-col gap-1 overflow-x-auto md:overflow-visible">            {ROOMS.map((r) => {              const active = r.key === room;              return (                <button key={r.key} onClick={() => setRoom(r.key)} className="flex items-center gap-2 px-3 py-2 rounded-md text-sm text-left whitespace-nowrap"                  style={{ background: active ? C.primary : "transparent", color: active ? "#fff" : C.muted, fontFamily: BODY_FONT }}>                  <Hash size={13} /> {r.label}                </button>              );            })}          </div>        </Panel>        <Panel className="flex flex-col" style={{ height: 480 }}>          <div className="flex items-center gap-2 px-4 py-2.5" style={{ borderBottom: `1px solid ${C.line}` }}>            <Activity size={14} style={{ color: C.ok }} />            <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>#{current.label} Ã‚Â· {current.desc}</span>          </div>          <div className="flex-1 overflow-y-auto px-4 py-3 space-y-3">            {roomMessages.length === 0 && <EmptyState text="Aucun message dans ce salon. Lance la discussion." />}            {roomMessages.map((m) => {              const reactions = m.reactions || {};              return (                <div key={m.id} className="flex items-start gap-2 group">                  <div className="w-7 h-7 rounded-md flex items-center justify-center text-xs shrink-0" style={{ background: C.panel2, color: C.primary, border: `1px solid ${C.line}`, fontFamily: MONO_FONT }}>                    {m.author.slice(0, 2).toUpperCase()}                  </div>                  <div className="flex-1">                    <div className="flex items-center gap-2">                      <span className="text-xs font-semibold" style={{ color: C.text, fontFamily: MONO_FONT }}>{m.author}</span>                      <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>{timeAgo(m.createdAt)}</span>                      {isAdmin && <button onClick={() => removeMsg(m.id)} className="opacity-0 group-hover:opacity-100 transition-opacity" style={{ color: C.alert }}><Trash2 size={11} /></button>}                    </div>                    <p className="text-sm mb-1.5" style={{ color: C.text, fontFamily: BODY_FONT }}>{m.text}</p>                    <div className="flex items-center gap-1 flex-wrap opacity-0 group-hover:opacity-100 focus-within:opacity-100 transition-opacity">                      {REACTION_EMOJIS.map((em) => {                        const users = reactions[em] || [];                        const active = users.includes(pseudo);                        if (users.length === 0 && !active) {                          return (                            <button key={em} onClick={() => toggleReaction(m.id, em)} className="text-xs px-1.5 py-0.5 rounded"                              style={{ background: "transparent", border: `1px solid ${C.line}`, opacity: 0.6 }}>{em}</button>                          );                        }                        return null;                      })}                    </div>                    {Object.keys(reactions).length > 0 && (                      <div className="flex items-center gap-1 flex-wrap mt-1">                        {Object.entries(reactions).map(([em, users]) => (                          <button key={em} onClick={() => toggleReaction(m.id, em)} className="text-xs px-1.5 py-0.5 rounded flex items-center gap-1"                            style={{ background: users.includes(pseudo) ? `${C.primary}22` : C.panel2, border: `1px solid ${users.includes(pseudo) ? C.primary : C.line}`, color: C.text, fontFamily: MONO_FONT }}>                            {em} {users.length}                          </button>                        ))}                      </div>                    )}                  </div>                </div>              );            })}            <div ref={bottomRef} />          </div>          <div className="p-3" style={{ borderTop: `1px solid ${C.line}` }}>            <div className="rounded-2xl p-2.5" style={{ background: composerBg, boxShadow: `inset 0 1px 0 rgba(255,255,255,0.12), 0 10px 24px -16px rgba(0,0,0,0.7)` }}>              <div className="flex items-center gap-2 rounded-xl p-2" style={{ background: `${C.panel}CC`, border: `1px solid ${C.line}` }}>                <input value={text} onChange={(e) => setText(e.target.value)} onKeyDown={(e) => e.key === "Enter" && send()}                  placeholder={`Ãƒâ€°crire dans #${current.label}...`} className="flex-1 px-3 py-2 rounded-md text-sm" style={{ ...inputStyle, background: "transparent", border: "none", boxShadow: "none" }} />                <PrimaryButton onClick={send}><Send size={14} /></PrimaryButton>              </div>            </div>            <div className="flex items-center gap-2 mt-2 flex-wrap">              {[                { label: "Neon", value: "linear-gradient(135deg, #5B6EF5 0%, #171B22 100%)" },                { label: "Cyber", value: "linear-gradient(135deg, #2ED9A3 0%, #0E1E1A 100%)" },                { label: "Danger", value: "linear-gradient(135deg, #FF4D5E 0%, #241016 100%)" },                { label: "Gold", value: "linear-gradient(135deg, #FFD166 0%, #241C0A 100%)" },              { icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },`n              ].map((preset) => (                <button key={preset.label} onClick={() => setComposerBg(preset.value)} className="px-2.5 py-1 rounded-full text-[11px]" style={{ background: preset.value, color: "#fff", border: `1px solid ${C.line}` }}>                  {preset.label}                </button>              ))}            </div>          </div>        </Panel>      </div>    </div>  );}/* ---------------------------------------------------------------------   Team Ã¢â‚¬â€ crÃƒÂ©ation de teams (max 16, publique ou privÃƒÂ©e) + annonces--------------------------------------------------------------------- */function TeamsTab({ pseudo, teams, setTeams, announcements, setAnnouncements, isAdmin, lang = "fr" }) {  const [showCreate, setShowCreate] = useState(false);  const [selectedId, setSelectedId] = useState(null);  const [name, setName] = useState("");  const [description, setDescription] = useState("");  const [logoType, setLogoType] = useState("emoji");  const [logoValue, setLogoValue] = useState(TEAM_EMOJIS[0]);  const [visibility, setVisibility] = useState("public");  const [password, setPassword] = useState("");  const [draft, setDraft] = useState("");  const [joinPwd, setJoinPwd] = useState({});  const [joinError, setJoinError] = useState({});  async function createTeam(e) {    e.preventDefault();    if (!name.trim()) return;    if (visibility === "private" && !password.trim()) return;    const passwordHash = visibility === "private" ? await hashText(password.trim()) : null;    const t = {      id: uid(), name: name.trim(), description: description.trim(),      logoType, logoValue: logoType === "emoji" ? logoValue : logoValue.trim(),      owner: pseudo, members: [pseudo], maxMembers: TEAM_MAX_MEMBERS,      visibility, passwordHash, createdAt: new Date().toISOString(),    };    const next = [t, ...teams];    setTeams(next);    await saveCollection("gowlsec:teams", next);    setName(""); setDescription(""); setLogoType("emoji"); setLogoValue(TEAM_EMOJIS[0]); setVisibility("public"); setPassword("");    setShowCreate(false);    setSelectedId(t.id);  }  async function joinTeam(team) {    if (team.members.includes(pseudo)) return;    if (team.members.length >= (team.maxMembers || TEAM_MAX_MEMBERS)) {      setJoinError((e) => ({ ...e, [team.id]: `Ãƒâ€°quipe complÃƒÂ¨te (${team.maxMembers || TEAM_MAX_MEMBERS} membres max).` }));      return;    }    if (team.visibility === "private") {      const attempt = (joinPwd[team.id] || "").trim();      if (!attempt) { setJoinError((e) => ({ ...e, [team.id]: "Mot de passe requis." })); return; }      const hash = await hashText(attempt);      if (hash !== team.passwordHash) { setJoinError((e) => ({ ...e, [team.id]: "Mot de passe incorrect." })); return; }    }    const next = teams.map((t) => t.id === team.id ? { ...t, members: [...t.members, pseudo] } : t);    setTeams(next);    await saveCollection("gowlsec:teams", next);    setJoinError((e) => ({ ...e, [team.id]: "" }));    setJoinPwd((p) => ({ ...p, [team.id]: "" }));  }  async function leaveTeam(teamId) {    const next = teams.map((t) => t.id === teamId ? { ...t, members: t.members.filter((m) => m !== pseudo) } : t);    setTeams(next);    await saveCollection("gowlsec:teams", next);  }  async function removeTeam(teamId) {    const next = teams.filter((t) => t.id !== teamId);    setTeams(next);    await saveCollection("gowlsec:teams", next);    const nextAnn = announcements.filter((a) => a.teamId !== teamId);    setAnnouncements(nextAnn);    await saveCollection("gowlsec:team_announcements", nextAnn);    setSelectedId(null);  }  async function postAnnouncement(teamId) {    const text = draft.trim();    if (!text) return;    const a = { id: uid(), teamId, author: pseudo, text, createdAt: new Date().toISOString() };    const next = [a, ...announcements];    setAnnouncements(next);    await saveCollection("gowlsec:team_announcements", next);    setDraft("");  }  async function removeAnnouncement(id) {    const next = announcements.filter((a) => a.id !== id);    setAnnouncements(next);    await saveCollection("gowlsec:team_announcements", next);  }  const selected = teams.find((t) => t.id === selectedId);  if (selected) {    const isMember = selected.members.includes(pseudo);    const isPrivate = selected.visibility === "private";    const isFull = selected.members.length >= (selected.maxMembers || TEAM_MAX_MEMBERS);    const teamAnnouncements = announcements.filter((a) => a.teamId === selected.id);    return (      <div>        <GhostButton onClick={() => setSelectedId(null)}>Ã¢â€ Â Retour aux ÃƒÂ©quipes</GhostButton>        <Panel className="p-5 mt-4">          <div className="flex items-start gap-4 flex-wrap">            <TeamLogo team={selected} size={56} />            <div className="flex-1 min-w-[200px]">              <div className="flex items-center gap-2 flex-wrap">                <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{selected.name}</h2>                <Chip label={isPrivate ? "PrivÃƒÂ©e" : "Publique"} color={isPrivate ? C.warn : C.ok} />              </div>              <p className="text-sm mt-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>{selected.description || "Aucune description."}</p>              <div className="flex items-center gap-3 mt-2 flex-wrap">                <span className="text-xs flex items-center gap-1" style={{ color: C.muted, fontFamily: MONO_FONT }}>                  <Users size={12} /> {selected.members.length}/{selected.maxMembers || TEAM_MAX_MEMBERS} membres                </span>                <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>Capitaine : {selected.owner}</span>              </div>            </div>            <div className="flex flex-col items-end gap-2">              {isMember ? (                selected.owner !== pseudo && <GhostButton onClick={() => leaveTeam(selected.id)}>Quitter</GhostButton>              ) : isFull ? (                <span className="text-xs" style={{ color: C.alert, fontFamily: BODY_FONT }}>Ãƒâ€°quipe complÃƒÂ¨te</span>              ) : isPrivate ? (                <div className="flex items-center gap-2">                  <input type="password" value={joinPwd[selected.id] || ""} onChange={(e) => setJoinPwd((p) => ({ ...p, [selected.id]: e.target.value }))}                    placeholder="mot de passe" className="px-2.5 py-1.5 rounded-md text-xs w-32" style={inputStyle} />                  <PrimaryButton onClick={() => joinTeam(selected)}><KeyRound size={13} /> Rejoindre</PrimaryButton>                </div>              ) : (                <PrimaryButton onClick={() => joinTeam(selected)}><Plus size={14} /> Rejoindre</PrimaryButton>              )}              {(isAdmin || selected.owner === pseudo) && <GhostButton danger onClick={() => removeTeam(selected.id)}><Trash2 size={12} /> Supprimer</GhostButton>}            </div>          </div>          {joinError[selected.id] && <p className="text-xs mt-2 text-right" style={{ color: C.alert, fontFamily: BODY_FONT }}>{joinError[selected.id]}</p>}        </Panel>        <div className="mt-5">          <h3 className="text-sm font-bold uppercase tracking-wide mb-3" style={{ color: C.muted, fontFamily: MONO_FONT }}>Annonces</h3>          {isMember && (            <Panel className="p-3 mb-4">              <div className="flex gap-2">                <input value={draft} onChange={(e) => setDraft(e.target.value)} onKeyDown={(e) => e.key === "Enter" && postAnnouncement(selected.id)}                  placeholder="Publier une annonce ÃƒÂ  l'ÃƒÂ©quipe..." className="flex-1 px-3 py-2 rounded-md text-sm" style={inputStyle} />                <PrimaryButton onClick={() => postAnnouncement(selected.id)}><Megaphone size={14} /></PrimaryButton>              </div>            </Panel>          )}          <div className="space-y-2">            {teamAnnouncements.length === 0 && <EmptyState text="Aucune annonce pour cette ÃƒÂ©quipe." />}            {teamAnnouncements.map((a) => (              <Panel key={a.id} className="p-3 flex items-start justify-between gap-3">                <div>                  <div className="text-xs mb-1" style={{ color: C.primary, fontFamily: MONO_FONT }}>{a.author} Ã‚Â· {timeAgo(a.createdAt)}</div>                  <p className="text-sm" style={{ color: C.text, fontFamily: BODY_FONT }}>{a.text}</p>                </div>                {(isAdmin || a.author === pseudo || selected.owner === pseudo) && (                  <button onClick={() => removeAnnouncement(a.id)} style={{ color: C.alert }} className="shrink-0"><Trash2 size={12} /></button>                )}              </Panel>            ))}          </div>        </div>      </div>    );  }  return (    <div>      <div className="flex items-center justify-between mb-5 flex-wrap gap-3">        <div>          <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{t(lang, "teamTitle")}</h2>          <p className="text-sm mt-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>{t(lang, "teamSub")}</p>        </div>        <PrimaryButton onClick={() => setShowCreate((s) => !s)}>{showCreate ? <X size={15} /> : <Plus size={15} />} {showCreate ? t(lang, "cancel") : t(lang, "newTeam")}</PrimaryButton>      </div>      {showCreate && (        <CreationHero scene={<TeamScene />} accent={C.warn} eyebrow={t(lang, "teamHeroEyebrow")}          title={t(lang, "teamHeroTitle")}          subtitle={t(lang, "teamHeroSub")}>          <form onSubmit={createTeam}>            <Field label="Nom de l'ÃƒÂ©quipe"><input value={name} onChange={(e) => setName(e.target.value)} placeholder="Ex : Les Corbeaux Noirs" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>            <Field label="Description"><textarea value={description} onChange={(e) => setDescription(e.target.value)} rows={2} placeholder="Objectif de l'ÃƒÂ©quipe, niveau, disponibilitÃƒÂ©s..." className="w-full px-3 py-2 rounded-md text-sm resize-none" style={inputStyle} /></Field>            <Field label="Logo de l'ÃƒÂ©quipe">              <div className="flex gap-1 mb-2 p-1 rounded-md w-fit" style={{ background: C.panel2 }}>                <button type="button" onClick={() => setLogoType("emoji")} className="text-xs px-3 py-1.5 rounded"                  style={{ background: logoType === "emoji" ? C.primary : "transparent", color: logoType === "emoji" ? "#fff" : C.muted, fontFamily: BODY_FONT }}>Ãƒâ€°moji</button>                <button type="button" onClick={() => { setLogoType("image"); setLogoValue(""); }} className="text-xs px-3 py-1.5 rounded"                  style={{ background: logoType === "image" ? C.primary : "transparent", color: logoType === "image" ? "#fff" : C.muted, fontFamily: BODY_FONT }}>Logo perso (URL)</button>              </div>              {logoType === "emoji" ? (                <div className="flex flex-wrap gap-2">                  {TEAM_EMOJIS.map((em) => (                    <button type="button" key={em} onClick={() => setLogoValue(em)} className="w-9 h-9 rounded-md flex items-center justify-center text-lg"                      style={{ background: C.panel2, outline: logoValue === em ? `2px solid ${C.primary}` : `1px solid ${C.line}`, outlineOffset: 1 }}>{em}</button>                  ))}                </div>              ) : (                <input value={logoValue} onChange={(e) => setLogoValue(e.target.value)} placeholder="https://..." className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} />              )}            </Field>            <Field label="VisibilitÃƒÂ© (16 membres max)">              <div className="flex gap-2">                <button type="button" onClick={() => setVisibility("public")} className="flex-1 inline-flex items-center justify-center gap-1.5 px-3 py-2 rounded-md text-sm"                  style={{ background: visibility === "public" ? C.primary : C.panel2, color: visibility === "public" ? "#fff" : C.muted, fontFamily: BODY_FONT }}>                  <Globe size={14} /> Publique                </button>                <button type="button" onClick={() => setVisibility("private")} className="flex-1 inline-flex items-center justify-center gap-1.5 px-3 py-2 rounded-md text-sm"                  style={{ background: visibility === "private" ? C.primary : C.panel2, color: visibility === "private" ? "#fff" : C.muted, fontFamily: BODY_FONT }}>                  <Lock size={14} /> PrivÃƒÂ©e                </button>              </div>            </Field>            {visibility === "private" && (              <Field label="Mot de passe de l'ÃƒÂ©quipe">                <input type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Requis pour une ÃƒÂ©quipe privÃƒÂ©e" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} />              </Field>            )}            <PrimaryButton type="submit"><CheckCircle2 size={14} /> CrÃƒÂ©er l'ÃƒÂ©quipe</PrimaryButton>          </form>        </CreationHero>      )}      <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">        {teams.length === 0 && <EmptyState text="Aucune ÃƒÂ©quipe pour l'instant. Sois le premier ÃƒÂ  en crÃƒÂ©er une." />}        {teams.map((t) => {          const isPrivate = t.visibility === "private";          return (            <Panel key={t.id} className="p-4 cursor-pointer" onClick={() => setSelectedId(t.id)}>              <div className="flex items-center gap-3 mb-2">                <TeamLogo team={t} size={40} />                <div className="min-w-0 flex-1">                  <div className="flex items-center gap-1.5 flex-wrap">                    <h3 className="font-semibold text-sm truncate" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{t.name}</h3>                  </div>                  <span className="text-xs flex items-center gap-1" style={{ color: C.muted, fontFamily: MONO_FONT }}>                    <Users size={11} /> {t.members.length}/{t.maxMembers || TEAM_MAX_MEMBERS}                  </span>                </div>                <Chip label={isPrivate ? "PrivÃƒÂ©e" : "Publique"} color={isPrivate ? C.warn : C.ok} />              </div>              <p className="text-xs line-clamp-2" style={{ color: C.muted, fontFamily: BODY_FONT }}>{t.description || "Aucune description."}</p>              <div className="mt-3 pt-2 flex items-center justify-between" style={{ borderTop: `1px solid ${C.line}` }}>                <span className="text-xs" style={{ color: C.primary, fontFamily: MONO_FONT }}>Capitaine : {t.owner}</span>                <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>{timeAgo(t.createdAt)}</span>              </div>            </Panel>          );        })}      </div>    </div>  );}/* ---------------------------------------------------------------------   Labs Ã¢â‚¬â€ salons de session pour HTB / TryHackMe / Root-Me (8 max)--------------------------------------------------------------------- */function LabsTab({ pseudo, labs, setLabs, labMessages, setLabMessages, isAdmin, lang = "fr" }) {  const [showCreate, setShowCreate] = useState(false);  const [selectedId, setSelectedId] = useState(null);  const [title, setTitle] = useState("");  const [platform, setPlatform] = useState(LAB_PLATFORMS[0].key);  const [description, setDescription] = useState("");  const [visibility, setVisibility] = useState("public");  const [password, setPassword] = useState("");  const [joinPwd, setJoinPwd] = useState({});  const [joinError, setJoinError] = useState({});  const [chatText, setChatText] = useState("");  const bottomRef = useRef(null);  async function createLab(e) {    e.preventDefault();    if (!title.trim()) return;    if (visibility === "private" && !password.trim()) return;    const passwordHash = visibility === "private" ? await hashText(password.trim()) : null;    const lab = {      id: uid(), title: title.trim(), platform, description: description.trim(),      owner: pseudo, members: [pseudo], maxMembers: LAB_MAX_MEMBERS,      visibility, passwordHash, createdAt: new Date().toISOString(),    };    const next = [lab, ...labs];    setLabs(next);    await saveCollection("gowlsec:labs", next);    setTitle(""); setPlatform(LAB_PLATFORMS[0].key); setDescription(""); setVisibility("public"); setPassword("");    setShowCreate(false);    setSelectedId(lab.id);  }  async function joinLab(lab) {    if (lab.members.includes(pseudo)) return;    if (lab.members.length >= (lab.maxMembers || LAB_MAX_MEMBERS)) {      setJoinError((e) => ({ ...e, [lab.id]: `Salon complet (${lab.maxMembers || LAB_MAX_MEMBERS} personnes max).` }));      return;    }    if (lab.visibility === "private") {      const attempt = (joinPwd[lab.id] || "").trim();      if (!attempt) { setJoinError((e) => ({ ...e, [lab.id]: "Mot de passe requis." })); return; }      const hash = await hashText(attempt);      if (hash !== lab.passwordHash) { setJoinError((e) => ({ ...e, [lab.id]: "Mot de passe incorrect." })); return; }    }    const next = labs.map((l) => l.id === lab.id ? { ...l, members: [...l.members, pseudo] } : l);    setLabs(next);    await saveCollection("gowlsec:labs", next);    setJoinError((e) => ({ ...e, [lab.id]: "" }));    setJoinPwd((p) => ({ ...p, [lab.id]: "" }));  }  async function leaveLab(labId) {    const next = labs.map((l) => l.id === labId ? { ...l, members: l.members.filter((m) => m !== pseudo) } : l);    setLabs(next);    await saveCollection("gowlsec:labs", next);  }  async function removeLab(labId) {    const next = labs.filter((l) => l.id !== labId);    setLabs(next);    await saveCollection("gowlsec:labs", next);    const nextMsgs = labMessages.filter((m) => m.labId !== labId);    setLabMessages(nextMsgs);    await saveCollection("gowlsec:lab_messages", nextMsgs);    setSelectedId(null);  }  async function sendLabMessage(labId) {    const text = chatText.trim();    if (!text) return;    const m = { id: uid(), labId, author: pseudo, text, createdAt: new Date().toISOString() };    const next = [...labMessages, m];    setLabMessages(next);    await saveCollection("gowlsec:lab_messages", next);    setChatText("");  }  async function removeLabMessage(id) {    const next = labMessages.filter((m) => m.id !== id);    setLabMessages(next);    await saveCollection("gowlsec:lab_messages", next);  }  const selected = labs.find((l) => l.id === selectedId);  const selectedMessages = selected ? labMessages.filter((m) => m.labId === selected.id) : [];  useEffect(() => { bottomRef.current?.scrollIntoView({ behavior: "smooth" }); }, [selectedMessages.length, selectedId]);  if (selected) {    const isMember = selected.members.includes(pseudo);    const isPrivate = selected.visibility === "private";    const isFull = selected.members.length >= (selected.maxMembers || LAB_MAX_MEMBERS);    const plat = LAB_PLATFORMS.find((p) => p.key === selected.platform) || LAB_PLATFORMS[3];    return (      <div>        <GhostButton onClick={() => setSelectedId(null)}>Ã¢â€ Â Retour aux labs</GhostButton>        <Panel className="p-5 mt-4">          <div className="flex items-start gap-4 flex-wrap">            <div className="w-14 h-14 rounded-md flex items-center justify-center shrink-0" style={{ background: `${C.primary}1A`, color: C.primary }}><Bug size={26} /></div>            <div className="flex-1 min-w-[200px]">              <div className="flex items-center gap-2 flex-wrap">                <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{selected.title}</h2>                <Chip label={isPrivate ? "PrivÃƒÂ©" : "Public"} color={isPrivate ? C.warn : C.ok} />                <Chip label={plat.label} color={C.primary} />              </div>              <p className="text-sm mt-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>{selected.description || "Aucune description."}</p>              <div className="flex items-center gap-3 mt-2 flex-wrap">                <span className="text-xs flex items-center gap-1" style={{ color: C.muted, fontFamily: MONO_FONT }}>                  <Users size={12} /> {selected.members.length}/{selected.maxMembers || LAB_MAX_MEMBERS} membres                </span>                <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>CrÃƒÂ©ÃƒÂ© par : {selected.owner}</span>              </div>            </div>            <div className="flex flex-col items-end gap-2">              {isMember ? (                selected.owner !== pseudo && <GhostButton onClick={() => leaveLab(selected.id)}>Quitter</GhostButton>              ) : isFull ? (                <span className="text-xs" style={{ color: C.alert, fontFamily: BODY_FONT }}>Salon complet</span>              ) : isPrivate ? (                <div className="flex items-center gap-2">                  <input type="password" value={joinPwd[selected.id] || ""} onChange={(e) => setJoinPwd((p) => ({ ...p, [selected.id]: e.target.value }))}                    placeholder="mot de passe" className="px-2.5 py-1.5 rounded-md text-xs w-32" style={inputStyle} />                  <PrimaryButton onClick={() => joinLab(selected)}><KeyRound size={13} /> Rejoindre</PrimaryButton>                </div>              ) : (                <PrimaryButton onClick={() => joinLab(selected)}><Plus size={14} /> Rejoindre</PrimaryButton>              )}              {(isAdmin || selected.owner === pseudo) && <GhostButton danger onClick={() => removeLab(selected.id)}><Trash2 size={12} /> Supprimer</GhostButton>}            </div>          </div>          {joinError[selected.id] && <p className="text-xs mt-2 text-right" style={{ color: C.alert, fontFamily: BODY_FONT }}>{joinError[selected.id]}</p>}          <div className="flex flex-wrap gap-1.5 mt-3 pt-3" style={{ borderTop: `1px solid ${C.line}` }}>            {selected.members.map((m) => (              <span key={m} className="text-xs px-2 py-1 rounded-md" style={{ background: C.panel2, color: C.text, fontFamily: MONO_FONT }}>{m}</span>            ))}          </div>        </Panel>        <Panel className="flex flex-col mt-5" style={{ height: 420 }}>          <div className="flex items-center gap-2 px-4 py-2.5" style={{ borderBottom: `1px solid ${C.line}` }}>            <MessageSquare size={14} style={{ color: C.ok }} />            <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>Discussion du salon lab</span>          </div>          <div className="flex-1 overflow-y-auto px-4 py-3 space-y-3">            {selectedMessages.length === 0 && <EmptyState text="Aucun message. Rejoins le salon pour discuter du lab." />}            {selectedMessages.map((m) => (              <div key={m.id} className="flex items-start gap-2 group">                <div className="w-7 h-7 rounded-md flex items-center justify-center text-xs shrink-0" style={{ background: C.panel2, color: C.primary, border: `1px solid ${C.line}`, fontFamily: MONO_FONT }}>                  {m.author.slice(0, 2).toUpperCase()}                </div>                <div className="flex-1">                  <div className="flex items-center gap-2">                    <span className="text-xs font-semibold" style={{ color: C.text, fontFamily: MONO_FONT }}>{m.author}</span>                    <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>{timeAgo(m.createdAt)}</span>                    {isAdmin && <button onClick={() => removeLabMessage(m.id)} className="opacity-0 group-hover:opacity-100 transition-opacity" style={{ color: C.alert }}><Trash2 size={11} /></button>}                  </div>                  <p className="text-sm" style={{ color: C.text, fontFamily: BODY_FONT }}>{m.text}</p>                </div>              </div>            ))}            <div ref={bottomRef} />          </div>          {isMember ? (            <div className="flex items-center gap-2 p-3" style={{ borderTop: `1px solid ${C.line}` }}>              <input value={chatText} onChange={(e) => setChatText(e.target.value)} onKeyDown={(e) => e.key === "Enter" && sendLabMessage(selected.id)}                placeholder="Ãƒâ€°crire dans le salon..." className="flex-1 px-3 py-2 rounded-md text-sm" style={inputStyle} />              <PrimaryButton onClick={() => sendLabMessage(selected.id)}><Send size={14} /></PrimaryButton>            </div>          ) : (            <div className="p-3 text-center" style={{ borderTop: `1px solid ${C.line}` }}>              <span className="text-xs" style={{ color: C.muted, fontFamily: BODY_FONT }}>Rejoins le salon pour participer ÃƒÂ  la discussion.</span>            </div>          )}        </Panel>      </div>    );  }  return (    <div>      <div className="flex items-center justify-between mb-5 flex-wrap gap-3">        <div>          <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{t(lang, "labsTitle")}</h2>          <p className="text-sm mt-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>{t(lang, "labsSub")}</p>        </div>        <PrimaryButton onClick={() => setShowCreate((s) => !s)}>{showCreate ? <X size={15} /> : <Plus size={15} />} {showCreate ? t(lang, "cancel") : t(lang, "newLab")}</PrimaryButton>      </div>      {showCreate && (        <CreationHero scene={<LabScene />} accent={C.alert} eyebrow={t(lang, "labsHeroEyebrow")}          title={t(lang, "labsHeroTitle")}          subtitle={t(lang, "labsHeroSub")}>          <form onSubmit={createLab}>            <div className="grid sm:grid-cols-2 gap-3">              <Field label="Titre du salon"><input value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Ex : Blue (THM) Ã¢â‚¬â€ session du soir" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>              <Field label="Plateforme">                <select value={platform} onChange={(e) => setPlatform(e.target.value)} className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle}>                  {LAB_PLATFORMS.map((p) => <option key={p.key} value={p.key}>{p.label}</option>)}                </select>              </Field>            </div>            <Field label="Description"><textarea value={description} onChange={(e) => setDescription(e.target.value)} rows={2} placeholder="Objectif de la session, niveau requis, horaires..." className="w-full px-3 py-2 rounded-md text-sm resize-none" style={inputStyle} /></Field>            <Field label="VisibilitÃƒÂ© (8 personnes max)">              <div className="flex gap-2">                <button type="button" onClick={() => setVisibility("public")} className="flex-1 inline-flex items-center justify-center gap-1.5 px-3 py-2 rounded-md text-sm"                  style={{ background: visibility === "public" ? C.primary : C.panel2, color: visibility === "public" ? "#fff" : C.muted, fontFamily: BODY_FONT }}>                  <Globe size={14} /> Public                </button>                <button type="button" onClick={() => setVisibility("private")} className="flex-1 inline-flex items-center justify-center gap-1.5 px-3 py-2 rounded-md text-sm"                  style={{ background: visibility === "private" ? C.primary : C.panel2, color: visibility === "private" ? "#fff" : C.muted, fontFamily: BODY_FONT }}>                  <Lock size={14} /> PrivÃƒÂ©                </button>              </div>            </Field>            {visibility === "private" && (              <Field label="Mot de passe du salon">                <input type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Requis pour un salon privÃƒÂ©" className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} />              </Field>            )}            <PrimaryButton type="submit"><CheckCircle2 size={14} /> CrÃƒÂ©er le salon</PrimaryButton>          </form>        </CreationHero>      )}      <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">        {labs.length === 0 && <EmptyState text="Aucun salon lab pour l'instant. Sois le premier ÃƒÂ  en crÃƒÂ©er un." />}        {labs.map((l) => {          const isPrivate = l.visibility === "private";          const plat = LAB_PLATFORMS.find((p) => p.key === l.platform) || LAB_PLATFORMS[3];          return (            <Panel key={l.id} className="p-4 cursor-pointer" onClick={() => setSelectedId(l.id)}>              <div className="flex items-center gap-3 mb-2">                <div className="w-10 h-10 rounded-md flex items-center justify-center shrink-0" style={{ background: `${C.primary}1A`, color: C.primary }}><Bug size={18} /></div>                <div className="min-w-0 flex-1">                  <h3 className="font-semibold text-sm truncate" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{l.title}</h3>                  <span className="text-xs flex items-center gap-1" style={{ color: C.muted, fontFamily: MONO_FONT }}>                    <Users size={11} /> {l.members.length}/{l.maxMembers || LAB_MAX_MEMBERS}                  </span>                </div>                <Chip label={isPrivate ? "PrivÃƒÂ©" : "Public"} color={isPrivate ? C.warn : C.ok} />              </div>              <p className="text-xs line-clamp-2 mb-2" style={{ color: C.muted, fontFamily: BODY_FONT }}>{l.description || "Aucune description."}</p>              <Chip label={plat.label} color={C.primary} />              <div className="mt-3 pt-2 flex items-center justify-between" style={{ borderTop: `1px solid ${C.line}` }}>                <span className="text-xs" style={{ color: C.primary, fontFamily: MONO_FONT }}>{l.owner}</span>                <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>{timeAgo(l.createdAt)}</span>              </div>            </Panel>          );        })}      </div>    </div>  );}/* ---------------------------------------------------------------------   Classement Ã¢â‚¬â€ points calculÃƒÂ©s depuis trophÃƒÂ©es, forum et labs crÃƒÂ©ÃƒÂ©s--------------------------------------------------------------------- */const TROPHY_POINTS = { facile: 10, moyen: 20, difficile: 35, insane: 50 };const RANK_MEDALS = ["Ã°Å¸Â¥â€¡", "Ã°Å¸Â¥Ë†", "Ã°Å¸Â¥â€°"];function LeaderboardTab({ questions, trophies, labs, teams, profiles }) {  const rows = useMemo(() => {    const scores = {};    function add(author, points, kind) {      if (!author) return;      if (!scores[author]) scores[author] = { author, total: 0, trophies: 0, forum: 0, labs: 0 };      scores[author].total += points;      scores[author][kind] += points;    }    trophies.forEach((t) => add(t.author, TROPHY_POINTS[t.difficulty] || 10, "trophies"));    questions.forEach((q) => {      add(q.author, 2, "forum");      (q.answers || []).forEach((a) => add(a.author, 3, "forum"));    });    labs.forEach((l) => add(l.owner, 5, "labs"));    return Object.values(scores).sort((a, b) => b.total - a.total);  }, [questions, trophies, labs]);  return (    <div>      <div className="mb-5">        <div className="flex items-center gap-2 mb-1">          <Trophy size={18} style={{ color: C.gold }} />          <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Classement</h2>        </div>        <p className="text-sm" style={{ color: C.muted, fontFamily: BODY_FONT }}>          Points cumulÃƒÂ©s : trophÃƒÂ©es (10 ÃƒÂ  50 pts selon la difficultÃƒÂ©), participation au forum (2 ÃƒÂ  3 pts), salons labs crÃƒÂ©ÃƒÂ©s (5 pts).        </p>      </div>      {rows.length === 0 ? (        <EmptyState text="Personne au classement pour l'instant. Ajoute un trophÃƒÂ©e ou pose une question pour dÃƒÂ©marrer !" />      ) : (        <div className="space-y-2">          {rows.map((r, i) => {            const profile = profiles.find((p) => p.username === r.author);            return (              <Panel key={r.author} className="p-3 flex items-center gap-3" style={{ borderColor: i < 3 ? `${C.gold}55` : C.line }}>                <span className="w-8 text-center text-lg shrink-0">{RANK_MEDALS[i] || <span className="text-sm" style={{ color: C.muted, fontFamily: MONO_FONT }}>#{i + 1}</span>}</span>                {profile ? <Avatar profile={profile} size={34} /> : <div className="w-[34px] h-[34px] rounded-full shrink-0" style={{ background: C.panel2, border: `1px solid ${C.line}` }} />}                <div className="flex-1 min-w-0">                  <p className="text-sm font-semibold truncate" style={{ color: C.text, fontFamily: BODY_FONT }}>{r.author}</p>                  <div className="flex items-center gap-2 flex-wrap mt-0.5">                    {r.trophies > 0 && <span className="text-xs flex items-center gap-1" style={{ color: C.muted, fontFamily: MONO_FONT }}><Trophy size={10} /> {r.trophies} pts</span>}                    {r.forum > 0 && <span className="text-xs flex items-center gap-1" style={{ color: C.muted, fontFamily: MONO_FONT }}><MessageSquare size={10} /> {r.forum} pts</span>}                    {r.labs > 0 && <span className="text-xs flex items-center gap-1" style={{ color: C.muted, fontFamily: MONO_FONT }}><Bug size={10} /> {r.labs} pts</span>}                  </div>                </div>                <span className="text-lg font-bold shrink-0" style={{ color: C.gold, fontFamily: DISPLAY_FONT }}>{r.total}</span>              </Panel>            );          })}        </div>      )}    </div>  );}/* ---------------------------------------------------------------------   TrophÃƒÂ©es--------------------------------------------------------------------- */function TrophyTab({ pseudo, trophies, setTrophies, isAdmin }) {  const [showForm, setShowForm] = useState(false);  const [filter, setFilter] = useState("Tous");  const [platform, setPlatform] = useState(PLATFORMS[0]);  const [title, setTitle] = useState("");  const [difficulty, setDifficulty] = useState(DIFFICULTIES[0].key);  const [note, setNote] = useState("");  async function submit(e) {    e.preventDefault();    if (!title.trim()) return;    const t = { id: uid(), author: pseudo, platform, title: title.trim(), difficulty, note: note.trim(), createdAt: new Date().toISOString() };    const next = [t, ...trophies];    setTrophies(next);    saveCollection("gowlsec:trophies", next);    setTitle(""); setNote(""); setDifficulty(DIFFICULTIES[0].key); setShowForm(false);  }  async function removeTrophy(id) {    const next = trophies.filter((t) => t.id !== id);    setTrophies(next);    saveCollection("gowlsec:trophies", next);  }  const filtered = filter === "Tous" ? trophies : trophies.filter((t) => t.platform === filter);  return (    <div>      <div className="flex items-center justify-between mb-5 flex-wrap gap-3">        <div>          <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Salon trophÃƒÂ©es</h2>          <p className="text-sm mt-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>Affiche tes badges Root-Me et TryHackMe.</p>        </div>        <PrimaryButton onClick={() => setShowForm((s) => !s)}>{showForm ? <X size={15} /> : <Award size={15} />} {showForm ? "Annuler" : "Ajouter un trophÃƒÂ©e"}</PrimaryButton>      </div>      <div className="flex gap-2 mb-4">        {["Tous", ...PLATFORMS{ icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },`n].map((p) => (          <button key={p} onClick={() => setFilter(p)} className="text-xs px-3 py-1.5 rounded-md"            style={{ background: filter === p ? C.primary : "transparent", color: filter === p ? "#fff" : C.muted, border: `1px solid ${filter === p ? C.primary : C.line}`, fontFamily: MONO_FONT }}>{p}</button>        ))}      </div>      {showForm && (        <Panel className="p-4 mb-6">          <form onSubmit={submit}>            <div className="grid sm:grid-cols-2 gap-3">              <Field label="Plateforme">                <select value={platform} onChange={(e) => setPlatform(e.target.value)} className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle}>                  {PLATFORMS.map((p) => <option key={p} value={p}>{p}</option>)}                </select>              </Field>              <Field label="Nom de la machine / badge"><input value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Ex : Blue, Vulnversity..." className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>            </div>            <Field label="DifficultÃƒÂ©">              <div className="flex flex-wrap gap-2">                {DIFFICULTIES.map((d) => (                  <button type="button" key={d.key} onClick={() => setDifficulty(d.key)} style={{ opacity: difficulty === d.key ? 1 : 0.5 }}><Chip label={d.label} color={d.color} /></button>                ))}              </div>            </Field>            <Field label="Note (optionnel)"><input value={note} onChange={(e) => setNote(e.target.value)} className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>            <PrimaryButton type="submit"><CheckCircle2 size={14} /> Ajouter au salon</PrimaryButton>          </form>        </Panel>      )}      <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">        {filtered.length === 0 && <EmptyState text="Aucun trophÃƒÂ©e ici pour le moment." />}        {filtered.map((t) => {          const d = DIFFICULTIES.find((x) => x.key === t.difficulty) || DIFFICULTIES[0];          return (            <Panel key={t.id} className="p-4 relative" style={{ borderColor: `${d.color}55` }}>              {isAdmin && <button onClick={() => removeTrophy(t.id)} className="absolute top-3 right-3" style={{ color: C.alert }}><Trash2 size={13} /></button>}              <div className="flex items-center gap-2 mb-2"><Trophy size={16} style={{ color: d.color }} /><span className="text-xs uppercase tracking-wide" style={{ color: C.muted, fontFamily: MONO_FONT }}>{t.platform}</span></div>              <h3 className="font-semibold text-sm mb-2" style={{ color: C.text, fontFamily: BODY_FONT }}>{t.title}</h3>              <Chip label={d.label} color={d.color} />              {t.note && <p className="text-xs mt-2" style={{ color: C.muted, fontFamily: BODY_FONT }}>{t.note}</p>}              <div className="mt-3 pt-2 flex items-center justify-between" style={{ borderTop: `1px solid ${C.line}` }}>                <span className="text-xs" style={{ color: C.primary, fontFamily: MONO_FONT }}>{t.author}</span>                <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>{timeAgo(t.createdAt)}</span>              </div>            </Panel>          );        })}      </div>    </div>  );}/* ---------------------------------------------------------------------   Boutique Ã¢â‚¬â€ formations & accompagnement (dÃƒÂ©mo, sans paiement rÃƒÂ©el)--------------------------------------------------------------------- */function ShopTab({ pseudo, currentUser, orders, setOrders }) {  const [cart, setCart] = useState([]);  const [showCheckout, setShowCheckout] = useState(false);  const [buyerName, setBuyerName] = useState(currentUser?.username || "");  const [buyerEmail, setBuyerEmail] = useState(currentUser?.email || "");  const [processing, setProcessing] = useState(false);  const [confirmation, setConfirmation] = useState(null);  const [error, setError] = useState("");  function toggleCart(id) {    setCart((c) => (c.includes(id) ? c.filter((x) => x !== id) : [...c, id]));  }  const cartProducts = SHOP_PRODUCTS.filter((p) => cart.includes(p.id));  const total = cartProducts.reduce((s, p) => s + p.price, 0);  async function submitOrder(e) {    e.preventDefault();    setError("");    if (!buyerName.trim() || !buyerEmail.trim()) return setError("Merci de renseigner ton nom et ton e-mail.");    if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(buyerEmail.trim())) return setError("Adresse e-mail invalide.");    if (cartProducts.length === 0) return setError("Ton panier est vide.");    setProcessing(true);    await new Promise((r) => setTimeout(r, 900));    const order = {      id: uid(), buyer: buyerName.trim(), email: buyerEmail.trim(),      items: cartProducts.map((p) => p.title), total, createdAt: new Date().toISOString(),    };    const next = [order, ...orders];    setOrders(next);    await saveCollection("gowlsec:orders", next);    setProcessing(false);    setConfirmation(order);    setCart([]);    setShowCheckout(false);  }  if (confirmation) {    return (      <div className="max-w-md mx-auto text-center py-16">        <CheckCircle2 size={36} className="mx-auto mb-3" style={{ color: C.ok }} />        <h2 className="text-lg font-bold mb-2" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Commande enregistrÃƒÂ©e !</h2>        <p className="text-sm mb-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>          RÃƒÂ©fÃƒÂ©rence : <span style={{ color: C.text, fontFamily: MONO_FONT }}>{confirmation.id.slice(0, 8).toUpperCase()}</span>        </p>        <p className="text-sm mb-5" style={{ color: C.muted, fontFamily: BODY_FONT }}>Un e-mail de confirmation sera envoyÃƒÂ© ÃƒÂ  {confirmation.email}.</p>        <Panel className="p-4 text-left mb-5">          {confirmation.items.map((it, i) => <p key={i} className="text-sm mb-1" style={{ color: C.text, fontFamily: BODY_FONT }}>Ã¢â‚¬Â¢ {it}</p>)}          <div className="mt-2 pt-2 flex justify-between" style={{ borderTop: `1px solid ${C.line}` }}>            <span className="text-sm font-semibold" style={{ color: C.text, fontFamily: BODY_FONT }}>Total</span>            <span className="text-sm font-semibold" style={{ color: C.ok, fontFamily: BODY_FONT }}>{confirmation.total}Ã¢â€šÂ¬</span>          </div>        </Panel>        <p className="text-xs mb-4" style={{ color: C.muted, fontFamily: BODY_FONT }}>          DÃƒÂ©mo sans paiement rÃƒÂ©el Ã¢â‚¬â€ aucune carte bancaire n'a ÃƒÂ©tÃƒÂ© dÃƒÂ©bitÃƒÂ©e. L'ÃƒÂ©quipe GowlSec te recontactera par e-mail pour organiser la formation.        </p>        <GhostButton onClick={() => setConfirmation(null)}>Retour ÃƒÂ  la boutique</GhostButton>      </div>    );  }  return (    <div>      <div className="mb-5">        <div className="flex items-center gap-2 mb-1">          <ShoppingCart size={18} style={{ color: C.primary }} />          <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Boutique Ã¢â‚¬â€ Formations</h2>        </div>        <p className="text-sm" style={{ color: C.muted, fontFamily: BODY_FONT }}>          Des formations et accompagnements pour bien dÃƒÂ©marrer en informatique, chacun ÃƒÂ  29Ã¢â€šÂ¬.        </p>      </div>      <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-6">        {SHOP_PRODUCTS.map((p) => {          const Icon = p.icon;          const inCart = cart.includes(p.id);          return (            <Panel key={p.id} className="p-4 flex flex-col" style={{ borderColor: inCart ? `${C.ok}66` : C.line }}>              <div className="flex items-center justify-between mb-2">                <div className="w-9 h-9 rounded-md flex items-center justify-center" style={{ background: `${C.primary}1A`, color: C.primary }}><Icon size={17} /></div>                <span className="text-lg font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{p.price}Ã¢â€šÂ¬</span>              </div>              <h3 className="text-sm font-semibold mb-1" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{p.title}</h3>              <p className="text-xs mb-3" style={{ color: C.muted, fontFamily: BODY_FONT }}>{p.tagline}</p>              <ul className="space-y-1.5 mb-3 flex-1">                {p.features.map((f, i) => (                  <li key={i} className="text-xs flex items-start gap-1.5" style={{ color: C.text, fontFamily: BODY_FONT }}>                    <CheckCircle2 size={12} className="mt-0.5 shrink-0" style={{ color: C.ok }} /> {f}                  </li>                ))}              </ul>              <div className="flex items-center gap-1.5 mb-3 text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}><Clock size={11} /> {p.format}</div>              <button onClick={() => toggleCart(p.id)} className="w-full inline-flex items-center justify-center gap-2 px-3 py-2 rounded-md text-sm font-semibold"                style={{ background: inCart ? "transparent" : C.primary, color: inCart ? C.ok : "#fff", border: inCart ? `1px solid ${C.ok}` : "none", fontFamily: BODY_FONT }}>                {inCart ? <><CheckCircle2 size={14} /> Dans le panier</> : <><ShoppingCart size={14} /> Ajouter au panier</>}              </button>            </Panel>          );        })}      </div>      {cart.length > 0 && !showCheckout && (        <Panel className="p-4 sticky bottom-4 flex items-center justify-between flex-wrap gap-3" style={{ boxShadow: "0 8px 24px rgba(0,0,0,0.4)" }}>          <div>            <span className="text-sm font-semibold" style={{ color: C.text, fontFamily: BODY_FONT }}>{cart.length} formation{cart.length > 1 ? "s" : ""} sÃƒÂ©lectionnÃƒÂ©e{cart.length > 1 ? "s" : ""}</span>            <span className="text-sm ml-2" style={{ color: C.ok, fontFamily: BODY_FONT }}>Total : {total}Ã¢â€šÂ¬</span>          </div>          <PrimaryButton onClick={() => setShowCheckout(true)}><CreditCard size={14} /> Commander</PrimaryButton>        </Panel>      )}      {showCheckout && (        <Panel className="p-4">          <div className="flex items-center justify-between mb-3">            <h3 className="text-sm font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Finaliser la commande</h3>            <button onClick={() => setShowCheckout(false)} style={{ color: C.muted }}><X size={15} /></button>          </div>          <div className="mb-3">            {cartProducts.map((p) => (              <div key={p.id} className="flex items-center justify-between text-sm py-1">                <span style={{ color: C.text, fontFamily: BODY_FONT }}>{p.title}</span>                <span style={{ color: C.muted, fontFamily: BODY_FONT }}>{p.price}Ã¢â€šÂ¬</span>              </div>            ))}            <div className="flex items-center justify-between text-sm pt-2 mt-1 font-semibold" style={{ borderTop: `1px solid ${C.line}`, color: C.text, fontFamily: BODY_FONT }}>              <span>Total</span><span style={{ color: C.ok }}>{total}Ã¢â€šÂ¬</span>            </div>          </div>          <form onSubmit={submitOrder}>            <Field label="Nom complet"><input value={buyerName} onChange={(e) => setBuyerName(e.target.value)} className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>            <Field label="E-mail"><input type="email" value={buyerEmail} onChange={(e) => setBuyerEmail(e.target.value)} className="w-full px-3 py-2 rounded-md text-sm" style={inputStyle} /></Field>            {error && <p className="text-xs mb-2" style={{ color: C.alert, fontFamily: BODY_FONT }}>{error}</p>}            <PrimaryButton type="submit" disabled={processing}>{processing ? "Traitement..." : <><CreditCard size={14} /> Payer {total}Ã¢â€šÂ¬</>}</PrimaryButton>            <p className="text-xs mt-3" style={{ color: C.muted, fontFamily: BODY_FONT }}>              DÃƒÂ©mo : aucun paiement rÃƒÂ©el n'est traitÃƒÂ© ici (pas de connexion ÃƒÂ  Stripe / PayPal). En production, il faudrait brancher un vrai prestataire de paiement.            </p>          </form>        </Panel>      )}    </div>  );}/* ---------------------------------------------------------------------   Assistant IA--------------------------------------------------------------------- */function AIAssistantTab({ pseudo }) {  const [messages, setMessages] = useState([    { role: "assistant", content: `Salut ${pseudo} ! Je suis l'assistant GowlSec. Blocage sur un lab, question de mÃƒÂ©thodologie, aide pour prÃƒÂ©parer un rapport de pentest ? Dis-moi tout.` },  ]);  const [input, setInput] = useState("");  const [loading, setLoading] = useState(false);  const bottomRef = useRef(null);  useEffect(() => { bottomRef.current?.scrollIntoView({ behavior: "smooth" }); }, [messages.length, loading]);  async function send() {    if (!input.trim() || loading) return;    const userMsg = { role: "user", content: input.trim() };    const next = [...messages, userMsg];    setMessages(next);    setInput("");    setLoading(true);    try {      const response = await fetch("https://api.anthropic.com/v1/messages", {        method: "POST",        headers: { "Content-Type": "application/json" },        body: JSON.stringify({          model: "claude-sonnet-4-6",          max_tokens: 1000,          system: "Tu es l'assistant IA de GowlSec, une communautÃƒÂ© francophone de pentest et de CTF (TryHackMe, Root-Me). RÃƒÂ©ponds en franÃƒÂ§ais, de faÃƒÂ§on claire et pÃƒÂ©dagogique. Tu aides sur : la mÃƒÂ©thodologie de pentest, le dÃƒÂ©blocage sur des labs/CTF lÃƒÂ©gaux, Linux, les rapports d'audit, la prÃƒÂ©paration d'examens (ex. STI2D). Tu ne donnes jamais d'instructions pour attaquer un systÃƒÂ¨me rÃƒÂ©el sans autorisation.",          messages: next.map((m) => ({ role: m.role, content: m.content })),        }),      });      const data = await response.json();      const textBlocks = (data?.content || []).filter((b) => b.type === "text").map((b) => b.text);      const reply = textBlocks.join("\n").trim() || "DÃƒÂ©solÃƒÂ©, je n'ai pas pu gÃƒÂ©nÃƒÂ©rer de rÃƒÂ©ponse. RÃƒÂ©essaie.";      setMessages((m) => [...m, { role: "assistant", content: reply }]);    } catch {      setMessages((m) => [...m, { role: "assistant", content: "Une erreur est survenue en contactant l'assistant. RÃƒÂ©essaie dans un instant." }]);    } finally {      setLoading(false);    }  }  return (    <div>      <div className="mb-5 flex items-center gap-2">        <Bot size={20} style={{ color: C.primary }} />        <div>          <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Assistant IA</h2>          <p className="text-sm" style={{ color: C.muted, fontFamily: BODY_FONT }}>Une aide rapide en cas de blocage, disponible ÃƒÂ  toute heure.</p>        </div>      </div>      <Panel className="flex flex-col" style={{ height: 500 }}>        <div className="flex-1 overflow-y-auto px-4 py-4 space-y-3">          {messages.map((m, i) => (            <div key={i} className={`flex ${m.role === "user" ? "justify-end" : "justify-start"}`}>              <div className="max-w-[80%] px-3 py-2 rounded-lg text-sm whitespace-pre-wrap"                style={{                  background: m.role === "user" ? C.primary : C.panel2,                  color: m.role === "user" ? "#fff" : C.text,                  fontFamily: BODY_FONT,                  border: m.role === "user" ? "none" : `1px solid ${C.line}`,                }}>                {m.content}              </div>            </div>          ))}          {loading && (            <div className="flex justify-start">              <div className="px-3 py-2 rounded-lg text-sm flex items-center gap-2" style={{ background: C.panel2, color: C.muted, border: `1px solid ${C.line}`, fontFamily: BODY_FONT }}>                <Sparkles size={13} className="animate-pulse" /> l'assistant rÃƒÂ©flÃƒÂ©chit...              </div>            </div>          )}          <div ref={bottomRef} />        </div>        <div className="flex items-center gap-2 p-3" style={{ borderTop: `1px solid ${C.line}` }}>          <input value={input} onChange={(e) => setInput(e.target.value)} onKeyDown={(e) => e.key === "Enter" && send()}            placeholder="DÃƒÂ©cris ton problÃƒÂ¨me..." disabled={loading} className="flex-1 px-3 py-2 rounded-md text-sm" style={inputStyle} />          <PrimaryButton onClick={send} disabled={loading}><Send size={14} /></PrimaryButton>        </div>      </Panel>    </div>  );}/* ---------------------------------------------------------------------   Admin--------------------------------------------------------------------- */function AdminList({ title, icon, items, onDelete }) {  return (    <Panel className="p-4 mb-4">      <div className="flex items-center gap-2 mb-3">        <span style={{ color: C.primary }}>{icon}</span>        <span className="text-sm font-semibold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{title}</span>        <span className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>({items.length})</span>      </div>      {items.length === 0 ? <p className="text-xs" style={{ color: C.muted, fontFamily: BODY_FONT }}>Rien ici.</p> : (        <div className="space-y-1.5 max-h-64 overflow-y-auto">          {items.map((it) => (            <div key={it.id} className="flex items-center justify-between gap-2 px-2.5 py-1.5 rounded-md" style={{ background: C.panel2 }}>              <div className="min-w-0">                <p className="text-xs truncate" style={{ color: C.text, fontFamily: BODY_FONT }}>{it.primary}</p>                <p className="text-xs" style={{ color: C.muted, fontFamily: MONO_FONT }}>{it.secondary}</p>              </div>              <button onClick={() => onDelete(it.id)} style={{ color: C.alert }} className="shrink-0"><Trash2 size={13} /></button>            </div>          ))}        </div>      )}    </Panel>  );}function AdminTab({  isAdmin, setIsAdmin, questions, setQuestions, messages, setMessages, trophies, setTrophies,  news, setNews, profiles, teams, setTeams, teamAnnouncements, setTeamAnnouncements, orders, setOrders,  labs, setLabs, labMessages, setLabMessages,}) {  const [pwd, setPwd] = useState("");  const [error, setError] = useState(false);  const [presence, setPresence] = useState([]);  const [loadingPresence, setLoadingPresence] = useState(false);  async function refreshPresence() {    setLoadingPresence(true);    try {      const listing = await window.storage.list("gowlsec:presence:", true);      const keys = listing?.keys || [];      const results = await Promise.all(keys.map(async (k) => {        try {          const r = await window.storage.get(k, true);          return r ? JSON.parse(r.value) : null;        } catch { return null; }      }));      setPresence(results.filter(Boolean));    } catch { /* best effort */ }    setLoadingPresence(false);  }  useEffect(() => { if (isAdmin) refreshPresence(); }, [isAdmin]);  function tryLogin(e) {    e.preventDefault();    if (pwd === ADMIN_PASSWORD) { setIsAdmin(true); setError(false); } else { setError(true); }  }  async function clearAll() {    setQuestions([]); setMessages([]); setTrophies([]);    saveCollection("gowlsec:questions", []);    saveCollection("gowlsec:chat", []);    saveCollection("gowlsec:trophies", []);  }  if (!isAdmin) {    return (      <div className="max-w-sm mx-auto text-center py-10">        <Lock size={28} className="mx-auto mb-3" style={{ color: C.primary }} />        <h2 className="text-lg font-bold mb-1" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Panel admin</h2>        <p className="text-sm mb-5" style={{ color: C.muted, fontFamily: BODY_FONT }}>AccÃƒÂ¨s rÃƒÂ©servÃƒÂ© Ã¢â‚¬â€ mot de passe requis.</p>        <form onSubmit={tryLogin}>          <input type="password" value={pwd} onChange={(e) => setPwd(e.target.value)} placeholder="mot de passe" className="w-full px-3 py-2 rounded-md text-sm mb-2 text-center" style={inputStyle} />          {error && <p className="text-xs mb-2" style={{ color: C.alert, fontFamily: BODY_FONT }}>Mot de passe incorrect.</p>}          <PrimaryButton type="submit"><Unlock size={14} /> DÃƒÂ©verrouiller</PrimaryButton>        </form>        <p className="text-xs mt-4" style={{ color: C.muted, fontFamily: BODY_FONT }}>DÃƒÂ©mo : ce mot de passe protÃƒÂ¨ge seulement l'affichage local.</p>      </div>    );  }  const now = Date.now();  const onlineCount = presence.filter((p) => now - new Date(p.lastSeen).getTime() < ONLINE_WINDOW_MS).length;  const totalRevenue = orders.reduce((s, o) => s + o.total, 0);  return (    <div>      <div className="flex items-center justify-between mb-5">        <div>          <h2 className="text-xl font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Panel admin</h2>          <p className="text-sm mt-1" style={{ color: C.muted, fontFamily: BODY_FONT }}>ModÃƒÂ©ration et suivi de la communautÃƒÂ©.</p>        </div>        <GhostButton onClick={() => setIsAdmin(false)}><Lock size={12} /> Se dÃƒÂ©connecter</GhostButton>      </div>      <div className="grid sm:grid-cols-3 lg:grid-cols-8 gap-3 mb-6">        <StatChip icon={<Wifi size={14} />} label="en ligne" value={onlineCount} />        <StatChip icon={<UserIcon size={14} />} label="profils" value={profiles.length} />        <StatChip icon={<Newspaper size={14} />} label="actus" value={news.length} />        <StatChip icon={<Flag size={14} />} label="questions" value={questions.length} />        <StatChip icon={<MessageSquare size={14} />} label="messages" value={messages.length} />        <StatChip icon={<Trophy size={14} />} label="trophÃƒÂ©es" value={trophies.length} />        <StatChip icon={<Bug size={14} />} label="labs" value={labs.length} />        <StatChip icon={<ShoppingCart size={14} />} label="ventes" value={orders.length} />      </div>      <Panel className="p-4 mb-6">        <div className="flex items-center justify-between mb-3 flex-wrap gap-2">          <div className="flex items-center gap-2">            <Activity size={15} style={{ color: C.ok }} />            <span className="text-sm font-semibold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Utilisateurs</span>          </div>          <GhostButton onClick={refreshPresence}><RefreshCw size={12} className={loadingPresence ? "animate-spin" : ""} /> Actualiser</GhostButton>        </div>        {profiles.length === 0 ? <p className="text-xs" style={{ color: C.muted, fontFamily: BODY_FONT }}>Aucun profil enregistrÃƒÂ©.</p> : (          <div className="overflow-x-auto">            <table className="w-full text-xs" style={{ fontFamily: BODY_FONT }}>              <thead>                <tr style={{ color: C.muted, fontFamily: MONO_FONT }}>                  <th className="text-left font-normal pb-2">Statut</th>                  <th className="text-left font-normal pb-2">Utilisateur</th>                  <th className="text-left font-normal pb-2">E-mail</th>                  <th className="text-left font-normal pb-2">Connexion</th>                  <th className="text-left font-normal pb-2">Membre depuis</th>                </tr>              </thead>              <tbody>                {profiles.map((p) => {                  const pres = presence.find((x) => x.userId === p.id);                  const online = pres && (now - new Date(pres.lastSeen).getTime() < ONLINE_WINDOW_MS);                  return (                    <tr key={p.id} style={{ borderTop: `1px solid ${C.line}` }}>                      <td className="py-2">                        <span className="inline-flex items-center gap-1.5">                          <Circle size={8} fill={online ? C.ok : C.muted} color={online ? C.ok : C.muted} />                          {online ? "En ligne" : "Hors ligne"}                        </span>                      </td>                      <td className="py-2" style={{ color: C.text }}>{p.username}</td>                      <td className="py-2" style={{ color: C.text }}>{p.email || "Ã¢â‚¬â€"}</td>                      <td className="py-2">{p.provider === "discord" ? "Discord" : "E-mail"}</td>                      <td className="py-2" style={{ color: C.muted }}>{timeAgo(p.joinedAt)}</td>                    </tr>                  );                })}              </tbody>            </table>          </div>        )}      </Panel>      <Panel className="p-4 mb-6">        <div className="flex items-center gap-2 mb-1"><AlertTriangle size={15} style={{ color: C.alert }} /><span className="text-sm font-semibold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>Zone dangereuse</span></div>        <p className="text-xs mb-3" style={{ color: C.muted, fontFamily: BODY_FONT }}>Supprime tout le contenu partagÃƒÂ© (questions, messages, trophÃƒÂ©es).</p>        <GhostButton danger onClick={clearAll}><Trash2 size={12} /> Tout rÃƒÂ©initialiser</GhostButton>      </Panel>      <AdminList title="ActualitÃƒÂ©s & annonces" icon={<Newspaper size={14} />} items={news.map((n) => ({ id: n.id, primary: n.title, secondary: `${n.ref} Ã‚Â· ${n.source}` }))}        onDelete={(id) => { const next = news.filter((n) => n.id !== id); setNews(next); saveCollection("gowlsec:news", next); }} />      <AdminList title="Questions" icon={<Flag size={14} />} items={questions.map((q) => ({ id: q.id, primary: q.title, secondary: `${q.author} Ã‚Â· ${timeAgo(q.createdAt)}` }))}        onDelete={(id) => { const next = questions.filter((q) => q.id !== id); setQuestions(next); saveCollection("gowlsec:questions", next); }} />      <AdminList title="Messages des salons" icon={<MessageSquare size={14} />} items={messages.map((m) => ({ id: m.id, primary: m.text, secondary: `#${m.room || "general"} Ã‚Â· ${m.author}` }))}        onDelete={(id) => { const next = messages.filter((m) => m.id !== id); setMessages(next); saveCollection("gowlsec:chat", next); }} />      <AdminList title="TrophÃƒÂ©es" icon={<Trophy size={14} />} items={trophies.map((t) => ({ id: t.id, primary: `${t.platform} Ã¢â‚¬â€ ${t.title}`, secondary: `${t.author} Ã‚Â· ${timeAgo(t.createdAt)}` }))}        onDelete={(id) => { const next = trophies.filter((t) => t.id !== id); setTrophies(next); saveCollection("gowlsec:trophies", next); }} />      <AdminList title="Team" icon={<Users size={14} />} items={teams.map((t) => ({ id: t.id, primary: `${t.name} (${t.visibility === "private" ? "privÃƒÂ©e" : "publique"})`, secondary: `${t.members.length}/${t.maxMembers || TEAM_MAX_MEMBERS} membre(s) Ã‚Â· capitaine ${t.owner}` }))}        onDelete={(id) => {          const next = teams.filter((t) => t.id !== id); setTeams(next); saveCollection("gowlsec:teams", next);          const na = teamAnnouncements.filter((a) => a.teamId !== id); setTeamAnnouncements(na); saveCollection("gowlsec:team_announcements", na);        }} />      <AdminList title="Salons labs" icon={<Bug size={14} />} items={labs.map((l) => ({ id: l.id, primary: `${l.title} (${l.visibility === "private" ? "privÃƒÂ©" : "public"})`, secondary: `${l.members.length}/${l.maxMembers || LAB_MAX_MEMBERS} membre(s) Ã‚Â· ${l.owner}` }))}        onDelete={(id) => {          const next = labs.filter((l) => l.id !== id); setLabs(next); saveCollection("gowlsec:labs", next);          const nm = labMessages.filter((m) => m.labId !== id); setLabMessages(nm); saveCollection("gowlsec:lab_messages", nm);        }} />      <AdminList title="Commandes boutique" icon={<ShoppingCart size={14} />} items={orders.map((o) => ({ id: o.id, primary: `${o.items.join(", ")} Ã¢â‚¬â€ ${o.total}Ã¢â€šÂ¬`, secondary: `${o.buyer} Ã‚Â· ${o.email} Ã‚Â· ${timeAgo(o.createdAt)}` }))}        onDelete={(id) => { const next = orders.filter((o) => o.id !== id); setOrders(next); saveCollection("gowlsec:orders", next); }} />      <Panel className="p-4">        <div className="flex items-center justify-between">          <span className="text-sm" style={{ color: C.muted, fontFamily: BODY_FONT }}>Chiffre d'affaires simulÃƒÂ©</span>          <span className="text-lg font-bold" style={{ color: C.ok, fontFamily: DISPLAY_FONT }}>{totalRevenue}Ã¢â€šÂ¬</span>        </div>      </Panel>    </div>  );}/* ---------------------------------------------------------------------   App shell--------------------------------------------------------------------- */const TABS = [  { key: "accueil", label: "Accueil" },  { key: "actus", label: "ActualitÃƒÂ©s" },  { key: "forum", label: "Question" },  { key: "salons", label: "Hub" },  { key: "equipes", label: "Team" },  { key: "labs", label: "Labs" },  { key: "classement", label: "Classement" },  { key: "trophies", label: "TrophÃƒÂ©es" },  { key: "boutique", label: "Boutique" },  { key: "assistant", label: "Assistant IA" },  { key: "admin", label: "Admin" },];export default function GowlSec() {  const [tab, setTab] = useState("accueil");  const [currentUser, setCurrentUser] = useState(null);  const [guestPseudo] = useState("invite" + Math.floor(Math.random() * 900 + 100));  const [isAdmin, setIsAdmin] = useState(false);  const [lang, setLang] = useState(null);  const [langLoading, setLangLoading] = useState(true);  useEffect(() => {    (async () => {      try {        const res = await window.storage.get("gowlsec:lang", false);        if (res?.value) setLang(res.value);      } catch { /* best effort */ }      setLangLoading(false);    })();  }, []);  async function chooseLang(key) {    setLang(key);    try { await window.storage.set("gowlsec:lang", key, false); } catch { /* best effort */ }  }  const L = (key) => t(lang || "fr", key);  const [profiles, setProfiles] = useState([]);  const [credentials, setCredentials] = useState([]);  const [questions, setQuestions] = useState([]);  const [messages, setMessages] = useState([]);  const [trophies, setTrophies] = useState([]);  const [news, setNews] = useState(SEED_NEWS);  const [teams, setTeams] = useState([]);  const [teamAnnouncements, setTeamAnnouncements] = useState([]);  const [labs, setLabs] = useState([]);  const [labMessages, setLabMessages] = useState([]);  const [orders, setOrders] = useState([]);  const [loading, setLoading] = useState(true);  const [liveCount, setLiveCount] = useState(null);  const pseudo = currentUser?.username || guestPseudo;  useEffect(() => {    let active = true;    async function refreshLive() {      try {        const listing = await window.storage.list("gowlsec:presence:", true);        const keys = listing?.keys || [];        const results = await Promise.all(keys.map(async (k) => {          try {            const r = await window.storage.get(k, true);            return r ? JSON.parse(r.value) : null;          } catch { return null; }        }));        if (!active) return;        const now = Date.now();        setLiveCount(results.filter(Boolean).filter((p) => now - new Date(p.lastSeen).getTime() < ONLINE_WINDOW_MS).length);      } catch { /* best effort */ }    }    refreshLive();    const interval = setInterval(refreshLive, 30000);    return () => { active = false; clearInterval(interval); };  }, []);  useEffect(() => {    (async () => {      const [pr, cr, q, m, t, n, tm, ta, ord, lb, lm] = await Promise.all([        loadCollection("gowlsec:profiles", []),        loadCollection("gowlsec:credentials", [], false),        loadCollection("gowlsec:questions", []),        loadCollection("gowlsec:chat", []),        loadCollection("gowlsec:trophies", []),        loadCollection("gowlsec:news", SEED_NEWS),        loadCollection("gowlsec:teams", []),        loadCollection("gowlsec:team_announcements", []),        loadCollection("gowlsec:orders", []),        loadCollection("gowlsec:labs", []),        loadCollection("gowlsec:lab_messages", []),      ]);      setProfiles(pr); setCredentials(cr); setQuestions(q); setMessages(m); setTrophies(t); setNews(n);      setTeams(tm); setTeamAnnouncements(ta); setOrders(ord); setLabs(lb); setLabMessages(lm);      setLoading(false);    })();  }, []);  // prÃƒÂ©sence Ã¢â‚¬â€ heartbeat pour le suivi admin des utilisateurs connectÃƒÂ©s  useEffect(() => {    if (!currentUser) return;    let active = true;    async function heartbeat() {      if (!active) return;      await saveCollection(`gowlsec:presence:${currentUser.id}`, {        userId: currentUser.id, username: currentUser.username, email: currentUser.email, lastSeen: new Date().toISOString(),      }, true);    }    heartbeat();    const interval = setInterval(heartbeat, 25000);    return () => { active = false; clearInterval(interval); };  }, [currentUser]);  const stats = useMemo(() => ({ questions: questions.length, messages: messages.length, trophies: trophies.length, news: news.length, teams: teams.length, labs: labs.length }), [questions, messages, trophies, news, teams, labs]);  const announcements = useMemo(() => news.filter((n) => n.category === "annonce").slice(0, 3), [news]);  const featuredNews = useMemo(() => {    const pool = news.filter((n) => n.category !== "annonce");    const list = pool.length > 0 ? pool : news;    if (list.length === 0) return null;    return list[dayOfYear() % list.length];  }, [news]);  const activityFeed = useMemo(() => {    const items = [      ...questions.map((q) => ({ kind: "question", id: q.id, author: q.author, text: q.title, createdAt: q.createdAt, tab: "forum" })),      ...trophies.map((t) => ({ kind: "trophy", id: t.id, author: t.author, text: `${t.platform} Ã¢â‚¬â€ ${t.title}`, createdAt: t.createdAt, tab: "trophies" })),      ...teams.map((t) => ({ kind: "team", id: t.id, author: t.owner, text: `a crÃƒÂ©ÃƒÂ© la team ${t.name}`, createdAt: t.createdAt, tab: "equipes" })),      ...labs.map((l) => ({ kind: "lab", id: l.id, author: l.owner, text: `a ouvert le salon lab ${l.title}`, createdAt: l.createdAt, tab: "labs" })),      ...messages.slice(-30).map((m) => ({ kind: "message", id: m.id, author: m.author, text: m.text, createdAt: m.createdAt, tab: "salons" })),    ];    return items.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt)).slice(0, 7);  }, [questions, trophies, teams, labs, messages]);  const topLeaders = useMemo(() => {    const scores = {};    const add = (author, pts) => { if (!author) return; scores[author] = (scores[author] || 0) + pts; };    trophies.forEach((t) => add(t.author, TROPHY_POINTS[t.difficulty] || 10));    questions.forEach((q) => { add(q.author, 2); (q.answers || []).forEach((a) => add(a.author, 3)); });    labs.forEach((l) => add(l.owner, 5));    return Object.entries(scores).map(([author, total]) => ({ author, total })).sort((a, b) => b.total - a.total).slice(0, 3);  }, [questions, trophies, labs]);  const openLabs = useMemo(() => labs.filter((l) => l.members.length < (l.maxMembers || LAB_MAX_MEMBERS)).slice(0, 3), [labs]);  const tickerItems = useMemo(() => {    const fromActivity = activityFeed.map((item) => {      const verbs = { question: "a posÃƒÂ© une question", trophy: "a dÃƒÂ©bloquÃƒÂ© un trophÃƒÂ©e", team: "a crÃƒÂ©ÃƒÂ© une team", lab: "a ouvert un salon lab", message: "discute sur le Hub" };      return `${item.author} ${verbs[item.kind] || "vient d'agir"}`;    });    const fromNews = news.slice(0, 4).map((n) => `Ã°Å¸â€œÂ° ${n.title}`);    const combined = [...fromActivity, ...fromNews];    if (combined.length === 0) {      return ["Bienvenue sur GowlSec Ã¢â‚¬â€ la communautÃƒÂ© dÃƒÂ©marre tout juste", "Pose ta premiÃƒÂ¨re question dans #Question", "Ouvre un salon lab et avance ÃƒÂ  plusieurs"];    }    return combined;  }, [activityFeed, news]);  return (    <div style={{ background: C.bg, minHeight: "100vh", fontFamily: BODY_FONT }}>      {!langLoading && !lang && <LanguageGate onChoose={chooseLang} />}      <style>{`        @import url('https://fonts.googleapis.com/css2?family=Sora:wght@500;600;700;800&family=Source+Sans+3:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap');        ::-webkit-scrollbar { width: 8px; height: 8px; }        ::-webkit-scrollbar-thumb { background: ${C.line}; border-radius: 4px; }        * { scrollbar-color: ${C.line} transparent; }        @keyframes gowl-pulse-dot { 0%, 100% { opacity: 1; transform: scale(1); } 50% { opacity: 0.4; transform: scale(0.7); } }        @keyframes gowl-ping-ring { 0% { transform: scale(0.9); opacity: 0.7; } 100% { transform: scale(2.2); opacity: 0; } }        @keyframes gowl-blob-drift { 0%, 100% { transform: translate(0, 0) scale(1); } 33% { transform: translate(20px, -24px) scale(1.08); } 66% { transform: translate(-16px, 14px) scale(0.96); } }        @keyframes gowl-ticker-scroll { from { transform: translateX(0); } to { transform: translateX(-50%); } }        @keyframes gowl-fade-up { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }        @keyframes gowl-glow-pulse { 0%, 100% { box-shadow: 0 0 0 0 ${C.primary}55; } 50% { box-shadow: 0 0 0 6px ${C.primary}00; } }        .gowl-fade-up { animation: gowl-fade-up 0.5s ease both; }        .gowl-ticker-track { animation: gowl-ticker-scroll 26s linear infinite; }        .gowl-ticker-track:hover { animation-play-state: paused; }        .gowl-navcard { transition: transform 0.2s ease, border-color 0.2s ease, background 0.2s ease; }        .gowl-navcard:hover { transform: translateY(-3px); border-color: ${C.primary}88 !important; background: ${C.panel2} !important; }        .gowl-navcard:hover .gowl-navcard-icon { transform: scale(1.1) rotate(-4deg); }        .gowl-navcard-icon { transition: transform 0.2s ease; }        .gowl-cta-glow { animation: gowl-glow-pulse 2.4s ease-in-out infinite; }        @keyframes gowl-scan-beam { 0% { top: 0%; opacity: 0; } 8% { opacity: 1; } 92% { opacity: 1; } 100% { top: 100%; opacity: 0; } }        @keyframes gowl-eye-scan { 0%, 100% { transform: translateX(-2.5px); } 50% { transform: translateX(2.5px); } }        @keyframes gowl-mascot-bob { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-7px); } }        @keyframes gowl-digit-flicker { 0%, 100% { opacity: var(--gowl-op, 0.3); } 50% { opacity: 0.05; } }        @keyframes gowl-crow-fly { 0% { transform: translate(-46%, 10px) rotate(-4deg); } 22% { transform: translate(-16%, -20px) rotate(3deg); } 50% { transform: translate(22%, 8px) rotate(-3deg); } 76% { transform: translate(42%, -16px) rotate(4deg); } 100% { transform: translate(-46%, 10px) rotate(-4deg); } }        @keyframes gowl-wing-flap-left { 0%, 100% { transform: rotate(-8deg); } 50% { transform: rotate(38deg); } }        @keyframes gowl-wing-flap-right { 0%, 100% { transform: rotate(8deg); } 50% { transform: rotate(-38deg); } }        .gowl-crow-fly-wrap { position: absolute; left: 50%; top: 40%; margin-left: -44px; margin-top: -44px; animation: gowl-crow-fly 9s ease-in-out infinite; }        .gowl-wing { transform-box: fill-box; }        .gowl-wing-left { transform-origin: 88% 50%; animation: gowl-wing-flap-left 0.55s ease-in-out infinite; }        .gowl-wing-right { transform-origin: 12% 50%; animation: gowl-wing-flap-right 0.55s ease-in-out infinite; }        .gowl-eye-pupil { animation: gowl-eye-scan 2.6s ease-in-out infinite; transform-box: fill-box; transform-origin: center; }        .gowl-mascot-wrap { animation: gowl-mascot-bob 3.2s ease-in-out infinite; }        .gowl-digit { animation: gowl-digit-flicker 4s ease-in-out infinite; animation-delay: var(--gowl-delay, 0s); }        @keyframes gowl-scene-float { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-5px); } }        .gowl-scene-float { animation: gowl-scene-float 3.6s ease-in-out infinite; transform-box: fill-box; transform-origin: center; }        @keyframes gowl-shield-glow { 0%, 100% { filter: drop-shadow(0 0 0px ${C.alert}); } 50% { filter: drop-shadow(0 0 6px ${C.alert}AA); } }        .gowl-shield-glow { animation: gowl-shield-glow 2.4s ease-in-out infinite; }        @keyframes gowl-cursor-blink { 0%, 100% { opacity: 1; } 50% { opacity: 0.15; } }        .gowl-cursor-blink { animation: gowl-cursor-blink 1s steps(2) infinite; }      `}</style>      <header className="sticky top-0 z-30" style={{ background: `${C.bg}F2`, backdropFilter: "blur(10px)", borderBottom: `1px solid ${C.line}`, boxShadow: "0 1px 0 rgba(0,0,0,0.4), 0 8px 24px -16px rgba(0,0,0,0.6)" }}>        <div aria-hidden style={{ height: 2, background: `linear-gradient(90deg, ${C.primary}, ${C.gold}, ${C.ok})`, opacity: 0.7 }} />        <div className="max-w-6xl mx-auto px-4 h-[70px] flex items-center justify-between gap-4">          <div className="flex items-center gap-5 min-w-0">            <button onClick={() => setTab("accueil")} className="flex items-center gap-2.5 shrink-0 group">              <span className="relative flex items-center justify-center w-10 h-10 rounded-xl transition-transform group-hover:scale-105" style={{ background: `linear-gradient(155deg, ${C.panel2}, ${C.bg})`, border: `1px solid ${C.line}`, boxShadow: `0 0 0 1px ${C.primary}22 inset` }}>                <CrowLogo size={20} />              </span>              <span className="flex flex-col leading-none">                <span className="font-extrabold text-lg tracking-tight" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>GowlSec</span>                <span className="text-[10px] uppercase tracking-widest" style={{ color: C.muted, fontFamily: MONO_FONT }}>{L("tagline")}</span>              </span>            </button>            <span className="hidden lg:block w-px h-7 shrink-0" style={{ background: C.line }} />            <nav className="hidden md:flex items-center gap-0.5 overflow-x-auto py-1">              {TABS.filter((t) => t.key !== "admin").map((t) => {                const active = tab === t.key;                return (                  <button key={t.key} onClick={() => setTab(t.key)} className="relative px-3 py-[7px] text-[13px] font-semibold whitespace-nowrap rounded-lg transition-all duration-200"                    style={{                      color: active ? "#fff" : C.muted,                      background: active ? `linear-gradient(155deg, ${C.primary}, ${C.primary}CC)` : "transparent",                      boxShadow: active ? `0 4px 14px -4px ${C.primary}88` : "none",                      fontFamily: BODY_FONT,                    }}                    onMouseEnter={(e) => { if (!active) e.currentTarget.style.background = C.panel2; }}                    onMouseLeave={(e) => { if (!active) e.currentTarget.style.background = "transparent"; }}                  >                    {(I18N[lang || "fr"].tabs[t.key]) || t.label}                  </button>                );              })}            </nav>          </div>          <div className="flex items-center gap-2.5 shrink-0">            {isAdmin && (              <button onClick={() => setTab("admin")} className="hidden sm:inline-flex items-center gap-1.5 px-2.5 py-1.5 rounded-lg text-xs font-semibold" style={{ background: `${C.gold}14`, border: `1px solid ${C.gold}44`, color: C.gold, fontFamily: MONO_FONT }}>                <Shield size={12} /> {L("admin")}              </button>            )}            <div className="hidden sm:flex items-center gap-2.5 pr-2.5" style={{ borderRight: `1px solid ${C.line}` }}>              <OnlineIndicator />            </div>            <LangSwitcher lang={lang || "fr"} onChoose={chooseLang} />            <AuthWidget currentUser={currentUser} setCurrentUser={setCurrentUser} profiles={profiles} setProfiles={setProfiles} credentials={credentials} setCredentials={setCredentials} setTab={setTab} />          </div>        </div>        <nav className="md:hidden flex items-center gap-1 px-4 pb-2.5 overflow-x-auto">          {TABS.map((t) => {            const active = tab === t.key;            return (              <button key={t.key} onClick={() => setTab(t.key)} className="px-3 py-1.5 text-xs font-semibold rounded-lg whitespace-nowrap transition-colors"                style={{ color: active ? "#fff" : C.muted, background: active ? C.primary : C.panel, border: `1px solid ${active ? C.primary : C.line}`, fontFamily: BODY_FONT }}>{(I18N[lang || "fr"].tabs[t.key]) || t.label}</button>            );          })}        </nav>      </header>      <main className="max-w-6xl mx-auto px-4 py-8">        {loading ? (          <div className="flex items-center justify-center py-24 gap-2 text-sm" style={{ color: C.muted, fontFamily: BODY_FONT }}>            <RefreshCw size={14} className="animate-spin" /> chargement...          </div>        ) : (          <>            {tab === "accueil" && (              <div className="space-y-10">                {/* Ticker live Ã¢â‚¬â€ donne le pouls de la communautÃƒÂ© en un coup d'Ã…â€œil */}                <div className="overflow-hidden rounded-lg" style={{ border: `1px solid ${C.line}`, background: C.panel }}>                  <div className="flex items-center">                    <div className="flex items-center gap-1.5 px-3 py-2 shrink-0" style={{ background: `${C.alert}14`, borderRight: `1px solid ${C.line}` }}>                      <Radio size={12} style={{ color: C.alert }} />                      <span className="text-xs font-bold tracking-wide" style={{ color: C.alert, fontFamily: MONO_FONT }}>{L("live2")}</span>                    </div>                    <div className="overflow-hidden flex-1">                      <div className="flex gowl-ticker-track" style={{ width: "max-content" }}>                        {[...tickerItems, ...tickerItems{ icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },`n].map((t, i) => (                          <span key={i} className="flex items-center gap-2 px-4 py-2 text-xs shrink-0" style={{ color: C.text, fontFamily: BODY_FONT }}>                            <Zap size={11} style={{ color: C.gold }} /> {t}                          </span>                        ))}                      </div>                    </div>                  </div>                </div>                {/* Hero */}                <div className="relative overflow-hidden rounded-2xl px-1 py-2" style={{ isolation: "isolate" }}>                  <div aria-hidden className="absolute rounded-full pointer-events-none" style={{ width: 420, height: 420, top: -160, left: -120, background: `radial-gradient(circle, ${C.primary}30, transparent 70%)`, filter: "blur(10px)", animation: "gowl-blob-drift 14s ease-in-out infinite", zIndex: -1 }} />                  <div aria-hidden className="absolute rounded-full pointer-events-none" style={{ width: 340, height: 340, top: -80, right: -100, background: `radial-gradient(circle, ${C.gold}22, transparent 70%)`, filter: "blur(10px)", animation: "gowl-blob-drift 18s ease-in-out infinite reverse", zIndex: -1 }} />                  <div className="grid md:grid-cols-2 gap-8 items-center py-4">                    <div className="gowl-fade-up">                      <div className="flex items-center gap-2 mb-4 flex-wrap">                        <LivePulseBadge count={liveCount} />                        <span className="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs" style={{ border: `1px solid ${C.line}`, color: C.muted, fontFamily: MONO_FONT }}>                          <Gauge size={12} style={{ color: C.ok }} /> {L("watching")}                        </span>                      </div>                      <h1 className="text-4xl md:text-5xl font-extrabold leading-tight mb-4" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>                        {L("heroTitle1")}<span style={{ background: `linear-gradient(90deg, ${C.primary}, ${C.gold})`, WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent", backgroundClip: "text" }}>{L("heroTitle2")}</span>                      </h1>                      <p className="text-base leading-relaxed mb-5" style={{ color: C.muted }}>                        {L("heroSub")}                      </p>                      <div className="flex items-center gap-3 mb-6 flex-wrap">                        <MemberAvatarStack profiles={profiles} />                        <span className="text-xs" style={{ color: C.muted, fontFamily: BODY_FONT }}>                          {profiles.length > 0 ? <><b style={{ color: C.text }}>{profiles.length}</b> {L("membersJoined")}</> : L("joinFirst")} Ã‚Â· {L("peerHelp")}                        </span>                      </div>                      <div className="flex flex-wrap gap-3 mb-6">                        <PrimaryButton onClick={() => setTab("forum")} style={{ boxShadow: `0 0 0 0 ${C.primary}55` }}><span className="gowl-cta-glow rounded-md inline-flex items-center gap-2"><Zap size={14} /> {L("ctaAsk")}</span></PrimaryButton>                        <GhostButton onClick={() => setTab("salons")}><Hash size={13} /> {L("ctaHub")}</GhostButton>                      </div>                      <div className="flex flex-wrap items-stretch rounded-xl overflow-hidden" style={{ border: `1px solid ${C.line}`, background: C.panel }}>                        {[                          { icon: <Newspaper size={13} />, label: L("statNews"), value: stats.news },                          { icon: <Flag size={13} />, label: L("statQuestions"), value: stats.questions },                          { icon: <Trophy size={13} />, label: L("statTrophies"), value: stats.trophies },                          { icon: <Users size={13} />, label: L("statTeams"), value: stats.teams },                          { icon: <Bug size={13} />, label: L("statLabs"), value: stats.labs },                        { icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },`n                        ].map((s, i) => (                          <div key={s.label} className="flex-1 min-w-[84px] px-3 py-2.5" style={{ borderLeft: i === 0 ? "none" : `1px solid ${C.line}` }}>                            <div className="flex items-center gap-1.5 mb-0.5" style={{ color: C.primary }}>{s.icon}<span className="text-base font-bold" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{s.value}</span></div>                            <span className="text-[11px]" style={{ color: C.muted, fontFamily: MONO_FONT }}>{s.label}</span>                          </div>                        ))}                      </div>                    </div>                    <div className="flex flex-col gap-3">                      <Panel className="p-3.5">                        <div className="flex items-center gap-2 mb-2.5">                          <Activity size={13} style={{ color: C.ok }} />                          <span className="text-xs font-bold uppercase tracking-wide" style={{ color: C.muted, fontFamily: MONO_FONT }}>{L("activityTitle")}</span>                        </div>                        <div className="space-y-2">                          {activityFeed.length === 0 && <p className="text-xs" style={{ color: C.muted, fontFamily: BODY_FONT }}>{L("activityEmpty")}</p>}                          {activityFeed.slice(0, 3).map((item) => (                            <button key={`${item.kind}-${item.id}`} onClick={() => setTab(item.tab)} className="flex items-center gap-2 w-full text-left px-2 py-1.5 rounded-md transition-colors hover:opacity-90" style={{ background: C.panel2 }}>                              <div className="w-6 h-6 rounded-full flex items-center justify-center text-[10px] font-bold shrink-0" style={{ background: `${C.primary}22`, color: C.primary, fontFamily: MONO_FONT }}>{item.author.slice(0, 2).toUpperCase()}</div>                              <p className="text-xs truncate flex-1" style={{ color: C.text, fontFamily: BODY_FONT }}><b>{item.author}</b> {item.text}</p>                              <span className="text-[10px] shrink-0" style={{ color: C.muted, fontFamily: MONO_FONT }}>{timeAgo(item.createdAt)}</span>                            </button>                          ))}                        </div>                      </Panel>                      <div className="grid grid-cols-1 gap-3">                        {[                          { icon: <Shield size={18} />, title: L("tabs").forum, desc: L("cardForumDesc"), tab: "forum", color: C.primary },                          { icon: <Hash size={18} />, title: L("tabs").salons, desc: L("cardHubDesc"), tab: "salons", color: C.ok },                          { icon: <Users size={18} />, title: L("tabs").equipes, desc: L("cardTeamDesc"), tab: "equipes", color: C.warn },                          { icon: <Bug size={18} />, title: L("tabs").labs, desc: L("cardLabsDesc"), tab: "labs", color: C.alert },                        { icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },`n                        ].map((c, i) => (                          <div key={c.tab} className="gowl-fade-up" style={{ animationDelay: `${i * 60}ms` }}>                            <NavCard icon={c.icon} title={c.title} desc={c.desc} onClick={() => setTab(c.tab)} accent={c.color} />                          </div>                        ))}                      </div>                    </div>                  </div>                </div>                {/* Le reste de la communautÃƒÂ©, en accÃƒÂ¨s rapide */}                <div className="grid sm:grid-cols-4 gap-3">                  {[                     { icon: <Trophy size={18} />, title: L("tabs").classement, desc: L("cardClassementDesc"), tab: "classement", color: C.gold },                    { icon: <ShoppingCart size={18} />, title: L("tabs").boutique, desc: L("cardBoutiqueDesc"), tab: "boutique", color: C.primary },                    { icon: <Bot size={18} />, title: L("tabs").assistant, desc: L("cardAssistantDesc"), tab: "assistant", color: C.ok },                    { icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },                  ].map((c) => (                    <NavCard key={c.tab} icon={c.icon} title={c.title} desc={c.desc} onClick={() => setTab(c.tab)} accent={c.color} />                  ))}                </div>                <DataScanScene onClick={() => setTab("salons")} label={L("scanLabel")} />                <div>                  <div className="flex items-center gap-2 mb-3">                    <Newspaper size={16} style={{ color: C.primary }} />                    <h2 className="text-sm font-bold uppercase tracking-wide" style={{ color: C.muted, fontFamily: MONO_FONT }}>{L("trending")}</h2>                  </div>                  <NewsMarquee news={news} />                  <div className="text-center mt-4">                    <GhostButton onClick={() => setTab("actus")}>{L("watchAll")}</GhostButton>                  </div>                </div>              </div>            )}            {tab === "actus" && <NewsTab news={news} setNews={setNews} isAdmin={isAdmin} full />}            {tab === "forum" && <ForumTab pseudo={pseudo} questions={questions} setQuestions={setQuestions} isAdmin={isAdmin} lang={lang || "fr"} />}            {tab === "salons" && <RoomsTab pseudo={pseudo} messages={messages} setMessages={setMessages} isAdmin={isAdmin} lang={lang || "fr"} />}            {tab === "equipes" && <TeamsTab pseudo={pseudo} teams={teams} setTeams={setTeams} announcements={teamAnnouncements} setAnnouncements={setTeamAnnouncements} isAdmin={isAdmin} lang={lang || "fr"} />}            {tab === "labs" && <LabsTab pseudo={pseudo} labs={labs} setLabs={setLabs} labMessages={labMessages} setLabMessages={setLabMessages} isAdmin={isAdmin} lang={lang || "fr"} />}            {tab === "classement" && <LeaderboardTab questions={questions} trophies={trophies} labs={labs} teams={teams} profiles={profiles} />}            {tab === "trophies" && <TrophyTab pseudo={pseudo} trophies={trophies} setTrophies={setTrophies} isAdmin={isAdmin} />}            {tab === "boutique" && <ShopTab pseudo={pseudo} currentUser={currentUser} orders={orders} setOrders={setOrders} />}            {tab === "assistant" && <AIAssistantTab pseudo={pseudo} />}`n            {tab === "connexion" && <ConnexionTab onLogin={() => setTab("accueil")} />}            {tab === "profil" && <ProfileTab currentUser={currentUser} setCurrentUser={setCurrentUser} profiles={profiles} setProfiles={setProfiles} questions={questions} trophies={trophies} setTab={setTab} />}            {tab === "admin" && (              <AdminTab isAdmin={isAdmin} setIsAdmin={setIsAdmin} questions={questions} setQuestions={setQuestions}                messages={messages} setMessages={setMessages} trophies={trophies} setTrophies={setTrophies} news={news} setNews={setNews} profiles={profiles}                teams={teams} setTeams={setTeams} teamAnnouncements={teamAnnouncements} setTeamAnnouncements={setTeamAnnouncements}                orders={orders} setOrders={setOrders} labs={labs} setLabs={setLabs} labMessages={labMessages} setLabMessages={setLabMessages} />            )}          </>        )}      </main>      <footer className="max-w-6xl mx-auto px-4 py-6 mt-6 flex items-center justify-center gap-2" style={{ borderTop: `1px solid ${C.line}` }}>        <CrowLogo size={14} />        <p className="text-xs text-center" style={{ color: C.muted, fontFamily: MONO_FONT }}>{L("footer")}</p>      </footer>    </div>  );}/* ---------------------------------------------------------------------   Mascotte corbeau Ã¢â‚¬â€ "fouille" la base de donnÃƒÂ©es (visuel central accueil)--------------------------------------------------------------------- */function CrowSearchMascot() {  return (    <div className="gowl-mascot-wrap relative">      <div aria-hidden className="absolute inset-0 rounded-full" style={{ background: `radial-gradient(circle, ${C.ok}33, transparent 70%)`, filter: "blur(6px)", transform: "scale(1.6)" }} />      <svg width="88" height="88" viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg" className="relative" overflow="visible">        <path className="gowl-wing gowl-wing-left" d="M27 48 C10 40 -2 44 2 57 C6 68 18 68 28 60 Z" fill={C.ok} opacity="0.9" />        <path className="gowl-wing gowl-wing-right" d="M73 48 C90 40 102 44 98 57 C94 68 82 68 72 60 Z" fill={C.ok} opacity="0.9" />        <path d="M50 18 C44 6 34 4 26 9 C36 9 41 14 44 22 Z" fill={C.ok} />        <path d="M50 18 C56 6 66 4 74 9 C64 9 59 14 56 22 Z" fill={C.ok} />        <ellipse cx="50" cy="58" rx="30" ry="27" fill="#10151D" stroke={C.ok} strokeWidth="2.5" />        <polygon points="50,63 43,73 57,73" fill="#FFB454" />        <circle cx="37" cy="52" r="9" fill="#fff" />        <circle cx="63" cy="52" r="9" fill="#fff" />        <circle className="gowl-eye-pupil" cx="37" cy="52" r="4" fill="#0A0C10" />        <circle className="gowl-eye-pupil" cx="63" cy="52" r="4" fill="#0A0C10" />      </svg>    </div>  );}function DataScanScene({ onClick, label = "corbeau en fouille dans la base de donnÃƒÂ©es" }) {  const COLS = 16, ROWS = 6;  const cells = useMemo(() => Array.from({ length: COLS * ROWS }).map(() => {    const show = Math.random() < 0.48;    if (!show) return null;    return { val: Math.random() < 0.5 ? "0" : "1", op: (0.15 + Math.random() * 0.35).toFixed(2), delay: (Math.random() * 4).toFixed(2) };  }), []);  return (    <button onClick={onClick} className="relative w-full overflow-hidden rounded-xl text-left" style={{ height: 300, background: C.panel, border: `1px solid ${C.line}` }}>      <div className="absolute inset-0 grid p-4" style={{ gridTemplateColumns: `repeat(${COLS}, 1fr)`, gridTemplateRows: `repeat(${ROWS}, 1fr)` }}>        {cells.map((c, i) => (          <span key={i} className={c ? "gowl-digit flex items-center justify-center text-xs" : ""}            style={c ? { color: C.ok, fontFamily: MONO_FONT, "--gowl-op": c.op, "--gowl-delay": `${c.delay}s`, opacity: c.op } : {}}>            {c ? c.val : ""}          </span>        ))}      </div>      <div className="absolute left-0 right-0 pointer-events-none" style={{ height: 2, background: `linear-gradient(90deg, transparent, ${C.ok}, transparent)`, boxShadow: `0 0 14px 2px ${C.ok}88`, animation: "gowl-scan-beam 3.4s ease-in-out infinite" }} />      <div className="gowl-crow-fly-wrap pointer-events-none">        <CrowSearchMascot />      </div>      <div className="absolute left-1/2 bottom-4 -translate-x-1/2 flex items-center gap-1.5 px-3 py-1 rounded-full pointer-events-none" style={{ background: `${C.bg}CC`, border: `1px solid ${C.ok}33` }}>        <span className="text-xs" style={{ color: C.ok, fontFamily: MONO_FONT }}>{label}</span>        <span className="text-xs" style={{ color: C.ok, fontFamily: MONO_FONT, animation: "gowl-pulse-dot 1.1s steps(3) infinite" }}>...</span>      </div>    </button>  );}function NavCard({ icon, title, desc, onClick, accent = C.primary }) {  return (    <button onClick={onClick} className="gowl-navcard text-left p-4 rounded-lg flex items-start gap-3 w-full" style={{ background: C.panel, border: `1px solid ${C.line}` }}>      <span className="gowl-navcard-icon mt-0.5 w-9 h-9 rounded-md flex items-center justify-center shrink-0" style={{ background: `${accent}1A`, color: accent }}>{icon}</span>      <div className="min-w-0">        <h3 className="text-sm font-semibold mb-0.5" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{title}</h3>        <p className="text-xs" style={{ color: C.muted, fontFamily: BODY_FONT }}>{desc}</p>      </div>    </button>  );}/* ---------------------------------------------------------------------   ScÃƒÂ¨nes illustrÃƒÂ©es Ã¢â‚¬â€ habillage dynamique des ÃƒÂ©crans de crÃƒÂ©ation--------------------------------------------------------------------- */function SceneFrame({ children, accent }) {  return (    <div className="relative rounded-xl overflow-hidden mb-4" style={{ background: `radial-gradient(circle at 50% 20%, ${accent}22, transparent 65%)` }}>      {children}    </div>  );}function ForumScene() {  return (    <SceneFrame accent={C.primary}>      <svg viewBox="0 0 220 150" width="100%" height="140" xmlns="http://www.w3.org/2000/svg">        <rect x="28" y="34" width="132" height="90" rx="9" fill={C.panel2} stroke={C.line} strokeWidth="1.5" />        <rect x="28" y="34" width="132" height="20" rx="9" fill={C.bg} />        <circle cx="40" cy="44" r="3" fill={C.alert} /><circle cx="50" cy="44" r="3" fill={C.warn} /><circle cx="60" cy="44" r="3" fill={C.ok} />        <rect x="40" y="64" width="62" height="5" rx="2.5" fill={C.primary} opacity="0.75" />        <rect x="40" y="76" width="90" height="5" rx="2.5" fill={C.line} />        <rect x="40" y="88" width="72" height="5" rx="2.5" fill={C.line} />        <rect x="40" y="100" width="34" height="5" rx="2.5" fill={C.ok} className="gowl-cursor-blink" />        <g className="gowl-scene-float">          <circle cx="176" cy="46" r="21" fill={`${C.gold}22`} stroke={C.gold} strokeWidth="1.5" />          <text x="176" y="54" textAnchor="middle" fontSize="22" fontWeight="800" fill={C.gold} fontFamily={DISPLAY_FONT}>?</text>        </g>      </svg>    </SceneFrame>  );}function HubScene() {  return (    <SceneFrame accent={C.ok}>      <svg viewBox="0 0 220 150" width="100%" height="130" xmlns="http://www.w3.org/2000/svg">        <g className="gowl-scene-float">          <rect x="26" y="46" width="76" height="42" rx="11" fill={C.panel2} stroke={C.primary} strokeWidth="1.5" />          <path d="M42 88 L42 99 L56 88 Z" fill={C.panel2} stroke={C.primary} strokeWidth="1.5" />          <rect x="40" y="59" width="42" height="5" rx="2.5" fill={C.primary} opacity="0.75" />          <rect x="40" y="70" width="28" height="5" rx="2.5" fill={C.line} />        </g>        <g className="gowl-scene-float" style={{ animationDelay: "0.45s" }}>          <rect x="116" y="24" width="76" height="42" rx="11" fill={C.panel2} stroke={C.ok} strokeWidth="1.5" />          <path d="M160 66 L160 77 L146 66 Z" fill={C.panel2} stroke={C.ok} strokeWidth="1.5" />          <rect x="128" y="37" width="48" height="5" rx="2.5" fill={C.ok} opacity="0.75" />          <rect x="128" y="48" width="30" height="5" rx="2.5" fill={C.line} />        </g>        <circle cx="150" cy="112" r="4" fill={C.gold} className="gowl-cursor-blink" />        <circle cx="168" cy="120" r="3" fill={C.warn} />      </svg>    </SceneFrame>  );}function TeamScene() {  const crew = [{ x: 68, c: C.primary }, { x: 110, c: C.gold }, { x: 152, c: C.alert }];  return (    <SceneFrame accent={C.warn}>      <svg viewBox="0 0 220 150" width="100%" height="140" xmlns="http://www.w3.org/2000/svg">        <rect x="16" y="56" width="34" height="72" rx="4" fill={C.panel2} stroke={C.line} strokeWidth="1.5" />        {[0, 1, 2, 3{ icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },`n].map((i) => <rect key={i} x="22" y={64 + i * 16} width="22" height="8" rx="2" fill={i % 2 === 0 ? C.ok : C.line} opacity={i % 2 === 0 ? 0.85 : 0.5} />)}        <rect x="170" y="56" width="34" height="72" rx="4" fill={C.panel2} stroke={C.line} strokeWidth="1.5" />        {[0, 1, 2, 3{ icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },`n].map((i) => <rect key={i} x="176" y={64 + i * 16} width="22" height="8" rx="2" fill={i % 2 === 1 ? C.primary : C.line} opacity={i % 2 === 1 ? 0.85 : 0.5} />)}        <path d={`M${crew[0].x} 78 L${crew[1].x} 68 L${crew[2].x} 78`} fill="none" stroke={C.line} strokeWidth="1.5" strokeDasharray="3 3" />        {crew.map((h, i) => (          <g key={i} className="gowl-scene-float" style={{ animationDelay: `${i * 0.3}s` }}>            <path d={`M${h.x - 17} 132 Q${h.x - 17} 96 ${h.x} 93 Q${h.x + 17} 96 ${h.x + 17} 132 Z`} fill={C.bg} stroke={h.c} strokeWidth="1.5" />            <circle cx={h.x} cy="87" r="13" fill={C.bg} stroke={h.c} strokeWidth="1.5" />            <rect x={h.x - 6} y="82" width="12" height="6" rx="3" fill={h.c} opacity="0.85" />          </g>        ))}      </svg>    </SceneFrame>  );}function LabScene() {  return (    <SceneFrame accent={C.alert}>      <svg viewBox="0 0 220 150" width="100%" height="140" xmlns="http://www.w3.org/2000/svg">        <rect x="63" y="38" width="94" height="98" rx="7" fill={C.panel2} stroke={C.line} strokeWidth="1.5" />        {[0, 1, 2, 3{ icon: <User size={18} />, title: L("tabs").connexion, desc: "Se connecter Ã  votre compte", tab: "connexion", color: C.primary },`n].map((i) => (          <g key={i}>            <rect x="73" y={48 + i * 19} width="74" height="13" rx="2.5" fill={C.bg} stroke={C.line} />            <circle cx="82" cy={54.5 + i * 19} r="2.2" fill={i % 2 === 0 ? C.ok : C.warn} />          </g>        ))}        <g className="gowl-shield-glow">          <path d="M110 12 L140 23 V50 C140 72 127 87 110 94 C93 87 80 72 80 50 V23 Z" fill={C.bg} stroke={C.alert} strokeWidth="2.5" />          <path d="M96 51 L106 61 L125 39" fill="none" stroke={C.alert} strokeWidth="3.2" strokeLinecap="round" strokeLinejoin="round" />        </g>      </svg>    </SceneFrame>  );}function CreationHero({ scene, eyebrow, title, subtitle, accent, children }) {  return (    <Panel className="overflow-hidden mb-6" style={{ border: `1px solid ${accent}40`, boxShadow: `0 16px 40px -28px ${accent}AA` }}>      <div className="grid md:grid-cols-[0.92fr_1.3fr]">        <div className="relative flex flex-col justify-center px-6 pt-6 pb-5" style={{ background: `linear-gradient(165deg, ${accent}14, transparent 75%)`, borderRight: `1px solid ${C.line}` }}>          {scene}          <span className="text-[11px] font-bold uppercase tracking-widest mb-1.5" style={{ color: accent, fontFamily: MONO_FONT }}>{eyebrow}</span>          <h3 className="text-lg font-bold mb-1.5 leading-tight" style={{ color: C.text, fontFamily: DISPLAY_FONT }}>{title}</h3>          <p className="text-xs leading-relaxed" style={{ color: C.muted, fontFamily: BODY_FONT }}>{subtitle}</p>        </div>        <div className="p-5">{children}</div>      </div>    </Panel>  );}
